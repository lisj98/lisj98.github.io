<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Spring MVC错误集</title>
    <url>/2022/04/06/Spring-MVC%E9%94%99%E8%AF%AF%E9%9B%86/</url>
    <content><![CDATA[<h6 id="1-一个或多个listeners启动失败，更多详细信息查看对应的容器日志文件；由于之前的错误，Context-启动失败"><a href="#1-一个或多个listeners启动失败，更多详细信息查看对应的容器日志文件；由于之前的错误，Context-启动失败" class="headerlink" title="1. 一个或多个listeners启动失败，更多详细信息查看对应的容器日志文件；由于之前的错误，Context[]启动失败"></a>1. 一个或多个listeners启动失败，更多详细信息查看对应的容器日志文件；由于之前的错误，Context[]启动失败</h6><p>原因是项目工程中缺少 jar 包。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E5%AF%BC%E5%85%A5lib%E5%8C%851.1s2xghsb4crk.webp" alt="导入lib包1"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E5%AF%BC%E5%85%A5lib%E5%8C%852.z5a16pfzicg.webp" alt="导入lib包2"></p>
<h6 id="2-org-apache-jasper-servlet-TldScanner-scanJars-至少有一个JAR被扫描用于TLD但尚未包含TLD。-为此记录器启用调试日志记录，以获取已扫描但未在其中找到TLD的完整JAR列表。-在扫描期间跳过不需要的JAR可以缩短启动时间和JSP编译时间。"><a href="#2-org-apache-jasper-servlet-TldScanner-scanJars-至少有一个JAR被扫描用于TLD但尚未包含TLD。-为此记录器启用调试日志记录，以获取已扫描但未在其中找到TLD的完整JAR列表。-在扫描期间跳过不需要的JAR可以缩短启动时间和JSP编译时间。" class="headerlink" title="2. org.apache.jasper.servlet.TldScanner.scanJars 至少有一个JAR被扫描用于TLD但尚未包含TLD。 为此记录器启用调试日志记录，以获取已扫描但未在其中找到TLD的完整JAR列表。 在扫描期间跳过不需要的JAR可以缩短启动时间和JSP编译时间。"></a>2. org.apache.jasper.servlet.TldScanner.scanJars 至少有一个JAR被扫描用于TLD但尚未包含TLD。 为此记录器启用调试日志记录，以获取已扫描但未在其中找到TLD的完整JAR列表。 在扫描期间跳过不需要的JAR可以缩短启动时间和JSP编译时间。</h6><p>解决方法：修改 Tomcat 安装目录 conf 目录下的 catalina.properties 文件。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%87%B3%E5%B0%91%E6%9C%89%E4%B8%80%E4%B8%AAJAR%E8%A2%AB%E6%89%AB%E6%8F%8F%E7%94%A8%E4%BA%8ETLD%E4%BD%86%E5%B0%9A%E6%9C%AA%E5%8C%85%E5%90%ABTLD.4bw634wkdg00.webp" alt="至少有一个JAR被扫描用于TLD但尚未包含TLD"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%87%B3%E5%B0%91%E6%9C%89%E4%B8%80%E4%B8%AAJAR%E8%A2%AB%E6%89%AB%E6%8F%8F%E7%94%A8%E4%BA%8ETLD%E4%BD%86%E5%B0%9A%E6%9C%AA%E5%8C%85%E5%90%ABTLD2.erx2r26mp0w.webp" alt="至少有一个JAR被扫描用于TLD但尚未包含TLD2"></p>
<h6 id="3-无法在web-xml或使用此应用程序部署的jar文件中解析绝对uri-http-java-sun-com-jsp-jstl-core"><a href="#3-无法在web-xml或使用此应用程序部署的jar文件中解析绝对uri-http-java-sun-com-jsp-jstl-core" class="headerlink" title="3.无法在web.xml或使用此应用程序部署的jar文件中解析绝对uri:[http://java.sun.com/jsp/jstl/core]"></a>3.无法在web.xml或使用此应用程序部署的jar文件中解析绝对uri:[<a class="link"   href="http://java.sun.com/jsp/jstl/core]" >http://java.sun.com/jsp/jstl/core]<i class="fas fa-external-link-alt"></i></a></h6><p>无法在web.xml或使用此应用程序部署的jar文件中解析绝对uri:[<a class="link"   href="http://www.springframework.org/tags/form]" >http://www.springframework.org/tags/form]<i class="fas fa-external-link-alt"></i></a></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/jsp%E6%97%A0%E6%B3%95%E8%A7%A3%E6%9E%90.yv4xf8rs08w.webp" alt="jsp无法解析"></p>
<p>可以看到在 jsp 页面中引用到了这两个网址：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/jsp%E4%B8%AD%E6%97%A0%E6%B3%95%E8%A7%A3%E6%9E%90%E7%9A%84%E7%BD%91%E5%9D%80.3dqy00n5ouk0.webp" alt="jsp中无法解析的网址"></p>
<p>一个可能的原因是 Maven 中的依赖包与 Tomcat 中的 Jar 包存在冲突，比如版本不同，这是 Jar 包层面的不同；</p>
<p>另一种可能的原因是 Tomcat 版本与 jstl jar 包版本不兼容，比如 Tomcat 版本过高而 jstl jar 包版本过低；</p>
<p>还有一种可能的原因是 Tomcat 中缺少必要的 jstl jar包（这是最常见的原因）。</p>
<p>解决方法：</p>
<p>无法解析<a class="link"   href="http://java.sun.com/jsp/jstl/core%EF%BC%9A%E5%B0%86" >http://java.sun.com/jsp/jstl/core：将<i class="fas fa-external-link-alt"></i></a> jstl jar 包添加到 Tomcat 的 lib 包中（实测也可以不加），然后在部署 Tomcat 时加上 jstl jar 包中的 c.tld、c-1_0.tld、c-1_0-rt.tld 三个文件。</p>
<p>无法解析<a class="link"   href="http://www.springframework.org/tags/form%EF%BC%9A%E5%9C%A8%E9%83%A8%E7%BD%B2" >http://www.springframework.org/tags/form：在部署<i class="fas fa-external-link-alt"></i></a> Tomcat 时加上spring-webmvc-4.2.2.RELEASE jar 包中的 spring.tld、spring-form.tld 两个文件。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E5%8A%A0%E5%85%A5jstl%E5%8C%85%E4%B8%AD%E7%BC%BA%E5%B0%91%E6%96%87%E4%BB%B6.6aowr5a3tnk0.webp" alt="加入jstl包中缺少文件"></p>
<h6 id="4-java-lang-NoSuchMethodError-org-apache-commons-io-FileUtils-moveFile-Ljava-x2F-io-x2F-File-Ljava-x2F-io-x2F-File-V"><a href="#4-java-lang-NoSuchMethodError-org-apache-commons-io-FileUtils-moveFile-Ljava-x2F-io-x2F-File-Ljava-x2F-io-x2F-File-V" class="headerlink" title="4. java.lang.NoSuchMethodError: org.apache.commons.io.FileUtils.moveFile(Ljava&#x2F;io&#x2F;File;Ljava&#x2F;io&#x2F;File;)V"></a>4. java.lang.NoSuchMethodError: org.apache.commons.io.FileUtils.moveFile(Ljava&#x2F;io&#x2F;File;Ljava&#x2F;io&#x2F;File;)V</h6><p>错误信息：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">11</span>-Apr-<span class="number">2022</span> <span class="number">12</span>:<span class="number">48</span>:<span class="number">32.134</span> 信息 [RMI TCP <span class="title function_">Connection</span><span class="params">(<span class="number">3</span>)</span>-<span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span>] org.apache.catalina.core.ApplicationContext.log No Spring WebApplicationInitializer types detected on classpath</span><br><span class="line"><span class="number">11</span>-Apr-<span class="number">2022</span> <span class="number">12</span>:<span class="number">48</span>:<span class="number">32.163</span> 信息 [RMI TCP <span class="title function_">Connection</span><span class="params">(<span class="number">3</span>)</span>-<span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span>] org.apache.catalina.core.ApplicationContext.log Initializing Spring root WebApplicationContext</span><br><span class="line"><span class="number">11</span>-Apr-<span class="number">2022</span> <span class="number">12</span>:<span class="number">48</span>:<span class="number">33.375</span> 信息 [RMI TCP <span class="title function_">Connection</span><span class="params">(<span class="number">3</span>)</span>-<span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span>] org.apache.catalina.core.ApplicationContext.log Initializing Spring FrameworkServlet <span class="string">&#x27;smart&#x27;</span></span><br><span class="line"><span class="number">11</span>-Apr-<span class="number">2022</span> <span class="number">12</span>:<span class="number">48</span>:<span class="number">45.403</span> 严重 [http-nio-<span class="number">8080</span>-exec-<span class="number">5</span>] org.apache.catalina.core.StandardWrapperValve.invoke 在路径为/springmvc6的上下文中，Servlet[smart]的Servlet.service（）引发了具有根本原因的异常Handler processing failed; nested exception is java.lang.NoSuchMethodError: org.apache.commons.io.FileUtils.moveFile(Ljava/io/File;Ljava/io/File;)V</span><br><span class="line">	java.lang.NoSuchMethodError: org.apache.commons.io.FileUtils.moveFile(Ljava/io/File;Ljava/io/File;)V</span><br><span class="line">		at org.apache.commons.fileupload.disk.DiskFileItem.write(DiskFileItem.java:<span class="number">405</span>)</span><br><span class="line">		at org.springframework.web.multipart.commons.CommonsMultipartFile.transferTo(CommonsMultipartFile.java:<span class="number">142</span>)</span><br><span class="line">		at com.smart.controller.UserController.updateThumb(UserController.java:<span class="number">24</span>)</span><br><span class="line">		at sun.reflect.NativeMethodAccessorImpl.invoke0(Native Method)</span><br><span class="line">		at sun.reflect.NativeMethodAccessorImpl.invoke(NativeMethodAccessorImpl.java:<span class="number">62</span>)</span><br><span class="line">		at sun.reflect.DelegatingMethodAccessorImpl.invoke(DelegatingMethodAccessorImpl.java:<span class="number">43</span>)</span><br><span class="line">		at java.lang.reflect.Method.invoke(Method.java:<span class="number">498</span>)</span><br><span class="line">		at org.springframework.web.method.support.InvocableHandlerMethod.doInvoke(InvocableHandlerMethod.java:<span class="number">221</span>)</span><br><span class="line">		at org.springframework.web.method.support.InvocableHandlerMethod.invokeForRequest(InvocableHandlerMethod.java:<span class="number">137</span>)</span><br><span class="line">		at org.springframework.web.servlet.mvc.method.annotation.ServletInvocableHandlerMethod.invokeAndHandle(ServletInvocableHandlerMethod.java:<span class="number">110</span>)</span><br><span class="line">		at org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter.invokeHandlerMethod(RequestMappingHandlerAdapter.java:<span class="number">806</span>)</span><br><span class="line">		at org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter.handleInternal(RequestMappingHandlerAdapter.java:<span class="number">729</span>)</span><br><span class="line">		at org.springframework.web.servlet.mvc.method.AbstractHandlerMethodAdapter.handle(AbstractHandlerMethodAdapter.java:<span class="number">85</span>)</span><br><span class="line">		at org.springframework.web.servlet.DispatcherServlet.doDispatch(DispatcherServlet.java:<span class="number">959</span>)</span><br><span class="line">		at org.springframework.web.servlet.DispatcherServlet.doService(DispatcherServlet.java:<span class="number">893</span>)</span><br><span class="line">		at org.springframework.web.servlet.FrameworkServlet.processRequest(FrameworkServlet.java:<span class="number">970</span>)</span><br><span class="line">		at org.springframework.web.servlet.FrameworkServlet.doPost(FrameworkServlet.java:<span class="number">872</span>)</span><br><span class="line">		at javax.servlet.http.HttpServlet.service(HttpServlet.java:<span class="number">681</span>)</span><br><span class="line">		at org.springframework.web.servlet.FrameworkServlet.service(FrameworkServlet.java:<span class="number">846</span>)</span><br><span class="line">		at javax.servlet.http.HttpServlet.service(HttpServlet.java:<span class="number">764</span>)</span><br><span class="line">		at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:<span class="number">227</span>)</span><br><span class="line">		at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:<span class="number">162</span>)</span><br><span class="line">		at org.apache.tomcat.websocket.server.WsFilter.doFilter(WsFilter.java:<span class="number">53</span>)</span><br><span class="line">		at org.apache.catalina.core.ApplicationFilterChain.internalDoFilter(ApplicationFilterChain.java:<span class="number">189</span>)</span><br><span class="line">		at org.apache.catalina.core.ApplicationFilterChain.doFilter(ApplicationFilterChain.java:<span class="number">162</span>)</span><br><span class="line">		at org.apache.catalina.core.StandardWrapperValve.invoke(StandardWrapperValve.java:<span class="number">197</span>)</span><br><span class="line">		at org.apache.catalina.core.StandardContextValve.invoke(StandardContextValve.java:<span class="number">97</span>)</span><br><span class="line">		at org.apache.catalina.authenticator.AuthenticatorBase.invoke(AuthenticatorBase.java:<span class="number">541</span>)</span><br><span class="line">		at org.apache.catalina.core.StandardHostValve.invoke(StandardHostValve.java:<span class="number">135</span>)</span><br><span class="line">		at org.apache.catalina.valves.ErrorReportValve.invoke(ErrorReportValve.java:<span class="number">92</span>)</span><br><span class="line">		at org.apache.catalina.valves.AbstractAccessLogValve.invoke(AbstractAccessLogValve.java:<span class="number">687</span>)</span><br><span class="line">		at org.apache.catalina.core.StandardEngineValve.invoke(StandardEngineValve.java:<span class="number">78</span>)</span><br><span class="line">		at org.apache.catalina.connector.CoyoteAdapter.service(CoyoteAdapter.java:<span class="number">360</span>)</span><br><span class="line">		at org.apache.coyote.http11.Http11Processor.service(Http11Processor.java:<span class="number">399</span>)</span><br><span class="line">		at org.apache.coyote.AbstractProcessorLight.process(AbstractProcessorLight.java:<span class="number">65</span>)</span><br><span class="line">		at org.apache.coyote.AbstractProtocol$ConnectionHandler.process(AbstractProtocol.java:<span class="number">890</span>)</span><br><span class="line">		at org.apache.tomcat.util.net.NioEndpoint$SocketProcessor.doRun(NioEndpoint.java:<span class="number">1743</span>)</span><br><span class="line">		at org.apache.tomcat.util.net.SocketProcessorBase.run(SocketProcessorBase.java:<span class="number">49</span>)</span><br><span class="line">		at org.apache.tomcat.util.threads.ThreadPoolExecutor.runWorker(ThreadPoolExecutor.java:<span class="number">1191</span>)</span><br><span class="line">		at org.apache.tomcat.util.threads.ThreadPoolExecutor$Worker.run(ThreadPoolExecutor.java:<span class="number">659</span>)</span><br><span class="line">		at org.apache.tomcat.util.threads.TaskThread$WrappingRunnable.run(TaskThread.java:<span class="number">61</span>)</span><br><span class="line">		at java.lang.Thread.run(Thread.java:<span class="number">748</span>)</span><br></pre></td></tr></table></figure>

<p>由错误信息可知是找不到这个方法：org.apache.commons.io.FileUtils.moveFile</p>
<p>原始依赖是：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.commons<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>commons-io<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.3.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在这个 jar 包中确实找不到这个方法，因此更换依赖如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>commons-io<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>commons-io<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.11.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>更换 jar 包后，找到该方法，问题解决：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/FileUtils.moveFile%E6%96%B9%E6%B3%95.4mkv8vb2lqc0.webp" alt="FileUtils"></p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
  </entry>
  <entry>
    <title>数据结构与算法分析(十)-算法设计技巧</title>
    <url>/2022/03/08/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95%E5%88%86%E6%9E%90-%E5%8D%81-%E7%AE%97%E6%B3%95%E8%AE%BE%E8%AE%A1%E6%8A%80%E5%B7%A7/</url>
    <content><![CDATA[<h1 id="（待补充）"><a href="#（待补充）" class="headerlink" title="（待补充）"></a>（待补充）</h1><p>当一个算法给定时，具体的数据结构无需指定。为使运行时间尽可能地少，需要由编程人员来选择适当的数据结构。</p>
<h1 id="10-1-贪婪算法"><a href="#10-1-贪婪算法" class="headerlink" title="10.1 贪婪算法"></a>10.1 贪婪算法</h1><p>贪婪算法分阶段地工作。在每一个阶段，可以认为所做决定是最好的，而不考虑将来的结果。通常，这意味着选择的是某个<em><strong>局部最优</strong></em>。这种“眼下能够拿到的就拿”的策略是这类算法名称的来源。当算法终止时，我们希望局部最优等于全局最优。如果是这样的话，那么算法就是正确的；否则，算法得到的是一个次最优解。如果不要求绝对最佳答案，那么有时使用简单的贪婪算法生成近似的答案，而不是使用通常产生准确答案所需要的负责算法。在图论中的 <em><strong>Dijkstra算法</strong></em> 、 <em><strong>Prim算法</strong></em> 以及 <em><strong>Kruskal算法</strong></em> 都用到了贪婪算法。</p>
<h2 id="10-1-1-调度问题"><a href="#10-1-1-调度问题" class="headerlink" title="10.1.1 调度问题"></a>10.1.1 调度问题</h2><h2 id="10-1-2哈夫曼编码"><a href="#10-1-2哈夫曼编码" class="headerlink" title="10.1.2哈夫曼编码"></a>10.1.2哈夫曼编码</h2><h2 id="10-1-3-近似装箱问题"><a href="#10-1-3-近似装箱问题" class="headerlink" title="10.1.3 近似装箱问题"></a>10.1.3 近似装箱问题</h2><h1 id="10-2-分治算法"><a href="#10-2-分治算法" class="headerlink" title="10.2 分治算法"></a>10.2 分治算法</h1><p>分治算法由两部分组成：</p>
<ul>
<li><em><strong>分</strong></em> ：递归解决较小的问题（基本情况除外）；</li>
<li><em><strong>治</strong></em> ：然后从子问题的解构建原问题的解。</li>
</ul>
<p>传统上，在正文中至少有两个递归调用的例程叫做分治算法，而正文中只含有一个递归调用的例程不是分治算法。一般坚持子问题是不相交的（即基本上不重叠）。<em><strong>最大子序列和问题</strong></em> 、<em><strong>一些线性时间的树遍历方法</strong></em> 、 <em><strong>归并排序</strong></em> 、<em><strong>快速排序</strong></em> 等均可以使用分治算法。</p>
<h2 id="10-2-1-分治算法的运行时间"><a href="#10-2-1-分治算法的运行时间" class="headerlink" title="10.2.1 分治算法的运行时间"></a>10.2.1 分治算法的运行时间</h2><h2 id="10-2-2-最近点问题"><a href="#10-2-2-最近点问题" class="headerlink" title="10.2.2 最近点问题"></a>10.2.2 最近点问题</h2><h2 id="10-2-3-选择问题"><a href="#10-2-3-选择问题" class="headerlink" title="10.2.3 选择问题"></a>10.2.3 选择问题</h2><h2 id="10-2-4-一些算术问题的理论改进"><a href="#10-2-4-一些算术问题的理论改进" class="headerlink" title="10.2.4 一些算术问题的理论改进"></a>10.2.4 一些算术问题的理论改进</h2><h1 id="10-3-动态规划"><a href="#10-3-动态规划" class="headerlink" title="10.3 动态规划"></a>10.3 动态规划</h1><p>任何数学递推公式都可以直接转换成递归算法，但是基本现实是编译器常常不能正确对待递归算法，结果导致低效的程序。当怀疑很可能是这种情况时，我们必须再给编译器提供一些帮助，将递归算法重新写成非递归算法，让后者把那些子问题的答案系统地记录在一个表内。利用这种方法的一种技巧叫做 <em><strong>动态规划</strong></em> 。</p>
<h2 id="10-3-1-用一个表代替递归"><a href="#10-3-1-用一个表代替递归" class="headerlink" title="10.3.1 用一个表代替递归"></a>10.3.1 用一个表代替递归</h2><h2 id="10-3-2-矩阵乘法的顺序安排"><a href="#10-3-2-矩阵乘法的顺序安排" class="headerlink" title="10.3.2 矩阵乘法的顺序安排"></a>10.3.2 矩阵乘法的顺序安排</h2><h2 id="10-3-3-最优二叉查找树"><a href="#10-3-3-最优二叉查找树" class="headerlink" title="10.3.3 最优二叉查找树"></a>10.3.3 最优二叉查找树</h2><h2 id="10-3-4-所有点对最短路径"><a href="#10-3-4-所有点对最短路径" class="headerlink" title="10.3.4 所有点对最短路径"></a>10.3.4 所有点对最短路径</h2><h1 id="10-4-随机化算法"><a href="#10-4-随机化算法" class="headerlink" title="10.4 随机化算法"></a>10.4 随机化算法</h1><h2 id="10-4-1-随机数发生器"><a href="#10-4-1-随机数发生器" class="headerlink" title="10.4.1 随机数发生器"></a>10.4.1 随机数发生器</h2><h2 id="10-4-2跳跃表"><a href="#10-4-2跳跃表" class="headerlink" title="10.4.2跳跃表"></a>10.4.2跳跃表</h2><h2 id="10-4-3素性测试"><a href="#10-4-3素性测试" class="headerlink" title="10.4.3素性测试"></a>10.4.3素性测试</h2><h1 id="10-5-回溯算法"><a href="#10-5-回溯算法" class="headerlink" title="10.5 回溯算法"></a>10.5 回溯算法</h1><p>在许多情况下， <em><strong>回溯算法</strong></em> 相当于穷举搜索的巧妙实现，虽然性能一般不理想。它表示这样一种算法，当算法进行到某一步时，该步之后的所有可能情况都不理想，都可以直接略过，那么这些可能情况将被抛弃，从而得不到执行。例如在一套新房子内摆放家具的问题，存在许多种摆法，但是有许多情况是不现实的。开始什么都不摆放，然后就是依次摆放每个家具，如果所有的家具都已经摆好并且户主很满意，那么算法终止。如果摆到某一步，该步之后所有家具摆放方法都不理想，那么我们就撤销这一步并且尝试其他的摆放方法，当然，可能导致进一步的撤销，如果连第一步都撤销了，那么就说明不存在令人满意的家具摆放方法。否则，我们总能找到一种满意的摆放方法。注意，虽然这个算法基本上是蛮力的，但是它并不尝试所有的可能。例如，某一步把沙发摆放到厨房，那么这一步以及之后的所有步骤都可以取消了，因为明显不合理。在一步内删除一大组可能性的做法叫做 <em><strong>裁剪</strong></em> 。</p>
<h2 id="10-5-1-收费公路重建问题"><a href="#10-5-1-收费公路重建问题" class="headerlink" title="10.5.1 收费公路重建问题"></a>10.5.1 收费公路重建问题</h2><h2 id="10-5-2-博弈"><a href="#10-5-2-博弈" class="headerlink" title="10.5.2 博弈"></a>10.5.2 博弈</h2>]]></content>
      <categories>
        <category>数据结构与算法分析</category>
      </categories>
  </entry>
  <entry>
    <title>精通SpringMVC(一)-SpringMVC体系结构概述</title>
    <url>/2022/04/06/%E7%B2%BE%E9%80%9ASpringMVC-%E4%B8%80-SpringMVC%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<p>​    本系列笔记所有代码地址：<a class="link"   href="https://github.com/lisj98/SpringMVCSource.git" >https://github.com/lisj98/SpringMVCSource.git<i class="fas fa-external-link-alt"></i></a></p>
<p>​    Spring MVC 通过一套 MVC 注解，让 POJO 成为处理请求的控制器，而不用像原生 Spring 那样需要事先实现接口。</p>
<p>​    Spring MVC 的核心是 <strong>DispatcherServlet</strong> ，DispatcherServlet 是Spring MVC的总导演、总策划，是Spring MVC的灵魂和心脏，它负责截获客户端发来的请求并协调好 Spring MVC 的各个组件来完成请求处理并返回响应的过程。</p>
<h4 id="1-1-体系结构"><a href="#1-1-体系结构" class="headerlink" title="1.1 体系结构"></a>1.1 体系结构</h4><p>​    Spring MVC，顾名思义，是 <strong>MVC(Model、View、Control)</strong> 的集中体现，它利用处理器来分离模型、视图和控制，达到不同层级间松散耦合的效果，是基于 <strong>Model 2</strong> 实现的技术框架，而 <strong>Model 1</strong> 就是把所有的展现逻辑和业务逻辑集中到一起，缺点自然就是紧耦合、复用性差、维护成本高。</p>
<p>Spring MVC 框架模型：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/Spring-MVC%E6%A1%86%E6%9E%B6%E6%A8%A1%E5%9E%8B.62clemp5dww0.webp" alt="Spring-MVC框架模型"></p>
<p>​    从 Spring MVC 框架模型中可以看出，DispatcherServlet 处于核心的地位，它负责协调和组织不同组件以完成请求处理并返回相应的工作。Spring MVC 通过一个前端 Servlet 接收所有的请求，并将具体工作委派给其他组件进行处理，DispatcherServlet 就是 Spring MVC 的前端 Servlet。</p>
<p><strong>Spring MVC处理请求的整体过程</strong> ：</p>
<ol>
<li>客户端发出一个 HTTP 请求，Web 应用服务器接收到这个请求，如果匹配到了 <strong>DispatcherServlet</strong> 指定的请求映射路径（在 web.xml 中指定），则将该请求转交给 DispatcherServlet 处理。</li>
<li>DispatcherServlet 接收到这个请求后，根据请求的信息（URL、HTTP 请求方法、请求报文头、请求参数、Cookie 等）及 <strong>HandlerMapping</strong> 的配置找到处理请求的特定处理器（Handler）。</li>
<li>当 DispatcherServlet 得到具体的处理器后，通过 <strong>HandlerAdapter</strong> 对 Handler 进行封装，再以统一的适配器接口调用 Handler。HandlerAdapter 是一个适配器，它用统一的接口对各种 Handler 方法进行调用。</li>
<li>当处理器（Handler）完成业务逻辑的处理后将返回一个 <strong>ModelAndView</strong> 给 DispatcherServlet ，ModelAndView 包含了<strong>逻辑视图名</strong>和模型数据信息。</li>
<li>ModelAndView 中包含的“逻辑视图名”并不是真正的视图对象，需要通过 <strong>ViewResolver</strong> 完成逻辑视图名到真实视图对象的解析工作。</li>
<li>当得到真实的视图对象 <strong>View</strong> 后，DispatcherServlet 就用这个 View 对象对 ModelAndView 中的模型数据进行视图渲染。</li>
<li>最终客户端得到的就是一个真实的视图，可以是一个 HTML 页面，也可以是一个 XML 串、一个 JSON 串、一张图片等。</li>
</ol>
<h4 id="1-2-配置-DispatcherServlet"><a href="#1-2-配置-DispatcherServlet" class="headerlink" title="1.2 配置 DispatcherServlet"></a>1.2 配置 DispatcherServlet</h4><p>​    要了解 Spring MVC 框架的工作原理，必须回答三个问题：</p>
<ol>
<li>DispatcherServlet 如何截获特定的 HTTP 请求并交由框架进行处理？（通过 &lt;servlet-mapping&gt;）</li>
<li>位于 Web 层的 Spring 容器（WebApplicationContext）如何与位于业务层的 Spring 容器（ApplicationContext）建立联系，以使 Web 层的 Bean 可以调用业务层的 Bean？（一个 web.xml 文件可以指定多个 DispatcherServlet，通过 &lt;servlet-mapping&gt; 配置，让每个 DispatcherServlet 处理不同的请求）</li>
<li>如何初始化 Spring MVC 的各个组件，并将它们装配到 DispatcherServlet 中？（initStrategies() 方法）</li>
</ol>
<h5 id="配置-DispatcherServlet-，截获特定的-HTTP-请求"><a href="#配置-DispatcherServlet-，截获特定的-HTTP-请求" class="headerlink" title="配置 DispatcherServlet ，截获特定的 HTTP 请求"></a>配置 DispatcherServlet ，截获特定的 HTTP 请求</h5><p>传统的 web.xml 的方式：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;!--添加web框架支持，src/main/webapp/WEB-INF/web.xml--&gt;</span><br><span class="line"></span><br><span class="line">&lt;!--业务层和持久层的配置文件，这些配置文件可以被父Spring容器所使用--&gt;</span><br><span class="line">&lt;context-param&gt;</span><br><span class="line">	&lt;!--&lt;context-param&gt;通常都是以一个整体的形式出现，如果参数名为contextConfigLocation，那么&lt;param-value&gt;中的文件就被指定为业务层或持久层的配置文件，可以有多个，用逗号分隔--&gt;</span><br><span class="line">	&lt;param-name&gt;contextConfigLocation&lt;/param-name&gt;</span><br><span class="line">	&lt;param-value&gt;classpath:/applicationContext.xml&lt;/param-value&gt;</span><br><span class="line">&lt;/context-param&gt;</span><br><span class="line">&lt;listener&gt;</span><br><span class="line">	&lt;!--ContextLoaderListener是一个ServletContextListener，它通过contextConfigLocation参数所指定的文件也就是applicationContext.xml文件启动“业务层”或者“持久层”的Spring容器--&gt;</span><br><span class="line">	&lt;listener-class&gt;org.springframework.web.context.ContextLoaderListener&lt;/listener-class&gt;</span><br><span class="line">&lt;/listener&gt;</span><br><span class="line"></span><br><span class="line">&lt;!--声明DispatcherServlet--&gt;</span><br><span class="line">&lt;servlet&gt;</span><br><span class="line">	&lt;!--DispatcherServlet默认自动加载/WEB-INF目录下的Spring配置文件，用来启动<span class="string">&quot;Web层&quot;</span>的Spring容器--&gt;</span><br><span class="line">	&lt;servlet-name&gt;smart&lt;/servlet-name&gt;</span><br><span class="line">	&lt;servlet-class&gt;org.springframework.web.servlet.DispatcherServlet&lt;/servlet-class&gt;</span><br><span class="line">	&lt;load-on-startup&gt;<span class="number">1</span>&lt;/load-on-startup&gt;</span><br><span class="line">&lt;/servlet&gt;</span><br><span class="line"></span><br><span class="line">&lt;!--名为DispatcherServlet匹配的URL模式--&gt;</span><br><span class="line">&lt;servlet-mapping&gt;</span><br><span class="line">	&lt;!--处理所有后缀为.html的HTTP请求--&gt;</span><br><span class="line">	&lt;servlet-name&gt;smart&lt;/servlet-name&gt;</span><br><span class="line">	&lt;url-pattern&gt;*.html&lt;/url-pattern&gt;</span><br><span class="line">&lt;/servlet-mapping&gt;</span><br></pre></td></tr></table></figure>

<p>​    一个 web.xml 可以配置多个 DispatcherServlet ，通过其 &lt;servlet-mapping&gt; 配置，让每个 DispatcherServlet 处理不同的请求。</p>
<p>​    DispatcherServlet 遵循 <strong>约定大于配置</strong> 的原则，在大多数情况下，用户无须进行额外的配置，只需要按照约定行事即可。</p>
<p>​    如果确实要对 DispatcherServlet 的默认规则进行调整的话，可以通过&lt;servlet&gt;的&lt;init-param&gt;指定：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;!--就是将上述代码里的&lt;context-param&gt;和&lt;servlet&gt;写在一起--&gt;</span><br><span class="line">&lt;servlet&gt;</span><br><span class="line">	&lt;servlet-name&gt;smart&lt;/servlet-name&gt;</span><br><span class="line">	&lt;servlet-class&gt;org.springframework.web.servlet.DispatcherServlet&lt;/servlet-class&gt;</span><br><span class="line">	&lt;init-param&gt;</span><br><span class="line">		&lt;param-name&gt;contextConfigLocation&lt;/param-name&gt;</span><br><span class="line">		&lt;param-value&gt;/WEB-INF/spring/webApplicationContext.xml&lt;/param-value&gt;</span><br><span class="line">	&lt;/init-param&gt;</span><br><span class="line">&lt;/servlet&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>namespace</strong>：DispatcherServlet 对应的命名空间，默认为&lt;servlet-name&gt;-servlet，用于构造 Spring 配置文件的路径。在显式指定路径后，配置文件对应的路径为 &#x2F;WEB-INF&#x2F;&lt;namespace&gt;.xml，而不是 &#x2F;WEB-INF&#x2F;&lt;servlet-name&gt;-servlet.xml。例如上述代码中的配置文件就为 smart-servlet.xml，而要是 namespace 指定为 smart，则配置文件就为 smart.xml。</li>
<li><strong>contextConfigLocation</strong>：用于指定业务层或持久层的配置文件，即 &lt;param-value&gt; 中的文件。</li>
<li><strong>publishContext</strong>：布尔类型，默认为 true。DispatcherServlet 根据该属性决定是否将 WebApplicationContext 发布到 ServletContext 的属性列表中，以便调用者可借由 ServletContext 找到 WebApplicationContext 实例，对应的属性名为 DispatcherServlet#getContextAttributeName() 方法的返回值。</li>
<li><strong>publishEvents</strong>：布尔类型。当 DispatcherServlet 处理完一个请求后，是否需要向容器发布一个 ServletRequestHandledEvents 事件，默认为true。</li>
</ul>
<p>编程的方式（Servlet 3.0的实现方式）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SmartApplicationInitializer</span> <span class="keyword">implements</span> <span class="title class_">WebApplicationInitializer</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onStartup</span><span class="params">(ServletContext container)</span> <span class="keyword">throws</span> ServletException &#123;</span><br><span class="line">        Registration.<span class="type">Dynamic</span> <span class="variable">registration</span> <span class="operator">=</span> container.addServlet(<span class="string">&quot;diapatcher&quot;</span>, <span class="keyword">new</span> <span class="title class_">DispatcherServlet</span>());</span><br><span class="line">        registration.setLoadOnStartup(<span class="number">1</span>);</span><br><span class="line">        registration.addMapping(<span class="string">&quot;*.html&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    也就是把 web.xml 中的实现方式用 Java 代码实现，仔细比较的话其实是一样的。</p>
<p>​    Servlet 3.0 的实现原理：在 Servlet 3.0 的环境中，容器会在类路径中查找实现 <code>javax.servlet.ServletContainerInitializer</code> 的类，如果发现已有实现类，就会调用它来配置 Servlet 容器。在 Spring 中，<code>org.springframework.web.SpringServletContainerInitializer</code> 类实现了该接口，同时这个类又会查找实现 <code>org.springframework.web.WebApplicationInitializer</code> 接口的类，并将配置任务交给这些实现类去完成。另外，Spring 提供了一个便利的抽象类 <strong>AbstractAnnotationConfigDispatcherServletInitializer</strong> 来实现这个接口，使得它在注册 DispatcherServlet 时只需简单地指定它的 Servlet 映射即可。在上述示例中，当应用部署到 Servlet 3.0 容器中时，容器启动时会自动发现它，并使用它来配置 Servlet 上下文。</p>
<h5 id="探究-DispatcherServlet-的内部逻辑"><a href="#探究-DispatcherServlet-的内部逻辑" class="headerlink" title="探究 DispatcherServlet 的内部逻辑"></a>探究 DispatcherServlet 的内部逻辑</h5><p>​    通过查看 DispatcherServlet 的 <strong>initStrategies()</strong> 方法的代码，就能知道 Spring 是如何将上下文中的 Spring MVC 组件装配到 DispatcherServlet 中的了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">initStrategies</span><span class="params">(ApplicationContext context)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.initMultipartResolver(context);	<span class="comment">//初始化文件上传解析器</span></span><br><span class="line">    <span class="built_in">this</span>.initLocaleResolver(context);	<span class="comment">//初始化本地解析器</span></span><br><span class="line">    <span class="built_in">this</span>.initThemeResolver(context);	<span class="comment">//初始化主体解析器</span></span><br><span class="line">    <span class="built_in">this</span>.initHandlerMappings(context);	<span class="comment">//初始化处理器映射器</span></span><br><span class="line">    <span class="built_in">this</span>.initHandlerAdapters(context);	<span class="comment">//初始化处理器适配器</span></span><br><span class="line">    <span class="built_in">this</span>.initHandlerExceptionResolvers(context);	<span class="comment">//初始化处理器异常解析器</span></span><br><span class="line">    <span class="built_in">this</span>.initRequestToViewNameTranslator(context);	<span class="comment">//初始化请求到视图名解析器</span></span><br><span class="line">    <span class="built_in">this</span>.initViewResolvers(context);	<span class="comment">//初始化视图解析器</span></span><br><span class="line">    <span class="built_in">this</span>.initFlashMapManager(context);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    initStrategies() 方法会在 WebApplicationContext 初始化后自动执行，此时 Spring 上下文中的 Bean 已经初始化完毕。该方法的工作原理是：通过反射机制查找并装配 Spring 容器中用户显式自定义的组件 Bean，如果找不到，则装配默认的组件实例。</p>
<p>​    Spring MVC 定义了一套默认的组件实现类，在 spring-webmvc-4.x.jar 包的 <code>org/springframework/web/servlet</code> 类路径下有一个 <strong>DispatcherServlet.properties</strong> 配置文件，该文件指定了 DispatcherServlet 所使用的默认组件：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/DispatcherServlet.properties.2yk2pov8hc80.webp" alt="DispatcherServlet"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Default implementation classes <span class="keyword">for</span> DispatcherServlet<span class="string">&#x27;s strategy interfaces.</span></span><br><span class="line"><span class="string"># Used as fallback when no matching beans are found in the DispatcherServlet context.</span></span><br><span class="line"><span class="string"># Not meant to be customized by application developers.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 本地化解析器</span></span><br><span class="line"><span class="string">org.springframework.web.servlet.LocaleResolver=org.springframework.web.servlet.i18n.AcceptHeaderLocaleResolver</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 主题解析器</span></span><br><span class="line"><span class="string">org.springframework.web.servlet.ThemeResolver=org.springframework.web.servlet.theme.FixedThemeResolver</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 处理器映射（共2个）</span></span><br><span class="line"><span class="string">org.springframework.web.servlet.HandlerMapping=org.springframework.web.servlet.handler.BeanNameUrlHandlerMapping,\</span></span><br><span class="line"><span class="string">	org.springframework.web.servlet.mvc.annotation.DefaultAnnotationHandlerMapping</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 处理器适配器（共3个）</span></span><br><span class="line"><span class="string">org.springframework.web.servlet.HandlerAdapter=org.springframework.web.servlet.mvc.HttpRequestHandlerAdapter,\</span></span><br><span class="line"><span class="string">	org.springframework.web.servlet.mvc.SimpleControllerHandlerAdapter,\</span></span><br><span class="line"><span class="string">	org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerAdapter</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 异常处理器（共3个）</span></span><br><span class="line"><span class="string">org.springframework.web.servlet.HandlerExceptionResolver=org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerExceptionResolver,\</span></span><br><span class="line"><span class="string">	org.springframework.web.servlet.mvc.annotation.ResponseStatusExceptionResolver,\</span></span><br><span class="line"><span class="string">	org.springframework.web.servlet.mvc.support.DefaultHandlerExceptionResolver</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 视图名称翻译器</span></span><br><span class="line"><span class="string">org.springframework.web.servlet.RequestToViewNameTranslator=org.springframework.web.servlet.view.DefaultRequestToViewNameTranslator</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string"># 视图解析器</span></span><br><span class="line"><span class="string">org.springframework.web.servlet.ViewResolver=org.springframework.web.servlet.view.InternalResourceViewResolver</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">org.springframework.web.servlet.FlashMapManager=org.springframework.web.servlet.support.SessionFlashMapManager</span></span><br></pre></td></tr></table></figure>

<p>​    如果希望使用非默认类型的组件，则只需在 Spring 配置文件中配置自定义的组件 Bean 即可。Spring MVC 一旦发现上下文中有用户自定义的组件，就不会使用默认的组件。</p>
<p>​    DispatcherServlet 装配各型组件的逻辑：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/DispatcherServlet-%E8%A3%85%E9%85%8D%E5%90%84%E5%9E%8B%E7%BB%84%E4%BB%B6%E7%9A%84%E9%80%BB%E8%BE%911.4bjkd50jmbc0.webp" alt="DispatcherServlet-装配各型组件的逻辑1"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/DispatcherServlet-%E8%A3%85%E9%85%8D%E5%90%84%E5%9E%8B%E7%BB%84%E4%BB%B6%E7%9A%84%E9%80%BB%E8%BE%912.6q7shtaqacc0.webp" alt="DispatcherServlet-装配各型组件的逻辑2"></p>
<p>​    有些组件最多允许存在一个实例，用☆表示；而另一些组件允许存在多个实例，用★表示。这些组件都实现了 <code>org.springframework.core.Ordered</code> 接口，同一类型的组件如果存在多个，可以通过 order 属性确定优先级顺序，值越小优先级越高。</p>
<p>​    简言之，当 DispatcherServlet 初始化后，就会自动扫描上下文的 Bean，根据名称或类型匹配的机制查找自定义的组件，找不到时则使用 DispatcherServlet.properties 定义的默认组件。</p>
<h4 id="1-3-一个简单的实例"><a href="#1-3-一个简单的实例" class="headerlink" title="1.3 一个简单的实例"></a>1.3 一个简单的实例</h4><p>​    Spring MVC 开发一般包括以下几个步骤：</p>
<ol>
<li>配置 web.xml，指定业务层对应的 Spring 配置文件，定义 DispatcherServlet。</li>
<li>编写处理请求的控制器（处理器）。</li>
<li>编写视图对象，即 jsp 页面、html 页面等。</li>
<li>配置 Spring MVC 的配置文件，使控制器、视图解析器等生效。</li>
</ol>
<h5 id="编写-web-xml"><a href="#编写-web-xml" class="headerlink" title="编写 web.xml"></a>编写 web.xml</h5><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"># src/main/webapp/WEB-INF/web.xml</span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">web-app</span> <span class="attr">version</span>=<span class="string">&quot;2.5&quot;</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://java.sun.com/xml/ns/javaee&quot;</span></span></span><br><span class="line"><span class="tag">	<span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">	<span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://java.sun.com/xml/ns/javaee </span></span></span><br><span class="line"><span class="string"><span class="tag">	http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd&quot;</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">context-param</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">param-value</span>&gt;</span>classpath:/applicationContext.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">context-param</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">listener</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">listener-class</span>&gt;</span>org.springframework.web.context.ContextLoaderListener<span class="tag">&lt;/<span class="name">listener-class</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">listener</span>&gt;</span></span><br><span class="line"></span><br><span class="line">	<span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>smart<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>org.springframework.web.servlet.DispatcherServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">load-on-startup</span>&gt;</span>1<span class="tag">&lt;/<span class="name">load-on-startup</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line"></span><br><span class="line">	<span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>smart<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>*.html<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h5 id="编写处理请求的控制器"><a href="#编写处理请求的控制器" class="headerlink" title="编写处理请求的控制器"></a>编写处理请求的控制器</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.smart.web;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Controller</span> <span class="comment">//@Controller注解可将一个POJO转化为处理HTTP请求的控制器</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span>    <span class="comment">//@RequestMapping为控制器指定处理哪些URL的请求，在类处标明则表示该类下的所有请求路径都包含在/user/下</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//通过请求 /user/register就可以调用到该方法，然后返回一个/user/路径下的register逻辑视图名，最后通过ViewResolver就可以将register逻辑视图名转化为register.jsp视图对象</span></span><br><span class="line">    <span class="meta">@RequestMapping(&quot;/register&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">register</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;user/register&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="编写视图对象"><a href="#编写视图对象" class="headerlink" title="编写视图对象"></a>编写视图对象</h5><p>​    编写 register.jsp，UserController#register() 方法处理完成后，将转向这个 register.jsp 页面。</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">&quot;java&quot;</span> contentType=<span class="string">&quot;text/html; charset=UTF-8&quot;</span> pageEncoding=<span class="string">&quot;UTF-8&quot;</span>%&gt;</span><br><span class="line">&lt;%<span class="meta">@taglib</span> uri=<span class="string">&quot;http://java.sun.com/jsp/jstl/core&quot;</span> prefix=<span class="string">&quot;c&quot;</span> %&gt;</span><br><span class="line">&lt;%@ taglib prefix=<span class="string">&quot;form&quot;</span> uri=<span class="string">&quot;http://www.springframework.org/tags/form&quot;</span> %&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;title&gt;新增用户&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  &lt;%--将表单提交到/user.html控制器中--%&gt;</span><br><span class="line">  &lt;form method=<span class="string">&quot;post&quot;</span> action=<span class="string">&quot;&lt;c:url value=&quot;</span>/user.html<span class="string">&quot;/&gt;&quot;</span>&gt;</span><br><span class="line">    &lt;table&gt;</span><br><span class="line">	    &lt;tr&gt;</span><br><span class="line">	       &lt;td&gt;用户名：&lt;/td&gt;</span><br><span class="line">	       &lt;td&gt;&lt;input type=<span class="string">&quot;text&quot;</span> name=<span class="string">&quot;userName&quot;</span>  value=<span class="string">&quot;$&#123;user.userName&#125;&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">	    &lt;/tr&gt;</span><br><span class="line">	    &lt;tr&gt;</span><br><span class="line">	     &lt;td&gt;密码：&lt;/td&gt;</span><br><span class="line">	       &lt;td&gt;&lt;input type=<span class="string">&quot;password&quot;</span> name=<span class="string">&quot;password&quot;</span> value=<span class="string">&quot;$&#123;user.password&#125;&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">	    &lt;/tr&gt;</span><br><span class="line">	    &lt;tr&gt;</span><br><span class="line">	     &lt;td&gt;姓名：&lt;/td&gt;</span><br><span class="line">	       &lt;td&gt;&lt;input type=<span class="string">&quot;text&quot;</span> name=<span class="string">&quot;realName&quot;</span> value=<span class="string">&quot;$&#123;user.realName&#125;&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">	    &lt;/tr&gt;</span><br><span class="line">	    &lt;tr&gt;</span><br><span class="line">	     &lt;td&gt;生日：&lt;/td&gt;</span><br><span class="line">	       &lt;td&gt;&lt;input type=<span class="string">&quot;text&quot;</span> name=<span class="string">&quot;realName&quot;</span> value=<span class="string">&quot;$&#123;user.birthday&#125;&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">	    &lt;/tr&gt;</span><br><span class="line">	    	    	    	    &lt;tr&gt;</span><br><span class="line">	     &lt;td&gt;工资：&lt;/td&gt;</span><br><span class="line">	       &lt;td&gt;&lt;input type=<span class="string">&quot;text&quot;</span> name=<span class="string">&quot;realName&quot;</span> value=<span class="string">&quot;$&#123;user.salary&#125;&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">	    &lt;/tr&gt;</span><br><span class="line">	    &lt;tr&gt;</span><br><span class="line">	     &lt;td colspan=<span class="string">&quot;2&quot;</span>&gt;&lt;input type=<span class="string">&quot;submit&quot;</span> name=<span class="string">&quot;提交&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">	    &lt;/tr&gt;	    </span><br><span class="line">    &lt;/table&gt;</span><br><span class="line">  &lt;/form&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>​    register.jsp 的作用是注册一个新的用户，因此 UserController 中还需添加一个 createUser() 方法用来处理&#x2F;user.html请求。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.smart.web;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.smart.UserService;</span><br><span class="line"><span class="keyword">import</span> com.smart.domain.User;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Controller;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RequestMethod;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.servlet.ModelAndView;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Controller</span> <span class="comment">//@Controller注解可将一个POJO转化为处理请求的控制器</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/user&quot;)</span>    <span class="comment">//@RequestMapping为控制器指定处理哪些URL的请求，在类处标明则表示该类下的所有请求路径都包含在/user/下</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//注入业务层的Bean</span></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//RequestMapping没有标注路径，说明它处理的是/user.html的请求，请求的方法必须为POST</span></span><br><span class="line">    <span class="comment">//将表单值映射到User对象中，调用UserService保存user，返回ModelAndView</span></span><br><span class="line">    <span class="meta">@RequestMapping(method = RequestMethod.POST)</span></span><br><span class="line">    <span class="keyword">public</span> ModelAndView <span class="title function_">createUser</span><span class="params">(User user)</span>&#123;</span><br><span class="line">        userService.createUser(user);</span><br><span class="line">        <span class="type">ModelAndView</span> <span class="variable">mav</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ModelAndView</span>();</span><br><span class="line">        mav.setViewName(<span class="string">&quot;user/createSuccess&quot;</span>);	<span class="comment">//逻辑视图名</span></span><br><span class="line">        mav.addObject(<span class="string">&quot;user&quot;</span>, user);			<span class="comment">//模型数据信息</span></span><br><span class="line">        <span class="keyword">return</span> mav;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping(&quot;/register&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">register</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;user/register&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    Spring MVC 自动将表单中的数据按参数名和 User 属性名相匹配的方式进行绑定，将参数值填充到 User 的相应属性中。User 中的参数名和表单组件是相同的，这样 Spring MVC 即可将表单组件值填充到 User 的相应属性中。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%A1%A8%E5%8D%95%E4%B8%AD%E7%9A%84%E6%95%B0%E6%8D%AE%E5%8F%82%E6%95%B0%E5%90%8D.27ybdjr6m9no.webp" alt="表单中的数据参数名"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/User%E5%B1%9E%E6%80%A7%E5%90%8D.4oxxvcbkdmy0.webp" alt="User属性名"></p>
<p>​    然后调用业务层的 UserService 进行业务处理，进而返回 ModelAndView 对象，逻辑视图名为 user&#x2F;createSuccess，user 作为模型数据暴露给视图对象。</p>
<p>​    视图解析器将 user&#x2F;createSuccess 解析为 &#x2F;web-INF&#x2F;views&#x2F;user&#x2F;createSuccess.jsp 的视图对象，createSuccess.jsp 页面代码如下所示：</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">&quot;java&quot;</span> contentType=<span class="string">&quot;text/html; charset=UTF-8&quot;</span> pageEncoding=<span class="string">&quot;UTF-8&quot;</span>%&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">&lt;title&gt;用户创建成功&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">      恭喜，用户$&#123;user.userName&#125;创建成功，用户ID为$&#123;user.userId&#125;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>​    由于 UserController#createUser() 方法中已将 user 作为模型数据信息填充到了模型中，因此 createSuccess.jsp 就可以通过 ${user.userName} 访问到模型中的数据了。</p>
<h5 id="配置-Spring-MVC-的配置文件"><a href="#配置-Spring-MVC-的配置文件" class="headerlink" title="配置 Spring MVC 的配置文件"></a>配置 Spring MVC 的配置文件</h5><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://www.springframework.org/schema/beans&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">&quot;http://www.springframework.org/schema/context&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:p</span>=<span class="string">&quot;http://www.springframework.org/schema/p&quot;</span> <span class="attr">xmlns:aop</span>=<span class="string">&quot;http://www.springframework.org/schema/aop&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:tx</span>=<span class="string">&quot;http://www.springframework.org/schema/tx&quot;</span> <span class="attr">xmlns:mvc</span>=<span class="string">&quot;http://www.springframework.org/schema/mvc&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:websocket</span>=<span class="string">&quot;http://www.springframework.org/schema/websocket&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd</span></span></span><br><span class="line"><span class="string"><span class="tag">    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/websocket http://www.springframework.org/schema/websocket/spring-websocket.xsd&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--&lt;context:component-scan&gt;使得Spring扫描指定包下的所有类，让标注Spring注解的类生效--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">&quot;com.smart.web&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--定义了一个视图解析器，指定了前缀路径和后缀路径--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.InternalResourceViewResolver&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:prefix</span>=<span class="string">&quot;/WEB-INF/views&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:suffix</span>=<span class="string">&quot;.jsp&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h5 id="运行用户注册模块"><a href="#运行用户注册模块" class="headerlink" title="运行用户注册模块"></a>运行用户注册模块</h5><p>​    将用户模块部署到 Web 服务器中，Web 服务器服务端口为8080，部署根路径为&#x2F;，通过 <a class="link"   href="http://localhost:8080/user/register.html" >http://localhost:8080/user/register.html<i class="fas fa-external-link-alt"></i></a> 即可访问用户注册页面，填写用户注册信息，将表单提交到 <a class="link"   href="http://localhost:8080/user.html" >http://localhost:8080/user.html<i class="fas fa-external-link-alt"></i></a> 地址中，UserController 控制器的 createUser() 方法响应这个请求，并导向 createSuccess.jsp 页面中。</p>
<p>​    对于用户注册表单提交直到返回响应这一过程，用实物对象对组件接口进行替换，可得到交互图如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/user.html%E6%8F%90%E4%BA%A4%E8%A1%A8%E5%8D%95%E7%9A%84%E6%95%B4%E4%BD%93%E8%BF%87%E7%A8%8B.3yg4bkoe1ms0.webp" alt="user"></p>
<p>​    简要描述一下 Spring MVC 处理 &#x2F;user.html 的整个过程：</p>
<ol>
<li>DispatcherServlet 接收到客户端的 &#x2F;user.html 请求。</li>
<li>DispatcherServlet 使用 DefaultAnnotationHandlerMapping 查找负责处理该请求的处理器。</li>
<li>DispatcherServlet 将请求分发给名为 &#x2F;user.html 的 UserController处理器。</li>
<li>处理器完成业务处理后，返回 ModelAndView 对象，其中 View 的逻辑视图名为 &#x2F;user&#x2F;createSuccess，而模型包含一个 user 的模型数据信息。</li>
<li>DispatcherServlet 调用 InternalResourceViewResolver 组件对 ModelAndView 中的逻辑视图名进行解析，得到真实的 &#x2F;WEB-INF&#x2F;view&#x2F;user&#x2F;createSuccess.jsp 视图对象。</li>
<li>DispatcherServlet 使用&#x2F;WEB-INF&#x2F;view&#x2F;user&#x2F;createSuccess.jsp 对模型中的 user 模型对象进行渲染。</li>
<li>返回相应页面给客户端。</li>
</ol>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
  </entry>
  <entry>
    <title>精通SpringMVC(三)-处理方法的数据绑定</title>
    <url>/2022/04/07/%E7%B2%BE%E9%80%9ASpringMVC-%E4%B8%89-%E5%A4%84%E7%90%86%E6%96%B9%E6%B3%95%E7%9A%84%E6%95%B0%E6%8D%AE%E7%BB%91%E5%AE%9A/</url>
    <content><![CDATA[<p>本系列笔记所有代码地址：<a class="link"   href="https://github.com/lisj98/SpringMVCSource.git" >https://github.com/lisj98/SpringMVCSource.git<i class="fas fa-external-link-alt"></i></a></p>
<p>经过之前两节练习之后，现在请求消息已经到达真正需要调用的方法处了，但是 Spring MVC 还有很多工作要做，包括数据转换、数据格式化及数据校验等。</p>
<h4 id="3-1-数据绑定流程剖析"><a href="#3-1-数据绑定流程剖析" class="headerlink" title="3.1 数据绑定流程剖析"></a>3.1 数据绑定流程剖析</h4><p>Spring MVC 通过反射机制对目标处理方法的签名进行分析，将请求消息绑定到处理方法的入参中。数据绑定的核心部件是 <strong>DataBinder</strong>。其运行机制为：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/DataBinder%E8%BF%90%E8%A1%8C%E6%9C%BA%E5%88%B6.2fnhpv9xi6ck.webp" alt="DataBinder运行机制"></p>
<p>Spring MVC 主框架将 ServletRequest 对象及处理方法的入参对象实例传递给 DataBinder，DataBinder 首先调用装配在 Spring Web 上下文（smart-servlet.xml）中的 <strong>ConversionService</strong> 组件进行数据类型转换、数据格式化等工作，然后将 ServletRequest 中的消息填充到入参对象中，接下来调用 <strong>Validator</strong> 组件对已经绑定了请求消息数据的入参对象进行数据合法性校验，最后生成数据绑定结果 <strong>BindingResult</strong> 对象。BindingResult 包含了已完成数据绑定的入参对象，还包含相应的校验错误对象。Spring MVC 抽取 BindingResult 中的入参对象及校验错误对象，将它们赋给处理方法的相应入参。最后的入参对象是根据 BindingResult 中的信息来赋值的，包含之前 ServletRequest 中的消息以及错误校验信息等。</p>
<h4 id="3-2-数据转换"><a href="#3-2-数据转换" class="headerlink" title="3.2 数据转换"></a>3.2 数据转换</h4><p>Java 标准的装配自定义属性编辑器需要用到 <strong>PropertyEditor</strong> ，它的核心功能是将一个字符串转换为一个 Java 对象，以便根据界面的输入或配置文件中的配置字符串构造出一个 JVM 内部的 Java 对象。</p>
<p>但是 PropertyEditor 存在以下不足：</p>
<ul>
<li>只能用于字符串到 Java 对象的转换，不适用于两个任意 Java 类型对象之间的转换。</li>
<li>对源对象及目标对象所在的上下文信息（如注解、所在宿主类（即相对于内部类的外部类）的结构等）不敏感，在类型转换时不能利用这些上下文信息实施高级的转换逻辑。</li>
</ul>
<p>鉴于此，Spring 在核心模型中添加了一个通用的类型转换模块，该模块位于 <code>org.springframework.core.convert</code> 包中。Spring 既支持该类型转换体系，同时也支持基于 PropertyEditor 的转换体系。</p>
<h5 id="1-ConversionService"><a href="#1-ConversionService" class="headerlink" title="1. ConversionService"></a>1. ConversionService</h5><p><strong>ConversionService</strong> 是 Spring 类型转换体系的核心接口，位于 <code>org.springframework.core.convert</code> 包中，该接口定义了 4 个方法：</p>
<ul>
<li>boolean canConvert(Class&lt;?&gt; sourceType, Class&lt;?&gt; targetType)：判断是否可以将一个 Java 类转换为另一个 Java 类。</li>
<li>boolean canConvert(TypeDescriptor sourceType, TypeDescriptor  targetType)：需转换的类将以成员变量的方式出现在宿主类中。TypeDescriptor 不但描述了需转换类的信息，还描述了从宿主类的上下文信息，如成员变量上的注解，成员变量是否以数组、集合或 Map 的方式呈现等。</li>
<li>&lt;T&gt; T convert(Object source, Class&lt;T&gt; targetType)：将原类型对象转换为目标类型对象。</li>
<li>Object convert(Object source, TypeDescriptor sourceType, TypeDescriptor  targetType)：将对象从原类型对象转换为目标类型对象，此时往往会用到所在宿主类的上下文信息。</li>
</ul>
<p>可以利用 <code>org.springframework.context.support.ConversionServiceFactoryBean</code> 在 Spring 的上下文中定义一个 ConversionService。Spring 将自动识别上下文中的 ConversionService，并在 Bean 属性配置和 Spring MVC 处理方法入参绑定等场合使用它进行数据转换。如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;conversionService&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.context.support.ConversionServiceFactoryBean&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure>

<p>该 FactoryBean 创建 ConversionService 的过程中内建了很多转换器，可以完成大多数 Java 类型的转换工作。</p>
<p><strong>ConversionServiceFactoryBean</strong> 的源码如下，其中包含了一个 <strong>converters</strong> 属性，用来注册自定义的类型转换器。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ConversionServiceFactoryBean</span> <span class="keyword">implements</span> <span class="title class_">FactoryBean</span>&lt;ConversionService&gt;, InitializingBean &#123;</span><br><span class="line">    <span class="keyword">private</span> Set&lt;?&gt; converters;</span><br><span class="line">    <span class="keyword">private</span> GenericConversionService conversionService;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">ConversionServiceFactoryBean</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setConverters</span><span class="params">(Set&lt;?&gt; converters)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.converters = converters;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterPropertiesSet</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.conversionService = <span class="built_in">this</span>.createConversionService();</span><br><span class="line">        ConversionServiceFactory.registerConverters(<span class="built_in">this</span>.converters, <span class="built_in">this</span>.conversionService);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> GenericConversionService <span class="title function_">createConversionService</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">DefaultConversionService</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> ConversionService <span class="title function_">getObject</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.conversionService;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Class&lt;? <span class="keyword">extends</span> <span class="title class_">ConversionService</span>&gt; getObjectType() &#123;</span><br><span class="line">        <span class="keyword">return</span> GenericConversionService.class;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">isSingleton</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过 ConversionServiceFactoryBean 的 converters 属性注册自定义的类型转换器，代码如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;conversionService&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.context.support.ConversionServiceFactoryBean&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;converters&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;com.smart.MyCustomConverter1&quot;</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;com.smart.MyCustomConverter2&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在 Spring 中，通过 <strong>CustomEditorConfigurer</strong> 注册的自定义属性编辑器必须实现 PropertyEditor 接口。那么注册到 ConversionServiceFactoryBean 中的自定义转换器必须实现哪些接口，并满足哪些约定呢？继续往下看。</p>
<h5 id="2-Spring-支持哪些转换器"><a href="#2-Spring-支持哪些转换器" class="headerlink" title="2. Spring 支持哪些转换器"></a>2. Spring 支持哪些转换器</h5><p>Spring 在 <code>org.springframework.core.convert.converter</code> 包中定义了 3 种类型的转换器接口，实现任意一个都可以作为自定义转换器注册到 ConversionServiceFactoryBean 中。这 3 中类型的转换器接口为：</p>
<ul>
<li>Converter&lt;S,T&gt;</li>
<li>GenericConverter</li>
<li>ConverterFactory</li>
</ul>
<p><strong>Converter</strong> 接口是 Spring 中最简单的一个转换器接口，其定义如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.core.convert.converter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Converter</span>&lt;S, T&gt; &#123;</span><br><span class="line">    T <span class="title function_">convert</span><span class="params">(S var1)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>T convert(S var1) 负责将 S 类型的对象转换为 T 类型的对象。如果希望将一种类型的对象转换为另一种类型及其子类的对象，例如将 String 转换为 Number 及 Number 的子类（Integer、Long、Double等）对象，就需要一系列的 Converter，如 StringToInteger、StringToLong 及 StringToDouble 等。</p>
<p>为了解决上述问题，Spring 提供了一个将相同系列多个“同质” Converter 封装在一起的 <strong>ConverterFactory</strong> 接口，其定义如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.core.convert.converter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">ConverterFactory</span>&lt;S, R&gt; &#123;</span><br><span class="line">    &lt;T <span class="keyword">extends</span> <span class="title class_">R</span>&gt; Converter&lt;S, T&gt; <span class="title function_">getConverter</span><span class="params">(Class&lt;T&gt; var1)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>S 为转换的原类型，R 为目标类型的基类，T 为继承于 R 的目标类型。如 Spring 的 StringToNumberConverterFactory 就实现了 ConverterFactory 接口，封装了 String 转换到各种数据类型的 Converter。</p>
<p>Converter 只负责将一个类型的对象转换为另一个类型的对象，并没有考虑类型对象所造宿主类上下文的信息，因此并不能完成“复杂”类型转换工作。<strong>GenericConverter</strong> 接口会根据源类对象及目标类对象所在宿主类的上下文信息进行类型转换工作，其定义如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.core.convert.converter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"><span class="keyword">import</span> org.springframework.core.convert.TypeDescriptor;</span><br><span class="line"><span class="keyword">import</span> org.springframework.util.Assert;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">GenericConverter</span> &#123;</span><br><span class="line">    Set&lt;GenericConverter.ConvertiblePair&gt; getConvertibleTypes();</span><br><span class="line"></span><br><span class="line">    Object <span class="title function_">convert</span><span class="params">(Object var1, TypeDescriptor var2, TypeDescriptor var3)</span>;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>ConvertiblePair</strong> 封装了源类型和目标类型，组成一对。而 <strong>TypeDescriptor</strong> 包含了需转换类型对象所在宿主类的信息，因此 GenericConverter 的 convert() 接口方法可以利用这些上下文信息进行类型转换工作。</p>
<p><strong>ConditionalGenericConverter</strong> 继承于 GenericConverter 接口，并添加了一个方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">boolean</span> <span class="title function_">matches</span><span class="params">(TypeDescriptor sourceType, TypeDescriptor targetType)</span></span><br></pre></td></tr></table></figure>

<p>该接口方法根据源类型和目标类型所在宿主类的上下文信息决定是否要进行类型转换，只有该方法返回 true 时，才调用 convert() 方法完成类型转换。正是因为在转换前有一个判断的动作，所以该接口命名为 ConditionalGenericConverter，即带”条件“的通用转换器。</p>
<p>ConversionServiceFactoryBean 的 converters 属性可接受 Converter、ConverterFactory、GenericConverter 、ConditionalGenericConverter 接口的实现类，并把这些转换器的转换逻辑统一封装到一个 ConversionService 实例对象中。Spring 在 Bean 属性配置及 Spring MVC 请求消息绑定时将利用这个 ConversionService 实例完成类型转换工作。过程图解如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/ConversionService%E5%88%9B%E5%BB%BA%E8%BF%87%E7%A8%8B%E7%A4%BA%E4%BE%8B%E5%9B%BE.2ptoh1f2iki0.webp" alt="ConversionService创建过程示例图"></p>
<h5 id="3-在Spring-MVC-中使用-ConversionService"><a href="#3-在Spring-MVC-中使用-ConversionService" class="headerlink" title="3. 在Spring MVC 中使用 ConversionService"></a>3. 在Spring MVC 中使用 ConversionService</h5><p>举一个例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.smart.domain;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.core.convert.converter.Converter;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 将 &lt;userName&gt;:&lt;password&gt;:&lt;realName&gt; 的格式化串转换为 User 对象</span></span><br><span class="line"><span class="comment"> * 1、实现 Converter 接口</span></span><br><span class="line"><span class="comment"> * 2、重写 convert() 方法，在方法里重写转换逻辑</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StringToUserConverter</span> <span class="keyword">implements</span> <span class="title class_">Converter</span>&lt;String, User&gt; &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">convert</span><span class="params">(String source)</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        <span class="keyword">if</span>(source != <span class="literal">null</span>)&#123;</span><br><span class="line">            String[] items = source.split(<span class="string">&quot;:&quot;</span>);</span><br><span class="line">            user.setUserName(items[<span class="number">0</span>]);</span><br><span class="line">            user.setPassword(items[<span class="number">1</span>]);</span><br><span class="line">            user.setRealName(items[<span class="number">2</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> user;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编写好 StringToUserConverter 类后，需要将它装配到 Spring 上下文中：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E5%B0%86StringToUserConverter%E8%A3%85%E9%85%8D%E5%88%B0Spring%E4%B8%8A%E4%B8%8B%E6%96%87%E4%B8%AD.51rxmetp4dc0.webp" alt="将StringToUserConverter装配到Spring上下文中"></p>
<p>&lt;mvc:annotation-driven&gt; 标签会自动创建一个默认的 DefaultHandlerMapping 和 RequestMappingHandlerAdapter 以及一个默认的 ConversionService，即 <strong>FormattingConversionServiceFactoryBean</strong> 以满足大多数类型转换的需求，因此如果需要用到自定义的 ConversionService 实现，就需要在 &lt;mvc:annotation-driven&gt; 标签中显式指定 conversion-service 属性的值来覆盖掉默认的 ConversionService 实现。</p>
<p>装配好 StringToUserConverter 后，就可以在任何控制器的任何方法中使用这个转换器了，例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@RequestMapping(&quot;/handle81&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">handle81</span><span class="params">(<span class="meta">@RequestParam(&quot;user&quot;)</span> User user, ModelMap modelMap)</span>&#123;</span><br><span class="line">        modelMap.put(<span class="string">&quot;user&quot;</span>, user);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;user/showUser&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动 Web 容器，发送一个 <code>http://localhost:8080/springmvc3/user/handle81.html?user=tom:1234:tomson</code> 请求，在控制器方法中打个断点，可以看到user请求参数的 tom:\1234:tomson 的值会被 StringToUserConverter 转换并绑定到 handle81() 的 User 入参中。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/StringToUserConverter%E8%BD%AC%E6%8D%A2%E6%95%B0%E6%8D%AE%E6%88%90%E5%8A%9F.36eddp43twe0.webp" alt="StringToUserConverter转换数据成功"></p>
<h5 id="4-使用-InitBinder-和-WebBindingInitializer-装配自定义编辑器"><a href="#4-使用-InitBinder-和-WebBindingInitializer-装配自定义编辑器" class="headerlink" title="4. 使用 @InitBinder 和 WebBindingInitializer 装配自定义编辑器"></a>4. 使用 @InitBinder 和 WebBindingInitializer 装配自定义编辑器</h5><p><strong>@InitBinder</strong> 可以用来添加自定义的编辑器，只对当前控制器有效。</p>
<p><strong>WebBindingInitializer</strong> 用于装配在全局范围内使用的编辑器，对所有控制器都有效。</p>
<p>@InitBinder 示例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@InitBinder</span> <span class="comment">//该注解在控制器初始化时调用</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initBinder</span><span class="params">(WebDataBinder binder)</span>&#123;</span><br><span class="line">    binder.registerCustomEditor(User.class, <span class="keyword">new</span> <span class="title class_">MyCustomConverter1</span>());	<span class="comment">//自定义编辑器</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%87%AA%E5%AE%9A%E4%B9%89%E7%BC%96%E8%BE%91%E5%99%A8%E5%AE%9E%E7%8E%B0PropertyEditor%E6%8E%A5%E5%8F%A3.1j1b4dkyxna8.webp" alt="自定义编辑器实现PropertyEditor接口"></p>
<p>Spring MVC 使用 <strong>WebDataBinder</strong> 处理请求消息和处理方法入参的绑定工作。<strong>registerCustomEditor()</strong> 方法为 User 注册一个自定义的编辑器，MyCustomConverter1 是 PropertyEditor 接口的实现类。</p>
<p>还可以使用 <strong>addCustomFormatter()</strong> 方法指定格式化程序实现，这样就不用实现 PropertyEditor 接口了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@InitBinder</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initBinder2</span><span class="params">(WebDataBinder binder)</span>&#123;</span><br><span class="line">    binder.addCustomFormatter(<span class="keyword">new</span> <span class="title class_">DateFormatter</span>(<span class="string">&quot;yyyy-MM-dd&quot;</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果希望在全局范围内使用 MyCustomConverter1 编辑器，则可实现 WebBindingInitializer 接口并在该实现类中注册 MyCustomConverter1。如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBindingInitializer</span> <span class="keyword">implements</span> <span class="title class_">WebBindingInitializer</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initBinder</span><span class="params">(WebDataBinder binder, WebRequest request)</span> &#123;</span><br><span class="line">        binder.registerCustomEditor(User.class, <span class="keyword">new</span> <span class="title class_">MyCustomConverter1</span>());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接下来在 Spring 上下文中装配 MyBindingInitializer，Spring 通过 RequestMappingHandlerAdapter 来装配 MyBindingInitializer：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;webBindingInitializer&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;com.smart.MyBindingInitializer&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>对于同一个类型对象来说，如果既在 ConversionService 中装配了自定义转换器，又通过 WebBindingInitializer 装配了自定义转换器，同时还在控制器中通过 @InitBinder 装配了自定义转换器，那么 Spring MVC 将按照如下顺序查找对应类型的编辑器：</p>
<ol>
<li>@InitBinder</li>
<li>ConversionService</li>
<li>WebBindingInitializer</li>
</ol>
<h4 id="3-3-数据格式化"><a href="#3-3-数据格式化" class="headerlink" title="3.3 数据格式化"></a>3.3 数据格式化</h4><p>Spring 使用转换器进行源类型对象到目标类型对象的转换，Spring 的转换器并不提供输入及输出信息格式化的工作。从客户端传来的数据一般都具有一定的格式，如何从这些格式化的数据中获取真正的数据以完成数据绑定，并将处理完的数据输出为格式化的数据，是 Spring 格式化框架要解决的重要问题。</p>
<p>Spring 引入了一个格式化框架，这个框架位于 <code>org.springframework.format</code> 包中，其中最重要的一个接口为 <strong>Formatter&lt;T&gt;</strong> 接口。</p>
<h5 id="1-Formatter-lt-T-gt-接口"><a href="#1-Formatter-lt-T-gt-接口" class="headerlink" title="1. Formatter&lt;T&gt;接口"></a>1. Formatter&lt;T&gt;接口</h5><p>其定义如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.format;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Formmater</span>&lt;T&gt; <span class="keyword">extends</span> <span class="title class_">Printer</span>&lt;T&gt;, Parser&lt;T&gt;&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Printer&lt;T&gt;</strong> 负责对象的格式化输出，Parser&lt;T&gt; 负责对象的格式化输入。</p>
<p>Printer&lt;T&gt; 的接口方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//用于将对象格式化为字符串然后输出</span></span><br><span class="line">String <span class="title function_">print</span><span class="params">(T fieldValue, Locale locale)</span>;</span><br></pre></td></tr></table></figure>

<p>Parser&lt;T&gt; 的接口方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//用于将格式化字符串转换为对象进而输入</span></span><br><span class="line">T <span class="title function_">parse</span><span class="params">(String clientValue, Locale locale)</span> <span class="keyword">throws</span> ParseException;</span><br></pre></td></tr></table></figure>

<p>Spring 的 <code>org.springframework.format.datetime</code> 包中提供了一个用于时间对象格式化的现类：</p>
<ul>
<li><strong>DateFormatter</strong></li>
</ul>
<p><code>org.springframework.format.number</code> 包中提供了 3 个用于数字对象格式化的实现类：</p>
<ul>
<li><strong>NumberFormatter</strong>：用于数字类型对象的格式化。</li>
<li><strong>CurrencyFormatter</strong>：用于货币类型对象的格式化。</li>
<li><strong>PercentFormatter</strong>：用于百分数数字类型对象的格式化。</li>
</ul>
<p>可以手工调用这些接口实现类进行对象数据输入&#x2F;输出的格式化工作。</p>
<p>同时，Spring 还提供了注解驱动的属性对象格式化功能，可在 Bean 属性设置、Spring MVC 处理方法入参数据绑定、模型数据输出时自动通过注解应用格式化功能。</p>
<h5 id="2-注解驱动格式化的重要接口"><a href="#2-注解驱动格式化的重要接口" class="headerlink" title="2. 注解驱动格式化的重要接口"></a>2. 注解驱动格式化的重要接口</h5><p>为了让注解和格式化的属性类型关联起来，Spring 在 Formatter&lt;T&gt; 所在的包中提供了一个 AnnotationFormatterFactory&lt;A extends Annotaion&gt; 接口，该接口有以下几个方法：</p>
<ul>
<li>Set&lt;Class&lt;?&gt;&gt; getFieldTypes()：注解 A 的应用范围，即哪些属性类可以标注 A 注解。</li>
<li>Parser&lt;?&gt; getParser(A annotation, Class&lt;?&gt; fieldType)：根据注解 A 获取特定属性类型的 Parser。</li>
<li>Printer&lt;?&gt; getPrinter(A annotation, Class&lt;?&gt; fieldType)：根据注解 A 获取特定属性类型的 Printer。</li>
</ul>
<p>Spring 提供了两个内建的实现类：</p>
<ul>
<li><strong>NumberFormatAnnotationFormatterFactory</strong>：支持数字类型的属性使用 <strong>@NumberFormat</strong> 注解。</li>
<li><strong>JodaDateTimeFormatAnnotationFormatterFactory</strong>：支持对日期类型的属性使用 <strong>@DateTimeFormat</strong> 注解。</li>
</ul>
<p>如何在 Spring 上下文中启用这个基于注解驱动的格式化模块，并在实际应用中使用格式化功能呢？</p>
<h5 id="3-启动注解驱动格式化功能"><a href="#3-启动注解驱动格式化功能" class="headerlink" title="3. 启动注解驱动格式化功能"></a>3. 启动注解驱动格式化功能</h5><p>Spring 在格式化模块中定义了一个实现 ConversionService 接口的 FormattingConversionService 实现类，该实现类扩展了 GenericConversionService ，因此它既具有类型转换功能，又具有格式化功能。</p>
<p>相对于 ConversionService 的 ConversionServiceFactoryBean 工厂类，FormattingConversionService 也有一个对应的 FormattingConversionServiceFactoryBean 工厂类，用于在 Spring 上下文中构造一个 FormattingConversionService，通过这个工厂类，既可以注册自定义的转换器，还可以注册自定义的注解驱动逻辑。</p>
<p>由于 FormattingConversionServiceFactoryBean 在内部自动注册了 NumberFormatAnnotationFormatterFactory 和 JodaDateTimeFormatAnnotationFormatterFactory，因此，在装配了 FormattingConversionServiceFactoryBean 后，就可以直接使用对应的两个注解完成格式化功能了。</p>
<p>在 Spring 上下文中装配 FormattingConversionServiceFactoryBean：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;conversionService&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.format.support.FormattingConversionServiceFactoryBean&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;converters&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;com.smart.domain.StringToUserConverter&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注意，&lt;mvc:annotation-driven&gt; 内部创建的默认 ConversionService 实例就是一个 FormattingConversionServiceFactoryBean。</p>
<h5 id="4-注解驱动格式化实例"><a href="#4-注解驱动格式化实例" class="headerlink" title="4. 注解驱动格式化实例"></a>4. 注解驱动格式化实例</h5><p>在 User 中添加两个新属性，并标注格式化注解：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String userId;</span><br><span class="line">    <span class="keyword">private</span> String userName;</span><br><span class="line">    <span class="keyword">private</span> String password;</span><br><span class="line">    <span class="keyword">private</span> String realName;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//可将形如2022-04-08的字符串转换到Date类型的birthday属性中</span></span><br><span class="line">    <span class="meta">@DateTimeFormat(pattern = &quot;yyyy-MM-dd&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> Date birthday;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//可将形如9999.99的字符串转换到long类型的salary属性中</span></span><br><span class="line">    <span class="meta">@NumberFormat(pattern = &quot;#,###.##&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> salary;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@DateTimeFormat 有几个互斥的属性（就是不能同时存在）：</p>
<ul>
<li><strong>iso</strong>，类型为 DateTimeFormat.ISO，有几个常用的可选值：<ul>
<li>DateTimeFormat.ISO.DATE：格式为 yyyy-MM-dd。</li>
<li>DateTimeFormat.ISO.DATE_TIME：格式为 yyyy-MM-dd hh:mm:ss.SSSZ。</li>
<li>DateTimeFormat.ISO.TIME：格式为 hh:mm:ss.SSSZ。</li>
<li>DateTimeFormat.ISO.NONE：表示不应该使用 ISO 格式的时间。</li>
</ul>
</li>
<li><strong>pattern</strong>：类型为 String，使用自定义的时间格式化串，如 yyyy&#x2F;mm&#x2F;dd hh:mm:ss。</li>
<li><strong>style</strong>：类型为 String，通过样式指定日期&#x2F;时间的格式，由两位字符组成。第一位表示日期的样式，第二位表示时间的样式，以下是几个常用的可选值：<ul>
<li>S：短日期&#x2F;时间的样式。</li>
<li>M：中日期&#x2F;时间的样式。</li>
<li>L：长日期&#x2F;时间的样式。</li>
<li>F：完整日期&#x2F;时间的样式。</li>
<li>-：忽略日期或时间的样式。</li>
</ul>
</li>
</ul>
<p>@NumberFormat有两个互斥的属性（就是不能同时存在）：</p>
<ul>
<li><strong>pattern</strong>：类型为 String，使用自定义的数字格式化串，如 “##,###.##”。</li>
<li><strong>style</strong>：类型为 NumberFormat.Style，以下是几个常用的可选值：<ul>
<li>NumberFormat.Style.CURRENCY：货币类型。</li>
<li>NumberFormat.Style.NUMBER：正常数字类型。</li>
<li>NumberFormat.Style.PERCENT：百分数类型。</li>
</ul>
</li>
</ul>
<p>举例说明：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testHandle82</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">RestTemplate</span> <span class="variable">restTemplate</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RestTemplate</span>();</span><br><span class="line">    LinkedMultiValueMap&lt;String, String&gt; form = <span class="keyword">new</span> <span class="title class_">LinkedMultiValueMap</span>&lt;&gt;();</span><br><span class="line">    form.add(<span class="string">&quot;userName&quot;</span>, <span class="string">&quot;tom&quot;</span>);</span><br><span class="line">    form.add(<span class="string">&quot;password&quot;</span>, <span class="string">&quot;123456&quot;</span>);</span><br><span class="line">    form.add(<span class="string">&quot;age&quot;</span>, <span class="string">&quot;23&quot;</span>);</span><br><span class="line">    form.add(<span class="string">&quot;birthday&quot;</span>, <span class="string">&quot;2022-04-08&quot;</span>);</span><br><span class="line">    form.add(<span class="string">&quot;salary&quot;</span>, <span class="string">&quot;10,000.00&quot;</span>);</span><br><span class="line">    <span class="type">String</span> <span class="variable">html</span> <span class="operator">=</span> restTemplate.postForObject(<span class="string">&quot;http://localhost:8080/springmvc3/user/handle82.html&quot;</span>, form, String.class);</span><br><span class="line">    Assert.assertNotNull(html);</span><br><span class="line">    Assert.assertTrue(html.indexOf(<span class="string">&quot;tom&quot;</span>)&gt;-<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果希望在视图页面中将模型属性数据也以格式化的方式渲染，则可通过 Spring 的页面标签（即&lt;%@taglib uri&#x3D;”<a class="link"   href="http://java.sun.com/jsp/jstl/core&quot;" >http://java.sun.com/jsp/jstl/core&quot;<i class="fas fa-external-link-alt"></i></a> prefix&#x3D;”c” %&gt; ，&lt;%@ taglib prefix&#x3D;”form” uri&#x3D;”<a class="link"   href="http://www.springframework.org/tags/form&quot;" >http://www.springframework.org/tags/form&quot;<i class="fas fa-external-link-alt"></i></a> %&gt; 等）显式模型数据来达到目的。</p>
<h4 id="3-4-数据校验"><a href="#3-4-数据校验" class="headerlink" title="3.4 数据校验"></a>3.4 数据校验</h4><p>应用程序在执行业务逻辑前，必须通过数据校验保证接收到的输入数据是正确合法的。</p>
<h5 id="1-JSR-303"><a href="#1-JSR-303" class="headerlink" title="1. JSR-303"></a>1. JSR-303</h5><p>JSR-303 是 Java 为 Bean 数据合法性校验所提供的标准框架。它定义了一套可标注在成员变量、属性方法上的校验注解：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/JSR-303%E6%B3%A8%E8%A7%A3.43m7t8vsixi0.webp" alt="JSR-303注解"></p>
<p>Hibernate Validator 是 JSR-303 的一个参考实现，它扩充了如下注解：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/Hibernate-Validator%E6%B3%A8%E8%A7%A3.6vl9u6r5twg0.webp" alt="Hibernate-Validator注解"></p>
<h5 id="2-Spring-校验框架"><a href="#2-Spring-校验框架" class="headerlink" title="2. Spring 校验框架"></a>2. Spring 校验框架</h5><p>Spring 拥有自己独立的校验框架，且支持 JSR-303 框架。Spring 的 DataBinder 在进行数据绑定时，可同时调用校验框架完成数据校验工作。在 Spring MVC 中，则可直接通过注解驱动的方式进行数据校验。</p>
<p>Spring 的 <code>org.springframework.validation</code> 是校验框架所在的包。包含几个重要接口：</p>
<ul>
<li><strong>Validator</strong>。拥有两个方法：<ul>
<li>boolean supports(Class&lt;?&gt; clazz)：能够对 clazz 类型的对象进行校验。</li>
<li>void validate(Object target, Errors errors)：对目标类 target 进行校验，并将校验错误记录在 errors 中。</li>
</ul>
</li>
</ul>
<p><strong>LocalValidatorFactoryBean</strong> 既实现了 Spring 的 Validator 接口，也实现了 JSR-303 的 Validator 接口，只要在 Spring 容器中定义一个 LocalValidatorFactoryBean，即可将其注入需要数据校验的 Bean 中。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;validator&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.validation.beanvalidation.LocalValidatorFactoryBean&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>需要注意的是 Spring 本身没有提供 JSR-303 的实现，因此需要添加 JSR-303 的实现者（如hibernate-validator）的相关依赖。 </p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.hibernate<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hibernate-validator<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>8.0.0.Alpha3<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h5 id="3-Spring-MVC-数据校验"><a href="#3-Spring-MVC-数据校验" class="headerlink" title="3. Spring MVC 数据校验"></a>3. Spring MVC 数据校验</h5><p>&lt;mvc:annotation-driven&gt; 默认会装配一个 LocalValidatorFactoryBean，通过在处理方法的入参上标注 <strong>@Valid</strong> 注解，即可让 Spring MVC 在完成数据绑定后执行数据校验工作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String userId;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//通过正则表达式进行校验，匹配4-30个包含数字、字母及下划线的字符</span></span><br><span class="line">    <span class="meta">@Pattern(regexp = &quot;w(4,30)&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String userName;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//通过正则表达式进行校验，匹配6-30个非空白的字符</span></span><br><span class="line">    <span class="meta">@Pattern(regexp = &quot;S(6,30)&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String password;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//这是hibernate-validator的扩展度，将属性值的长度限制在2-100之间</span></span><br><span class="line">    <span class="meta">@Length(min = 2,max = 100)</span></span><br><span class="line">    <span class="keyword">private</span> String realName;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//时间必须是一个过去的时间</span></span><br><span class="line">    <span class="meta">@Past</span></span><br><span class="line">    <span class="comment">//可将形如2022-04-08的字符串转换到Date类型的birthday属性中</span></span><br><span class="line">    <span class="meta">@DateTimeFormat(pattern = &quot;yyyy-MM-dd&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> Date birthday;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//数据必须在1000.00-100000.00之间</span></span><br><span class="line">    <span class="meta">@DecimalMin(value = &quot;1000.00&quot;)</span></span><br><span class="line">    <span class="meta">@DecimalMax(value = &quot;100000.00&quot;)</span></span><br><span class="line">    <span class="comment">//可将形如9999.99的字符串转换到long类型的salary属性中</span></span><br><span class="line">    <span class="meta">@NumberFormat(pattern = &quot;#,###.##&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> salary;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在实体类属性上标注注解后，接下来的问题是如何在 Spring MVC 中使用注解所声明的限制规则进行数据校验。</p>
<p>对处理方法的入参数据进行校验：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E5%AF%B9%E5%A4%84%E7%90%86%E6%96%B9%E6%B3%95%E7%9A%84%E5%85%A5%E5%8F%82%E6%95%B0%E6%8D%AE%E8%BF%9B%E8%A1%8C%E6%A0%A1%E9%AA%8C.5hljqhy28xs0.webp" alt="对处理方法的入参数据进行校验"></p>
<p>关键问题是，校验产生的校验结果保存在什么地方？又如何传递给请求处理方法？</p>
<p>Spring MVC 是通过对处理方法签名的规约来保存校验结果的：前一个表单&#x2F;命令对象的校验结果保存在其后的入参中，这个保存校验结果的入参必须为 <strong>BindingResult</strong> 或 <strong>Errors</strong> 类型。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E6%95%B0%E6%8D%AE%E6%A0%A1%E9%AA%8C%E6%97%B6%E5%A4%84%E7%90%86%E6%96%B9%E6%B3%95%E5%85%A5%E5%8F%82%E7%9A%84%E7%AD%BE%E5%90%8D%E8%A7%84%E7%BA%A6.7i6on0ubntg0.webp" alt="数据校验时处理方法入参的签名规约"></p>
<p>Errors 接口提供了获取错误信息的方法，而 BindingResult 接口扩展了 Errors 接口，既可以对数据进行校验，还可以获取数据绑定结果对象的信息。</p>
<h5 id="4-如何获取校验结果"><a href="#4-如何获取校验结果" class="headerlink" title="4. 如何获取校验结果"></a>4. 如何获取校验结果</h5><p>在表单&#x2F;命令对象类（User）的属性中标注校验注解，在处理方法对应的入参前添加 @Valid，Spring MVC 就会实施校验并将校验结果保存在被校验入参对象之后的 BindingResult 或 Errors 入参中。在处理方法内部可以通过 BindingResult 或 Errors 入参对象获取错误信息，还可以通过接口方法获取更多的信息，接口方法请看源码。</p>
<h5 id="5-如何在页面中显式错误"><a href="#5-如何在页面中显式错误" class="headerlink" title="5. 如何在页面中显式错误"></a>5. 如何在页面中显式错误</h5><p>由于表单&#x2F;命令对象所对应的请求一般是从客户端网页中传过来的，因此，如果发生了错误，则必须通过网页显示错误，提示用户更正错误。</p>
<p>Spring MVC 除了将表单&#x2F;命令对象的校验结果保存在相应的 BindingResult 或 Errors 中外，还将所有的校验结果保存到“隐含模型”（隐含模型的概念贱 2.5 节）中。也就是说，即使处理方法的签名中没有对应表单&#x2F;命令对象的校验结果入参，校验结果也不会丢失，它们始终可以从“隐含模型”对象中获取。校验结果对象和被校验的表单&#x2F;命令对象是一对一的关系，但“隐含模型”除了存储模型数据外，还保存了所有被校验的表单&#x2F;命令对象的校验结果。隐含模型中的所有数据最终将通过 HttpServletRequest 的属性列表暴露给 JSP 视图对象，因此我们可以在 JSP 视图对象中获取校验错误信息。</p>
<p>抛开 Spring MVC 的所有组件，我们单从输入&#x2F;输出的层面分析 Spring MVC 框架的数据流：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/Spring-MVC%E7%9A%84%E6%95%B0%E6%8D%AE%E6%B5%81%E5%9B%BE.5g7j0y5wg5g0.webp" alt="Spring-MVC的数据流图"></p>
<ol>
<li>HTTP 请求报文到达 Web 服务器，Web 服务器将其封装成一个 HttpServletRequest 对象。</li>
<li>Spring MVC 框架截获这个 HttpServletRequest 对象。</li>
<li>Spring MVC 创建一个隐含模型对象，作为处理本次请求的上下文数据存放处。</li>
<li>Spring MVC 将 HttpServletRequest  对象数据绑定到处理方法的入参对象中（即表单&#x2F;命令对象）。</li>
<li>将绑定错误信息、校验错误信息都保存到隐含模型中。</li>
<li>将本次请求的对应隐含模型数据存放到 HttpServletRequest 属性列表中，暴露给视图对象。</li>
<li>视图对象对已经存放在 HttpServletRequest 属性列表中的模型数据进行渲染。</li>
<li>将渲染后的 HTTP 响应报文发送给客户端。</li>
</ol>
<p>可通过 Spring 的 &lt;form:errors path&#x3D;”propName”&gt; 标签在 JSP 页面中显示错误信息：</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">&quot;java&quot;</span> contentType=<span class="string">&quot;text/html; charset=UTF-8&quot;</span> pageEncoding=<span class="string">&quot;UTF-8&quot;</span> %&gt;</span><br><span class="line">&lt;%<span class="meta">@taglib</span> uri=<span class="string">&quot;http://java.sun.com/jsp/jstl/core&quot;</span> prefix=<span class="string">&quot;c&quot;</span> %&gt;</span><br><span class="line">&lt;%@ taglib prefix=<span class="string">&quot;form&quot;</span> uri=<span class="string">&quot;http://www.springframework.org/tags/form&quot;</span> %&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;title&gt;新增用户&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;form:form modelAttribute=<span class="string">&quot;user&quot;</span> action=<span class="string">&quot;/springmvc3/user/handle91.html&quot;</span>&gt;</span><br><span class="line">    &lt;form:errors path=<span class="string">&quot;*&quot;</span>/&gt;</span><br><span class="line">    &lt;table&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td&gt;ID：&lt;/td&gt;</span><br><span class="line">            &lt;td&gt;</span><br><span class="line">				&lt;form:errors path=<span class="string">&quot;userId&quot;</span> cssClass=<span class="string">&quot;errorClass&quot;</span>/&gt;</span><br><span class="line">				&lt;form:input path=<span class="string">&quot;userId&quot;</span>/&gt;</span><br><span class="line">			&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td&gt;用户名：&lt;/td&gt;</span><br><span class="line">            &lt;td&gt;</span><br><span class="line">				&lt;form:errors path=<span class="string">&quot;userName&quot;</span> cssClass=<span class="string">&quot;errorClass&quot;</span>/&gt;</span><br><span class="line">				&lt;form:input path=<span class="string">&quot;userName&quot;</span>/&gt;</span><br><span class="line">			&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td&gt;密码：&lt;/td&gt;</span><br><span class="line">            &lt;td&gt;&lt;form:errors path=<span class="string">&quot;password&quot;</span> cssClass=<span class="string">&quot;errorClass&quot;</span>/&gt;</span><br><span class="line">				&lt;form:input path=<span class="string">&quot;password&quot;</span>/&gt;</span><br><span class="line">			&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td&gt;姓名：&lt;/td&gt;</span><br><span class="line">            &lt;td&gt;</span><br><span class="line">				&lt;form:errors path=<span class="string">&quot;realName&quot;</span> cssClass=<span class="string">&quot;errorClass&quot;</span>/&gt;</span><br><span class="line">				&lt;form:input path=<span class="string">&quot;realName&quot;</span>/&gt;</span><br><span class="line">			&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td&gt;生日：&lt;/td&gt;</span><br><span class="line">            &lt;td&gt;&lt;input type=<span class="string">&quot;text&quot;</span> name=<span class="string">&quot;birthday&quot;</span> value=<span class="string">&quot;$&#123;user.birthday&#125;&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td&gt;薪水：&lt;/td&gt;</span><br><span class="line">            &lt;td&gt;</span><br><span class="line">				&lt;form:errors path=<span class="string">&quot;salary&quot;</span> cssClass=<span class="string">&quot;errorClass&quot;</span>/&gt;</span><br><span class="line">				&lt;form:input path=<span class="string">&quot;salary&quot;</span>/&gt;</span><br><span class="line">			&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td colspan=<span class="string">&quot;2&quot;</span>&gt;&lt;input type=<span class="string">&quot;submit&quot;</span> name=<span class="string">&quot;提交&quot;</span>/&gt;&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">    &lt;/table&gt;</span><br><span class="line">&lt;/form:form&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>启动 Web 服务，输入 <code>http://localhost:8080/user/handle91.html</code> 显示错误信息如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E9%94%99%E8%AF%AF%E4%BF%A1%E6%81%AF%E4%B8%8EJSP%E5%AF%B9%E5%BA%94%E5%85%B3%E7%B3%BB.2kik7ajpzwo0.webp" alt="错误信息与JSP对应关系"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E9%94%99%E8%AF%AF%E4%BF%A1%E6%81%AF%E4%B8%8E%E5%AE%9E%E4%BD%93%E7%B1%BB%E5%AF%B9%E5%BA%94%E5%85%B3%E7%B3%BB.4mnbv3m0l5e0.webp" alt="错误信息与实体类对应关系"></p>
<h5 id="6-通过国际化资源显示错误信息"><a href="#6-通过国际化资源显示错误信息" class="headerlink" title="6. 通过国际化资源显示错误信息"></a>6. 通过国际化资源显示错误信息</h5><p>每个属性在数据绑定和数据校验发生错误时，都会生成一个对应的 FieldError 对象。该对象实现了 <code>org.springframework.context.MessageSourceResolvable</code> 接口，<strong>MessageSourceResolvable</strong> 顾名思义是可用国际化资源进行解析的对象，它拥有 3 个接口方法：</p>
<ul>
<li>Object[] getArguments()：返回一组参数对象。</li>
<li>String[] getCodes()：返回一组消息代码，每个代码对应一个资源属性。可以使用 getArguments() 方法返回的参数对资源属性值进行参数替换。</li>
<li>String getDefaultMessage()：默认的消息。</li>
</ul>
<p>当一个属性校验失败后，校验框架会为该属性生成4个消息代码，这些消息代码以校验注解类名为前缀，结合类名、属性名及属性类型名生成多个对应的消息代码。</p>
<p>如 User 类的 password 属性标注了一个 @Pattern 注解，当该属性值不满足 @Pattern 所定义的限制规则时，就会产生以下 4 个错误代码：</p>
<ul>
<li>Pattern.user.password：根据类型、属性名产生的错误代码。</li>
<li>Pattern.password：根据属性名产生的错误代码。</li>
<li>Pattern.java.lang.String：根据属性类型产生的错误代码。</li>
<li>Pattern：根据验证注解名产生的错误代码。</li>
</ul>
<p>当使用 Spring MVC 标签显示错误信息时，Spring MVC 会查看 Web 上下文是否装配了对应的国际化消息，如果没有，则显示默认的错误信息；否则使用国际化消息对错误代码进行翻译。</p>
<p>如果在数据类型转换或数据格式转换时发生错误，或者该有的参数不存在，或调用处理方法时发生错误，则都会在隐含模型中创建错误信息，其错误代码前缀如下：</p>
<ul>
<li><strong>required</strong>：必要的参数不存在。</li>
<li><strong>typeMisMatch</strong>：数据绑定时类型不匹配。</li>
<li><strong>methodInvocation</strong>：Spring MVC 在调用处理方法时发生了错误。</li>
</ul>
<p>例如，如果将“aaa”的非数值参数传递给 User 对象的 salary 属性，就会发生数据转换错误，错误代码如下：</p>
<ul>
<li>typeMisMatch.user.salary</li>
<li>typeMisMatch.salary</li>
<li>typeMisMatch.long</li>
<li>typeMisMatch</li>
</ul>
<p>如果需要自定义错误代码，则可通过 DataBinder 的 <strong>setMessageCodesResolver()</strong> 方法注入一个自定义的 <strong>MessageCodesResolver</strong> 对象来调整，不过一般情况下使用默认的就足够了。</p>
<p>在知道错误对象的错误代码时对应国际化消息的键名后，接下来的工作就简单了：定义一个国际化资源，在国际化资源文件中为错误代码定义相应的本地化信息。</p>
<p>我们在 i18n&#x2F; 下添加基名为 messages 的国际化资源，一个是默认的 messages.properties，另一个是对应中国大陆的 messages_zh_CN.properties。定义 messages_zh_CN.properties 的内容：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">required.user.userName</span>=<span class="string">用户名不正确，必须是4-30个英数及_的字符</span></span><br><span class="line"><span class="attr">Pattern.user.password</span>=<span class="string">密码不正确，必须是6-30个字符，不允许空格</span></span><br><span class="line"><span class="attr">Length.user.realName</span>=<span class="string">姓名不合法，长度必须是2-100个字符</span></span><br><span class="line"><span class="attr">Past.user.birthday</span>=<span class="string">日期格式不正确，必须是一个过去的日期</span></span><br><span class="line"><span class="attr">DecimalMin.user.salary</span>=<span class="string">工资必须在1000-100000之间</span></span><br><span class="line"><span class="attr">DecimalMax.user.salary</span>=<span class="string">$&#123;DecimalMin.user.salary&#125;</span></span><br></pre></td></tr></table></figure>

<p>然后在 smart-servlet.xml 中装配好这个国际化资源：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;messageSource&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.context.support.ResourceBundleMessageSource&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:basename</span>=<span class="string">&quot;i18n/messages&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以通过 <strong>ResourceBundleMessageSource</strong> 的 <strong>basename</strong> 属性指定一个国际化资源，还可以通过 <strong>basenames</strong> 属性指定多个国际化资源。重新启动容器，当发生错误时，错误页面如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%B8%AD%E6%96%87%E5%9B%BD%E9%99%85%E5%8C%96%E4%BF%A1%E6%81%AF.59126en8uo80.webp" alt="中文国际化信息"></p>
<h5 id="7-自定义校验规则"><a href="#7-自定义校验规则" class="headerlink" title="7. 自定义校验规则"></a>7. 自定义校验规则</h5><p>如何给请求处理类装配一个自定义的 Validator，或者直接在处理方法中使用自定义的 Validator 对入参进行校验？</p>
<p>可以在 UserController 中标注 @InitBinder 注解的 <strong>initBinder()</strong> 方法中装配自定义的 MyValidator。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@InitBinder</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">initBinder</span><span class="params">(WebDataBinder binder)</span>&#123;</span><br><span class="line">    <span class="comment">//指定进行数据绑定时使用的校验器</span></span><br><span class="line">    <span class="comment">//通过binder.setValidator()方法设置自定义的Validator后，Spring MVC将使用它对入参对象进行校验，而不再使用Spring MVC框架默认的Validator进行校验</span></span><br><span class="line">    binder.setValidator(<span class="keyword">new</span> <span class="title class_">UserValidator</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>还可以借助请求处理方法的签名传递一个 Errors 或 BindingResult 对象进来，然后在处理方法中直接校验。如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/handle92&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle92</span><span class="params">(<span class="meta">@ModelAttribute(&quot;user&quot;)</span> User user, BindingResult bindingResult)</span>&#123;</span><br><span class="line">    <span class="comment">//使用校验工具类进行校验</span></span><br><span class="line">    ValidationUtils.rejectIfEmptyOrWhitespace(bindingResult, <span class="string">&quot;userName&quot;</span>, <span class="string">&quot;required&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span>(<span class="string">&quot;aaaa&quot;</span>.equalsIgnoreCase(user.getUserName()))&#123;</span><br><span class="line">        <span class="comment">//手工生成一条错误信息</span></span><br><span class="line">        bindingResult.rejectValue(<span class="string">&quot;userName&quot;</span>, <span class="string">&quot;reserved&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(bindingResult.hasErrors())&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;user/register3&quot;</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;user/showUser&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以从错误代码组中任选一个错误代码，并在国际化资源文件中定义错误代码对应的信息内容，这样，当发生错误时，就会显示自定义的错误信息。</p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
  </entry>
  <entry>
    <title>精通SpringMVC(五)-本地化解析</title>
    <url>/2022/04/11/%E7%B2%BE%E9%80%9ASpringMVC-%E4%BA%94-%E6%9C%AC%E5%9C%B0%E5%8C%96%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<p>本系列笔记所有代码地址：<a class="link"   href="https://github.com/lisj98/SpringMVCSource.git" >https://github.com/lisj98/SpringMVCSource.git<i class="fas fa-external-link-alt"></i></a></p>
<h4 id="5-1-本地化的概念"><a href="#5-1-本地化的概念" class="headerlink" title="5.1 本地化的概念"></a>5.1 本地化的概念</h4><p>一般情况下，Web 应用根据客户端浏览器的设置判断客户端的本地化类型，例如：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%AE%BE%E7%BD%AE%E8%AF%AD%E8%A8%80%E9%A6%96%E9%80%89%E9%A1%B9.3p1p68u0qbc0.webp" alt="设置语言首选项"></p>
<p>浏览器中设置的<strong>本地化类型</strong>会包含在 HTML 请求报文头中发送给 Web 服务器，确切来说是通过<strong>报文头的 Accept-Language 参数</strong>将“语言首选项”对话框中选择的语言发送到服务器，成为服务器判别客户端本地化类型的依据。</p>
<p>如果 Web 应用基于这种方式提供本地化页面，则用户只得通过改变浏览器的设置进行本地化类型的切换。也有一种其他的形式，比如浏览器右上角有一个转换语言的图标，这种语言切换功能不要求用户更改浏览器的设置，<strong>它们通过 Cookie、Session 或请求参数即可切换本地化类型</strong>，这种方式明显更为便利。</p>
<p><strong>在默认情况下，Spring MVC 根据 Accept-Language 参数判断客户端的本地化类型。</strong>此外，它还提供了多种指定客户端本地化类型的方式，如<strong>通过设置 Cookie、Session 指定</strong>。</p>
<p><strong>事实上，当收到请求时，Spring MVC 在上下文中寻找一个本地化解析器（LocaleResolver），找到后使用它获取请求所对应的本地化类型信息。</strong></p>
<p><strong>除此之外，Spring MVC 还允许装配一个动态更改本地化类型的拦截器，这样通过指定一个请求参数就可以控制单个请求的本地化类型。</strong></p>
<p>本地化解析器和拦截器都定义在 <code>org.springframework.web.servlet.i18n</code> 包中，用户可以在 DispatcherServlet 上下文中配置它们。</p>
<p>Spring 提供了以下 4 个本地化解析器：</p>
<ul>
<li><strong>AcceptHeaderLocaleResolver</strong>：根据 HTTP 报文头的 Accept-Language 参数确定本地化类型。如果没有显式化定义本地化解析器，则 Spring MVC 默认采用该解析器。</li>
<li><strong>CookieLocaleResolver</strong>：根据指定的 Cookie 值确定本地化类型。</li>
<li><strong>SessionLocaleResolver</strong>：根据 Session 中特定的属性值确定本地化类型。</li>
<li><strong>LocaleChangeInterceptor</strong>：从请求参数中获取本次请求对应的本地化类型。</li>
</ul>
<p>在 3.4 节的实例中，我们为表单校验错误信息提供了国家化支持，当表单数据校验存在错误时，Spring MVC 使用默认的 AcceptHeaderLocaleResolver 抽取请求报文头的 Accept-Language 属性以确定客户端本地化类型，并返回对应的本地化错误信息。</p>
<h4 id="5-2-使用-CookieLocaleResolver"><a href="#5-2-使用-CookieLocaleResolver" class="headerlink" title="5.2 使用 CookieLocaleResolver"></a>5.2 使用 CookieLocaleResolver</h4><p>要使用 Cookie 保存本地化类型信息，只需在 Spring MVC 上下文中配置一个 CookieLocaleResolver 就可以了，DispatcherServlet 会自动识别本地化解析器并装配它。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/CookieLocaleResolver%E9%85%8D%E7%BD%AE.1k24u9zbr0ow.webp" alt="CookieLocaleResolver配置"></p>
<h4 id="5-3-使用-SessionLocaleResolver"><a href="#5-3-使用-SessionLocaleResolver" class="headerlink" title="5.3 使用 SessionLocaleResolver"></a>5.3 使用 SessionLocaleResolver</h4><p>SessionLocaleResolver 查找 Session 中属性名为 <strong>SessionLocaleResolver.LOCALE.SESSION_ATTRIBUTE_NAME</strong> 的属性，并将其转换为 Locale 对象，以此作为客户端的本地化类型。</p>
<p>配置过程如 CookieLocaleResolver 一样：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;localeResolver&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.i18n.SessionLocaleResolver&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>SessionLocaleResolver 和 CookieLocaleResolver 的区别是，前者一般要求用户登录后产生相应的用户会话才有效，而后者只要浏览器有 Cookie 存在即可生效。</p>
<h4 id="5-4-使用-LocaleChangeInterceptor"><a href="#5-4-使用-LocaleChangeInterceptor" class="headerlink" title="5.4 使用 LocaleChangeInterceptor"></a>5.4 使用 LocaleChangeInterceptor</h4><p>很多国际化网站都允许通过一个请求参数控制网站的本地化，如 <a class="link"   href="http://www.xxx.com/?locale=zh_CN" >www.xxx.com?locale=zh_CN<i class="fas fa-external-link-alt"></i></a> 返回对应中国大陆的本地化网页，而 <a class="link"   href="http://www.xxx.com/?locale=en_US" >www.xxx.com?locale=en_US<i class="fas fa-external-link-alt"></i></a> 返回本地化为英语的网页。这样，网页使用者可以通过对 URL 的控制返回不同本地化的页面。</p>
<p>在 Spring MVC 中，可以通过 LocaleChangeInterceptor 过滤器来完成这一需求。</p>
<p>在 Spring MVC 上下文中通过 <strong>&lt;mvc:interceptors&gt;</strong> 配置过滤器：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;localeResolver&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.i18n.CookieLocaleResolver&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:cookieName</span>=<span class="string">&quot;clientLanguage&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:cookieMaxAge</span>=<span class="string">&quot;100000&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:cookiePath</span>=<span class="string">&quot;/&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:defaultLocale</span>=<span class="string">&quot;zh_CN&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:interceptors</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.i18n.LocaleChangeInterceptor&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mvc:interceptors</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>LocaleChangeInterceptor 的角色类似于 ContentNegotiatingViewResolver，相当于一个中介。</strong></p>
<p>LocaleChangeInterceptor 在默认情况下通过 locale 请求参数获取本次请求对应的本地化类型，用户可以通过其 paramName 属性指定一个其他参数名。由于 LocaleChangeInterceptor 的主要任务是从请求中获取本地化类型并将其设置给真正的本地化解析器，所以在配置 LocaleChangeInterceptor 之前，必须在上下文中先配置一个本地化解析器，如 AcceptHeaderLocaleResolver、CookieLocaleResolver、SessionLocaleResolver 等。</p>
<p>值得注意的是，由于 AcceptHeaderLocaleResolver 是从请求报文头中获取本地化信息的，因此不能被动态更改，<strong>如果配置了 LocaleChangeInterceptor，则只能选择 CookieLocaleResolver 或 SessionLocaleResolver</strong>。</p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
  </entry>
  <entry>
    <title>精通SpringMVC(二)-注解驱动的控制器</title>
    <url>/2022/04/06/%E7%B2%BE%E9%80%9ASpringMVC-%E4%BA%8C-%E6%B3%A8%E8%A7%A3%E9%A9%B1%E5%8A%A8%E7%9A%84%E6%8E%A7%E5%88%B6%E5%99%A8/</url>
    <content><![CDATA[<p>本系列笔记所有代码地址：<a class="link"   href="https://github.com/lisj98/SpringMVCSource.git" >https://github.com/lisj98/SpringMVCSource.git<i class="fas fa-external-link-alt"></i></a></p>
<h4 id="2-1-使用-RequestMapping-映射请求"><a href="#2-1-使用-RequestMapping-映射请求" class="headerlink" title="2.1 使用 @RequestMapping 映射请求"></a>2.1 使用 @RequestMapping 映射请求</h4><p>在 POJO 类定义 <strong>@Controller</strong>，再通过 <code>&lt;context:component-scan&gt;</code> 扫描相应的类包，即可使 POJO 成为一个能处理 HTTP 请求的控制器。</p>
<p>可以创建数量不限的控制器，用来分别处理不同的请求。每个控制器同样可以拥有多个方法，负责不同的请求操作。如何将每个请求毫无差错的映射到对应的控制器方法中是 Spring MVC 框架的重要任务之一，这项任务由 <strong>@RequestMapping</strong> 承担。</p>
<p>在控制器的类定义和方法定义处都可以标注 @RequestMapping。<strong>类定义处的 @RequestMapping</strong> 提供初步的请求映射信息，表示该类下的所有请求都必须先经过类定义处的路径；<strong>方法定义处的 @RequestMapping</strong> 提供进一步更加细分的请求映射信息。DispatcherServlet 截获请求后，就能通过控制器上的 @RequestMapping 确定具体的处理方法。</p>
<p>将请求映射到控制器处理方法的工作包含一系列映射规则，这些规则是根据请求中的各种信息制定的，具体包括<strong>请求 URL</strong>、<strong>请求参数</strong>、<strong>请求方法</strong>、<strong>请求头</strong>这4个方面的信息。</p>
<h5 id="通过请求-URL-进行映射"><a href="#通过请求-URL-进行映射" class="headerlink" title="通过请求 URL 进行映射"></a>通过请求 URL 进行映射</h5><p>@RequestMapping 使用 value 值指定请求的 URL，如 @RequestMapping(“&#x2F;user”)。</p>
<p>@RequestMapping 在<strong>类定义处指定的 URL</strong> 相对于 Web 应用的部署路径，而在<strong>方法定义处指定的 URL</strong> 则相对于类定义处指定的 URL。如果类定义处未标注 @RequestMapping，则方法定义处指定的 URL 同样也要相对于 Web 应用的部署路径。比如你在 Tomcat 里部署的根路径是 &#x2F;springmvc2，那么类定义处的 URL 实际是 &#x2F;springmvc2&#x2F;user，方法定义处的 URL 是 &#x2F;user&#x2F;register.html，当然前面还是要经过&#x2F;springmvc2。而如果类定义处没有标注 @RequestMapping，那么方法定义处的 URL 就是 &#x2F;springmvc2&#x2F;register.html</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/@RequestMapping%E8%B7%AF%E5%BE%84%E7%A4%BA%E4%BE%8B.6aoikpjxpfw0.webp" alt="@RequestMapping路径示例"></p>
<p>@RequestMapping 不但支持标准的 URL，还支持 Ant 风格（？、* 和 ** 字符）和带 {xxx} 占位符的 URL。如：</p>
<ul>
<li>&#x2F;user&#x2F;*&#x2F;createUser：匹配 &#x2F;user&#x2F;aaa&#x2F;createUser，&#x2F;user&#x2F;bbb&#x2F;createUser 等；</li>
<li>&#x2F;user&#x2F;**&#x2F;createUser：匹配 &#x2F;user&#x2F;aaa&#x2F;bbb&#x2F;createUser，&#x2F;user&#x2F;createUser 等；</li>
<li>&#x2F;user&#x2F;createUser??：匹配 &#x2F;user&#x2F;createUseraa，&#x2F;user&#x2F;createUserbb 等；</li>
<li>&#x2F;user&#x2F;{userId}：匹配 user&#x2F;123，user&#x2F;456 等；</li>
<li>&#x2F;user&#x2F;**&#x2F;{userId}：匹配 user&#x2F;aaa&#x2F;bbb&#x2F;123，user&#x2F;aaa&#x2F;456 等；</li>
<li>company&#x2F;{companyId}&#x2F;user&#x2F;{userId}&#x2F;detail：匹配 company&#x2F;123&#x2F;user&#x2F;456&#x2F;detail 等。</li>
</ul>
<p>通过 <strong>@PathVariable</strong> 可以将 URL 中的占位符参数绑定到控制器处理方法的入参中，这样调用该方法时控制器方法中参数的值就是 URL 中占位符参数的值。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/@PathVariable%E7%BB%91%E5%AE%9A%E5%85%A5%E5%8F%82.65mr1umlcqs0.webp" alt="@PathVariable绑定入参"></p>
<p>如果 @PathVariable 后面的括号中包含了参数名称，则必须和 URL 占位符参数一致，如果未指定参数名称，则控制器方法入参要和 URL 占位符参数一致。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/@PathVariable%E5%90%8E%E4%B8%8D%E6%8C%87%E5%AE%9A%E5%8F%82%E6%95%B0%E5%90%8D%E7%A7%B0.3rvbb384x2g0.webp" alt="@PathVariable后不指定参数名称"></p>
<p>不过在默认情况下，Java 类的反射对象并未包含方法入参的名称，在 Java 8.0 中可以通过 javac -parameter 生成方法入参的元数据信息，在低版本的 Java 中则可以通过 javac -g 打开生成所有调试信息的开发，这样也会包含方法入参名的元数据信息。所以要使 findPet() 方法入参成功绑定 URL 中的占位符操作，必须保证在编译时输出方法名元信息。在 Maven 中可以显式配置 maven-compile-plugin 编译插件，开启编译输出调试信息的开关。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.maven.plugins<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-compile-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">source</span>&gt;</span>$&#123;java.version&#125;<span class="tag">&lt;/<span class="name">source</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">target</span>&gt;</span>$&#123;java.version&#125;<span class="tag">&lt;/<span class="name">target</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">encoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">encoding</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">debug</span>&gt;</span>true<span class="tag">&lt;/<span class="name">debug</span>&gt;</span>		# 开启开关</span><br><span class="line">    <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>不过编译时打开 debug 开关会使目标类变大，对运行效率也有一定影响，正式场景往往将此开关关闭。因此在<strong>使用 @PathVariable 时最好显式地指定绑定的参数名</strong>。</p>
<h5 id="通过请求参数、请求方法或请求头进行映射"><a href="#通过请求参数、请求方法或请求头进行映射" class="headerlink" title="通过请求参数、请求方法或请求头进行映射"></a>通过请求参数、请求方法或请求头进行映射</h5><p>一个标准的 HTTP 请求报文结构：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/HTTP%E8%AF%B7%E6%B1%82%E6%8A%A5%E6%96%87.4onp7yy3cgi0.webp" alt="HTTP请求报文"></p>
<p>①<strong>请求方法</strong>，包含 GET、POST、HEAD、DELETE、OPTIONS、PUT、TRACE 等。</p>
<p>②<strong>请求 URL</strong>，和报文头的 Host 属性组成完整的请求 URL。</p>
<p>③<strong>协议名称及版本号</strong>。</p>
<p>④<strong>报文头</strong>，包含若干属性，格式为“属性名:属性值”，服务器端根据报文头获取客户端的信息。</p>
<p>⑤<strong>报文体</strong>，它将表单页面中的组件值通过 param&#x3D;value1&amp;param2&#x3D;value2 的键值对形式编码成一个格式化串，不但报文体可以传递请求参数，URL 也可以通过类似于 &#x2F;springmvc2&#x2F;user.html?param&#x3D;value1&amp;param2&#x3D;value2 的方式传递请求参数。</p>
<p>@RequestMapping 不仅可以使用 URL 映射请求，还可以使用请求方法、请求头参数及请求参数（报文体和 URL 包含的请求参数）映射请求。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%BD%BF%E7%94%A8%E5%85%B6%E4%BB%96%E4%BF%A1%E6%81%AF%E6%98%A0%E5%B0%84%E8%AF%B7%E6%B1%82.39rkwt04qpg0.webp" alt="使用其他信息映射请求"></p>
<p>params 和 headers 支持简单的映射表达式，如：</p>
<ul>
<li>“param1”：表示请求需包含名为 param1 的请求参数。</li>
<li>“!param1”：表示请求不能包含名为 param1 的请求参数。</li>
<li>“param1!&#x3D;value1”：表示请求包含名为 param1 的请求参数，但值不能为 value1。</li>
<li>{“param1&#x3D;value1”,”param2”}：表示请求必须包含名为 param1 和 param2 的请求参数，且 param1 的值必须为 value1。</li>
</ul>
<h4 id="2-2-请求处理方法签名"><a href="#2-2-请求处理方法签名" class="headerlink" title="2.2 请求处理方法签名"></a>2.2 请求处理方法签名</h4><p>Spring MVC 通过<strong>分析处理方法的签名</strong>（就是入参），将 HTTP 请求信息绑定到处理方法的相应入参中，然后再调用处理方法得到返回值，最后对返回值进行处理并响应。</p>
<p>一般情况下，处理方法的返回值类型为 <strong>ModelAndView</strong> 或者 <strong>String</strong>，前者包含模型和逻辑视图名，后者仅包含一个逻辑视图名。</p>
<p>几种典型的方法签名：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//请求参数按名称匹配的方式绑定到方法入参中，方法返回的字符串代表逻辑视图名</span></span><br><span class="line"><span class="meta">@RequestMapping(path = &quot;/handle1&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle1</span><span class="params">(<span class="meta">@RequestParam(&quot;userName&quot;)</span> String userName,</span></span><br><span class="line"><span class="params">                      <span class="meta">@RequestParam(&quot;password&quot;)</span> String password,</span></span><br><span class="line"><span class="params">                      <span class="meta">@RequestParam(&quot;realName&quot;)</span> String realName)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//将Cookie值及报文头属性绑定到入参中，方法返回ModelAndView</span></span><br><span class="line"><span class="meta">@RequestMapping(path = &quot;/handle2&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ModelAndView <span class="title function_">handle2</span><span class="params">(<span class="meta">@CookieValue(&quot;JSESSIONID&quot;)</span> String sessionId,</span></span><br><span class="line"><span class="params">                            <span class="meta">@RequestHeader(&quot;Accept-Language&quot;)</span> String acceptLanguage)</span>&#123;</span><br><span class="line">    <span class="type">ModelAndView</span> <span class="variable">mav</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ModelAndView</span>();</span><br><span class="line">    mav.setViewName(<span class="string">&quot;success&quot;</span>);</span><br><span class="line">    mav.addObject(<span class="string">&quot;user&quot;</span>, <span class="keyword">new</span> <span class="title class_">User</span>());</span><br><span class="line">    <span class="keyword">return</span> mav;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//请求参数按名称匹配的方式绑定到user的属性中，方法返回的字符串代表逻辑视图名</span></span><br><span class="line"><span class="meta">@RequestMapping(path = &quot;/handle3&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle3</span><span class="params">(User user)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//直接将HTTP请求对象传递给处理方法，方法返回的字符串代表逻辑视图名</span></span><br><span class="line"><span class="meta">@RequestMapping(path = &quot;/hanlde4&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle4</span><span class="params">(HttpServletRequest request)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-3-使用矩阵变量绑定参数"><a href="#2-3-使用矩阵变量绑定参数" class="headerlink" title="2.3 使用矩阵变量绑定参数"></a>2.3 使用矩阵变量绑定参数</h4><p>在 Spring MVC 3.2 中出现了 <strong>@MatrixVariable</strong> 注解，该注解能够将请求中的<strong>矩阵变量</strong>（Matrix Variable，即请求路径中的一个&#x2F;*&#x2F;中有多个变量）绑定到处理器的方法参数中。</p>
<p>在 Matrix Variable 中，多个变量可以使用“;”分隔，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/books;author=Tom;year=2022</span><br></pre></td></tr></table></figure>

<p> 如果一个变量对应多个值，那么可以使用“,”分隔，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">author=smart1,smart2,smart3</span><br></pre></td></tr></table></figure>

<p>或者使用重复的变量名，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">author=smart1;author=smart2;author=smart3</span><br></pre></td></tr></table></figure>

<p>举一个例子来说明矩阵变量：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// GET /books/22;a=11;b=22</span></span><br><span class="line"><span class="meta">@RequestMapping(value = &quot;/books/&#123;bookId&#125;&quot;, method = RequestMethod.GET)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">findBookId</span><span class="params">(<span class="meta">@PathVariable</span> String bookId, <span class="meta">@MatrixVariable</span> <span class="type">int</span> a)</span>&#123;</span><br><span class="line">    <span class="comment">// do sth</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当 URL 输入 &#x2F;books&#x2F;22;a&#x3D;11;b&#x3D;22（22;a&#x3D;11;b&#x3D;22就是矩阵变量） 时，相应的 bookId 和 a 都会被映射到 findBookId() 方法中，如果映射不到，则会报错。如果 URL 中没有相应的 a 和 b 的值，则需要指定空值（@MatrixVariable(required&#x3D;false)）才不会报错。</p>
<p>再来看一个复杂的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//GET /books/42;a=11/authors/21;q=22</span></span><br><span class="line"><span class="meta">@RequestMapping(value = &quot;/books/&#123;bookId&#125;/authors/&#123;authorId&#125;&quot;, method = RequestMethod.GET)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">findBook</span><span class="params">(<span class="meta">@MatrixVariable(value = &quot;a&quot;, pathVar = &quot;bookId&quot;)</span> <span class="type">int</span> q1,</span></span><br><span class="line"><span class="params">                     <span class="meta">@MatrixVariable(value = &quot;b&quot;, pathVar = &quot;authorId&quot;)</span> <span class="type">int</span> q2)</span>&#123;</span><br><span class="line">    <span class="comment">// q1=11</span></span><br><span class="line">    <span class="comment">// q2=22</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>针对每个 Path Variable 绑定一个 Matrix Variable，然后使用 value 和 pathVar 属性就能找到该值。根据 bookId 和 a 就能确定值在哪个 &#x2F;*&#x2F; 中，进而就可以绑定参数的值。所以取参数名称时应该避免重复。</p>
<p>Matrix Variable 也自带了一些属性可供选择，例如是否必须，默认值等。</p>
<p>默认 Matrix Variable 功能是开启的，如果要关闭该功能，则需要手工将 RequestMappingHandlerMapping 中的 removeSemicolonContent 属性设置为 true，按照 xml 的方式就是 <code>&lt;mvc:annotation-driven enable-matrix-varibale=&quot;true&quot;&gt;</code>。</p>
<h4 id="2-4-请求处理方法签名详细说明"><a href="#2-4-请求处理方法签名详细说明" class="headerlink" title="2.4 请求处理方法签名详细说明"></a>2.4 请求处理方法签名详细说明</h4><h5 id="使用-RequestParam-绑定请求参数值"><a href="#使用-RequestParam-绑定请求参数值" class="headerlink" title="使用 @RequestParam 绑定请求参数值"></a>使用 @RequestParam 绑定请求参数值</h5><p>由于 Java 类反射对象默认不记录方法入参的名称，因此需要在方法入参处使用 <strong>@RequestParam</strong> 注解来指定其对应的请求参数，该注解有以下 3 个参数：</p>
<ul>
<li><strong>value</strong>：参数名。</li>
<li><strong>required</strong>：是否必须，默认为 true，表示必须包含对应参数名。</li>
<li><strong>defaultValue</strong>：默认参数名，不推荐使用该参数。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//将请求参数userName和age绑定到handle11()方法的userName和age参数中，并自动完成类型转换，age参数是必须的</span></span><br><span class="line"><span class="meta">@RequestMapping(path = &quot;/handle11&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handle11</span><span class="params">(<span class="meta">@RequestParam(value = &quot;userName&quot;,required = false)</span> String userName, <span class="meta">@RequestParam(&quot;age&quot;)</span> <span class="type">int</span> age)</span>&#123;</span><br><span class="line">    <span class="comment">// do sth</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="使用-CookieValue-绑定请求中的-Cookie-值"><a href="#使用-CookieValue-绑定请求中的-Cookie-值" class="headerlink" title="使用 @CookieValue 绑定请求中的 Cookie 值"></a>使用 @CookieValue 绑定请求中的 Cookie 值</h5><p><strong>@CookieValue</strong> 和 @RequestParam 拥有 3 个一样的参数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/handle12&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handle12</span><span class="params">(<span class="meta">@CookieValue(value = &quot;sessionId&quot;,required = false)</span> String sessionId, <span class="meta">@RequestParam(&quot;age&quot;)</span> <span class="type">int</span> age)</span>&#123;</span><br><span class="line">    <span class="comment">// do sth</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="使用-RequestHeader-绑定请求报文头的属性值"><a href="#使用-RequestHeader-绑定请求报文头的属性值" class="headerlink" title="使用 @RequestHeader 绑定请求报文头的属性值"></a>使用 @RequestHeader 绑定请求报文头的属性值</h5><p>根据 HTTP 报文的结构可知，请求报文头包含了若干个报文头属性，服务器可以据此获知客户端的信息，通过 <strong>@RequestHeader</strong> 即可将报文头属性值绑定到处理方法的入参中。@RequestHeader 和 @RequestParam 拥有 3 个一样的参数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/handle13&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handle13</span><span class="params">(<span class="meta">@RequestHeader(&quot;Accept-Encoding&quot;)</span> String encoding,</span></span><br><span class="line"><span class="params">                     <span class="meta">@RequestHeader(&quot;Keep-Alive&quot;)</span> <span class="type">long</span> keepAlive)</span>&#123;</span><br><span class="line">    <span class="comment">// do sth</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="使用命令-x2F-表单对象绑定请求参数值"><a href="#使用命令-x2F-表单对象绑定请求参数值" class="headerlink" title="使用命令&#x2F;表单对象绑定请求参数值"></a>使用命令&#x2F;表单对象绑定请求参数值</h5><p>所谓命令&#x2F;表单对象不需要实现任何接口，仅仅是一个拥有若干属性的 POJO 。下述代码中的 user 就是一个命令&#x2F;表单对象，前端传来的表单数据和 user 对象根据具体属性值一一绑定在一起（详情可见 <a href="https://lisj98.github.io/2022/04/06/%E7%B2%BE%E9%80%9ASpringMVC-%E4%B8%80-SpringMVC%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84%E6%A6%82%E8%BF%B0/">https://lisj98.github.io/2022/04/06/%E7%B2%BE%E9%80%9ASpringMVC-%E4%B8%80-SpringMVC%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84%E6%A6%82%E8%BF%B0/</a> 的”编写视图对象“部分）。Spring MVC 会按照请求参数名和命令&#x2F;表单对象属性名匹配的方式，自动为该对象填充属性值。支持级联的属性名，如 dept.deptId，dept.address.tel 等。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/handle14&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handle14</span><span class="params">(User user)</span>&#123;</span><br><span class="line">    <span class="comment">// do sth</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">/handle14.html?userName=tom&amp;dept.deptId=<span class="number">1</span>&amp;dept.address.tel=<span class="number">102</span></span><br></pre></td></tr></table></figure>

<h5 id="使用-Servlet-API-对象作为入参"><a href="#使用-Servlet-API-对象作为入参" class="headerlink" title="使用 Servlet API 对象作为入参"></a>使用 Servlet API 对象作为入参</h5><p>在 Spring MVC 中，控制器类可以不依赖任何 Servlet API 对象，但是 Spring MVC 并不能阻止我们使用 Servlet API 的类作为处理方法的入参。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//同时使用HttpServletRequest和HttpServletResponse作为入参</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/handle21&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handle21</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span>&#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">userName</span> <span class="operator">=</span> WebUtils.findParameterValue(request, <span class="string">&quot;userName&quot;</span>);</span><br><span class="line">    response.addCookie(<span class="keyword">new</span> <span class="title class_">Cookie</span>(<span class="string">&quot;userName&quot;</span>, userName));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//仅使用HttpServletRequest作为入参</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/handle22&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ModelAndView <span class="title function_">handle22</span><span class="params">(HttpServletRequest request)</span>&#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">userName</span> <span class="operator">=</span> WebUtils.findParameterValue(request, <span class="string">&quot;userName&quot;</span>);</span><br><span class="line">    <span class="type">ModelAndView</span> <span class="variable">mav</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ModelAndView</span>();</span><br><span class="line">    mav.setViewName(<span class="string">&quot;success&quot;</span>);</span><br><span class="line">    mav.addObject(<span class="string">&quot;userName&quot;</span>, userName);</span><br><span class="line">    <span class="keyword">return</span> mav;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//使用HttpSession作为入参</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/handle23&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle23</span><span class="params">(HttpSession session)</span>&#123;</span><br><span class="line">    session.setAttribute(<span class="string">&quot;sessionId&quot;</span>, <span class="number">1234</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//既使用HttpServletRequest，又使用基本类型的入参</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/handle24&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle24</span><span class="params">(HttpServletRequest request, <span class="meta">@RequestParam(&quot;userName&quot;)</span> String userName)</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在使用 Servlet API 的类作为入参时，Spring MVC 会自动将 Web 层对应的 Servlet 对象传递给处理方法的入参。处理方法入参可以同时使用 Servlet API 类的入参和其他符合要求的入参，它们之间的位置顺序没有要求。</p>
<p>Spring MVC 在 <code>org.sprnigframework.web.context.request</code> 包中定义了若干个可代理 Servlet 原生 API 类的接口，如 <strong>WebRequest</strong> 和 <strong>NativeRequest</strong>，它们也允许作为处理类的入参，通过这些代理类可访问请求对象的任何信息。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/handle25&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle25</span><span class="params">(WebRequest request)</span>&#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">userName</span> <span class="operator">=</span> request.getParameter(<span class="string">&quot;userName&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="使用-I-x2F-O-对象作为入参"><a href="#使用-I-x2F-O-对象作为入参" class="headerlink" title="使用 I&#x2F;O 对象作为入参"></a>使用 I&#x2F;O 对象作为入参</h5><p>Servlet 的 <strong>ServletRequest</strong> 拥有 <strong>getInputStream()</strong> 和 <strong>getReader()</strong> 方法，可以通过它们读取请求的信息。相应的，Servlet 的 <strong>ServletResponse</strong> 拥有 <strong>getOutputStream()</strong> 和 <strong>getWriter()</strong> 方法，可以通过它们输出响应信息。</p>
<p>Spring MVC 允许控制器的处理方法使用 <code>java.io.InputStream</code>&#x2F;<code>java.io.Reader</code> 及 <code>java.io.OutputStream</code>&#x2F;<code>java.io.Writer</code> 作为方法的入参，Spring MVC 将获取 ServletRequest 的 InputStream&#x2F;Reader 或 ServletResponse 的 OutputStream&#x2F;Writer，然后传递给控制器的处理方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用OutputStream输出一张图片</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;handle31&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handle31</span><span class="params">(OutputStream os)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="type">Resource</span> <span class="variable">res</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ClassPathResource</span>(<span class="string">&quot;/image.jpg&quot;</span>); <span class="comment">//读取类路径下的图片文件</span></span><br><span class="line">    FileCopyUtils.copy(res.getInputStream(), os);   <span class="comment">//将图片写到输出流中</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="其他类型的参数"><a href="#其他类型的参数" class="headerlink" title="其他类型的参数"></a>其他类型的参数</h5><p>控制器处理方法的入参除了支持以上类型的参数外，还支持 <code>java.util.Locale</code>、<code>java.security.Principal</code>，可以通过 Servlet 的 HttpServletRequest 的 getLocale() 和 getUserPrinciple() 方法得到相应的值，如果处理方法的入参类型为 Locale 或 Principle ，则 Spring MVC 会自动从请求对象中获取相应的对象并传递给处理方法的入参。</p>
<h4 id="2-5-使用-HttpMessageConverter-lt-T-gt"><a href="#2-5-使用-HttpMessageConverter-lt-T-gt" class="headerlink" title="2.5 使用 HttpMessageConverter&lt;T&gt;"></a>2.5 使用 HttpMessageConverter&lt;T&gt;</h4><p><strong>HttpMessageConverter&lt;T&gt;</strong> 负责将请求信息转换为一个对象（类型为 T），将对象（类型为 T）输出为响应信息。</p>
<p>DispatcherServlet 默认安装了 <strong>RequestMappingHandlerAdapter</strong> 作为 HandlerAdapter 的组件实现类，HttpMessageConverter 即由 RequestMappingHandlerAdapter 使用，将请求信息转换为对象，或将对象转换为响应信息。</p>
<p>HttpMessageConverter&lt;T&gt;接口定义了以下几个方法：</p>
<ul>
<li>**Boolean canRead(Class&lt;?&gt; clazz, MediaType mediaType)**：指定转换器可以读取的对象类型，即转换器可将请求信息转换为 clazz 类型的对象；同时指定支持的 MIME 媒体类型（如 text&#x2F;html，application&#x2F;json 等）。</li>
<li>**Boolean canWrite(Class&lt;?&gt; clazz, MediaType mediaType)**：指定转换器可以将 clazz 类型的对象写到相应流中。</li>
<li>**List&lt;MediaType &gt; getSupportedMediaTypes()**：该转换器支持的媒体类型。</li>
<li>**T read(Class&lt;? extends T&gt; clazz, HttpInputMessage inputMessage)**：将请求信息流转换为T类型的对象。</li>
<li>**void write(T t, MediaType contentType, HttpOutputMessage outputMessage)**：将 T 类型的对象写到响应流中，同时指定的媒体类型为 contentType。</li>
</ul>
<h5 id="HttpMessageConverter-lt-T-gt-的实现类"><a href="#HttpMessageConverter-lt-T-gt-的实现类" class="headerlink" title="HttpMessageConverter&lt;T&gt; 的实现类"></a>HttpMessageConverter&lt;T&gt; 的实现类</h5><p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/HttpMessageConverter%E7%9A%84%E5%AE%9E%E7%8E%B0%E7%B1%BB1.3os5c4txvlk0.webp" alt="HttpMessageConverter的实现类1"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/HttpMessageConverter%E7%9A%84%E5%AE%9E%E7%8E%B0%E7%B1%BB2.250rgbrxarr4.webp" alt="HttpMessageConverter的实现类2"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/HttpMessageConverter%E7%9A%84%E5%AE%9E%E7%8E%B0%E7%B1%BB3.27ym0dlkj0lc.webp" alt="HttpMessageConverter的实现类3"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/HttpMessageConverter%E7%9A%84%E5%AE%9E%E7%8E%B0%E7%B1%BB4.trzs189qv8g.webp" alt="HttpMessageConverter的实现类4"></p>
<p>RequestMappingHandlerAdapter 默认已经装配了以下 HttpMessageConverter：</p>
<ul>
<li>StringHttpMessageConverter</li>
<li>ByteArrayHttpMessageConverter</li>
<li>SourceHttpMessageConverter</li>
<li>AllEncompassingFormHttpMessageConverter</li>
</ul>
<p>如果需要装配其他类型的 HttpMessageConverter ，只需在 Spring 容器的 Web 容器上下文（smart-servlet.xml）中自行定义一个 RequestMappingHandlerAdapter。如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--定义一个RequestMappingHandlerAdapter --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">p:messageConverters-ref</span>=<span class="string">&quot;messageConverters&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--HttpMessageConverter列表--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">util:list</span> <span class="attr">id</span>=<span class="string">&quot;messageConverters&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.BufferedImageHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.ByteArrayHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.StringHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.support.AllEncompassingFormHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">util:list</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果显式定义了一个 RequestMappingHandlerAdapter ，则 Spring MVC 将会覆盖默认的 RequestMappingHandlerAdapter。</p>
<h5 id="使用-HttpMessageConverter-lt-T-gt"><a href="#使用-HttpMessageConverter-lt-T-gt" class="headerlink" title="使用 HttpMessageConverter&lt;T&gt;"></a>使用 HttpMessageConverter&lt;T&gt;</h5><p>如何使用 HttpMessageConverter&lt;T&gt; 将请求信息转换并绑定到处理方法的入参中呢？Spring MVC 提供了两种途径。</p>
<ul>
<li>使用 <strong>@RequestBody&#x2F;@ResponseBody</strong> 对处理方法进行标注。</li>
<li>使用 <strong>HttpEntity&lt;T&gt;&#x2F;ResponseEntity&lt;T&gt;</strong> 作为处理方法的入参或返回值。</li>
</ul>
<p>@RequestBody&#x2F;@ResponseBody 的例子：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%BD%BF%E7%94%A8@RequestBody%E4%B8%8E@ResponseBody.75sqoa12vvs0.webp" alt="使用@RequestBody与@ResponseBody"></p>
<p>接下来编写代码进行测试：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserControllerTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testHandle41</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="type">RestTemplate</span> <span class="variable">restTemplate</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RestTemplate</span>();</span><br><span class="line">        LinkedMultiValueMap&lt;String, String&gt; form = <span class="keyword">new</span> <span class="title class_">LinkedMultiValueMap</span>&lt;&gt;();</span><br><span class="line">        form.add(<span class="string">&quot;userName&quot;</span>, <span class="string">&quot;tom&quot;</span>);</span><br><span class="line">        form.add(<span class="string">&quot;password&quot;</span>, <span class="string">&quot;123456&quot;</span>);</span><br><span class="line">        form.add(<span class="string">&quot;age&quot;</span>, <span class="string">&quot;45&quot;</span>);</span><br><span class="line">        <span class="comment">//第一个参数为URL，第二个参数通过MultiValueMap准备报文体的参数数据</span></span><br><span class="line">        restTemplate.postForLocation(<span class="string">&quot;http://localhost:8080/springmvc2/user/handle41.html&quot;</span>, form);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testHandle42</span><span class="params">()</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        <span class="type">RestTemplate</span> <span class="variable">restTemplate</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RestTemplate</span>();</span><br><span class="line">        <span class="comment">//第一个参数为URL，第二个参数为报文体参数数据，第三个参数指定方法的返回值类型，第四个参数为URL占位符参数的值</span></span><br><span class="line">        <span class="type">byte</span>[] response = restTemplate.postForObject(<span class="string">&quot;http://localhost:8080/springmvc2/user/handle42/&#123;itemId&#125;.html&quot;</span>, <span class="literal">null</span>, <span class="type">byte</span>[].class, <span class="string">&quot;1233&quot;</span>);</span><br><span class="line">        <span class="type">Resource</span> <span class="variable">outFile</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileSystemResource</span>(<span class="string">&quot;d:/image_copy.jpg&quot;</span>);</span><br><span class="line">        FileCopyUtils.copy(response, outFile.getFile());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>RestTemplate</strong> 是 Spring 的模板类，在客户端程序中可使用该类调用 Web 服务器端的服务，它支持 REST 风格的 URL。此外，它像 RequestMappingHandlerAdapter 一样拥有一张 HttpMessageConverter 的注册表，默认注册了以下 HttpMessageConverter：</p>
<ul>
<li>ByteArrayHttpMessageConverter</li>
<li>StringHttpMessageConverter</li>
<li>ResourceHttpMessageConverter</li>
<li>SourceHttpMessageConverter</li>
<li>AllEncompassingFormHttpMessageConverter</li>
</ul>
<p>在默认情况下，RestTemplate 可使用这些 HttpMessageConverter 对响应数据进行相应的转换处理。也可通过 RestTemplate 的 <strong>setMessageConverters(List&lt;HttpMessageConverter&lt;?&gt;&gt; messageConverters)</strong> 方法手工注册 HttpMessageConverter。</p>
<p>和 @RequestBody&#x2F;@ResponseBody 类似，<strong>HttpEntity&lt;?&gt;</strong> 不但可以访问请求和响应报文体的数据，还可以访问请求和响应报文头的数据，Spring MVC 根据 HttpEntity 的泛型类型查找对应的 HttpMessageConverter。</p>
<p>HttpEntity&lt;T&gt;&#x2F;ResponseEntity&lt;T&gt; 的例子：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%BD%BF%E7%94%A8HttpEntity%E5%92%8CResponseEntity.22kmxyjd4g4g.webp" alt="使用HttpEntity和ResponseEntity"></p>
<p>通过以上实例，可以得出如下结论：</p>
<ul>
<li>当控制器处理方法使用 @RequestBody&#x2F;@ResponseBody 或者 HttpEntity&lt;T&gt;&#x2F;ResponseEntity&lt;T&gt; 时，Spring MVC 才使用注册的 HttpMessageConverter 对请求&#x2F;响应消息进行处理。</li>
<li>当控制器处理方法使用 @RequestBody&#x2F;@ResponseBody 或者 HttpEntity&lt;T&gt;&#x2F;ResponseEntity&lt;T&gt; 时，Spring 首先根据请求头或响应头的 Accept 属性选择匹配的 HttpMessageConverter ，然后根据参数类型或泛型类型的过滤得到匹配的 HttpMessageConverter ，如果找不到可用的 HttpMessageConverter 则报错。</li>
<li>@RequestBody 和 @ResponseBody 不需要成对出现。如果方法入参使用了 @RequestBody，则 Spring MVC 选择匹配的 HttpMessageConverter 将请求消息转换并绑定到入参中；如果处理方法标注了 @ResponseBody 则 Spring MVC 选择匹配的 HttpMessageConverter 将方法返回值转换并输出响应消息。</li>
<li>HttpEntity&lt;T&gt;&#x2F;ResponseEntity&lt;T&gt; 的功能和 @RequestBody&#x2F;@ResponseBody 几乎一致。</li>
</ul>
<h5 id="处理-XML-和-JSON"><a href="#处理-XML-和-JSON" class="headerlink" title="处理 XML 和 JSON"></a>处理 XML 和 JSON</h5><p>Spring MVC 提供了几个处理 XML 和 JSON 格式的请求&#x2F;响应消息的 HttpMessageConverter。</p>
<ul>
<li><strong>MarshallingHttpMessageConverter</strong>：处理 XML 格式的请求或响应消息。</li>
<li><strong>Jaxb2RootElementHttpMessageConverter</strong>：同上，底层使用 JXAB。</li>
<li><strong>MappingJackson2HttpMessageConverter</strong>：处理 JSON 格式的请求或响应消息。</li>
</ul>
<p>只要在 Spring Web 容器中为 RequestMappingHandlerAdapter 装配好相应的处理 XML 和 JSON 格式的请求&#x2F;响应消息的 HttpMessageConverter，并在交互中通过请求的 Accept 指定 MIME 类型，Spring MVC 就可使服务器端的处理方法和客户端透明地通过 XML 或 JSON 格式的消息进行通信，开发者无需关心通信层数据格式的问题，可以将精力集中到业务层的处理上。</p>
<p>首先为 RequestMappingHandlerAdapter 装配可处理 XML 和 JSON 格式的请求&#x2F;响应消息的 HttpMessageConverter：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--定义一个RequestMappingHandlerAdapter --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">p:messageConverters-ref</span>=<span class="string">&quot;messageConverters&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--HttpMessageConverter列表--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">util:list</span> <span class="attr">id</span>=<span class="string">&quot;messageConverters&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.BufferedImageHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.ByteArrayHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.StringHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.support.AllEncompassingFormHttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.xml.MarshallingHttpMessageConverter&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:marshaller-ref</span>=<span class="string">&quot;xmlMarshaller&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:unmarshaller-ref</span>=<span class="string">&quot;xmlMarshaller&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.http.converter.json.MappingJackson2HttpMessageConverter&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">util:list</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--声明Marshaller，使用XStream技术--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;xmlMarshaller&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.oxm.xstream.XStreamMarshaller&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;streamDriver&quot;</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--使用STAX对XML消息进行处理，STAX占用内存少，响应速度也很快--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;com.thoughtworks.xstream.io.xml.StaxDriver&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;annotatedClasses&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">            <span class="comment">&lt;!--使用XStream的注解定义XML转换规则，使用XStream注解的类在此声明--&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">value</span>&gt;</span>com.smart.entity.User<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>再在控制器中编写响应的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/handle51&quot;)</span></span><br><span class="line"><span class="keyword">public</span> ResponseEntity&lt;User&gt; <span class="title function_">handle51</span><span class="params">(HttpEntity&lt;User&gt; requestEntity)</span>&#123;</span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> requestEntity.getBody();</span><br><span class="line">    user.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ResponseEntity</span>&lt;User&gt;(user, HttpStatus.OK);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于服务器端的处理方法而言，除使用 @RequestBody&#x2F;@ResponseBody 或 HttpEntity&lt;T&gt;&#x2F;ResponseEntity&lt;T&gt; 进行方法签名外，不需要进行任何额外的处理，借由 Spring MVC 中装配的 HttpMessageConverter，它便拥有了处理 XML 和 JSON 格式的消息的能力。</p>
<p>在接收到一个 HTTP 请求时，handle51() 如何知道请求消息的格式？在处理完后又根据什么确定响应消息的格式？答案很简单：通过请求头的 <strong>Content-Type</strong> （请求）及 <strong>Accept</strong>（响应） 属性确定。</p>
<p>下面编写测试程序：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用RestTemplate测试UserController#handle51()方法</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">testHandle51</span><span class="params">()</span> <span class="keyword">throws</span> IOException&#123;</span><br><span class="line">    <span class="type">RestTemplate</span> <span class="variable">restTemplate</span> <span class="operator">=</span> buildRestTemplate();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建User对象，它将通过RestTemplate流化为XML请求报文</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">    user.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">    user.setPassword(<span class="string">&quot;1234&quot;</span>);</span><br><span class="line">    user.setRealName(<span class="string">&quot;汤姆&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//指定请求的报文头信息</span></span><br><span class="line">    <span class="type">HttpHeaders</span> <span class="variable">entityHeaders</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HttpHeaders</span>();</span><br><span class="line">    entityHeaders.setContentType(MediaType.valueOf(<span class="string">&quot;application/xml;UTF-8&quot;</span>));</span><br><span class="line">    entityHeaders.setAccept(Collections.singletonList(MediaType.APPLICATION_XML));</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将User流化为XML，放到报文体中，同时指定请求方法和报文头</span></span><br><span class="line">    HttpEntity&lt;User&gt; requestEntity = <span class="keyword">new</span> <span class="title class_">HttpEntity</span>&lt;&gt;(user, entityHeaders);</span><br><span class="line">    ResponseEntity&lt;User&gt; responseEntity = restTemplate.exchange(<span class="string">&quot;http://localhost:8080/springmvc2/user/handle51.html&quot;</span>, HttpMethod.POST, requestEntity, User.class);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将请求响应消息转换为User对象，并将响应值进行判断</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">responseUser</span> <span class="operator">=</span> responseEntity.getBody();</span><br><span class="line">    Assert.assertNotNull(responseUser);</span><br><span class="line">    Assert.assertEquals(<span class="string">&quot;tom&quot;</span>, responseUser.getUserName());</span><br><span class="line">    Assert.assertEquals(<span class="string">&quot;1234&quot;</span>, responseUser.getPassword());</span><br><span class="line">    Assert.assertEquals(<span class="string">&quot;汤姆&quot;</span>,responseUser.getRealName());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建RestTemplate实例</span></span><br><span class="line">    <span class="keyword">private</span> RestTemplate <span class="title function_">buildRestTemplate</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="type">RestTemplate</span> <span class="variable">restTemplate</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">RestTemplate</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//使用XStream流化器，使用STAX技术处理XML，同时加载使用了XStream注解的User类</span></span><br><span class="line">    <span class="type">XStreamMarshaller</span> <span class="variable">xmlMarshaller</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">XStreamMarshaller</span>();</span><br><span class="line">    xmlMarshaller.setStreamDriver(<span class="keyword">new</span> <span class="title class_">StaxDriver</span>());</span><br><span class="line">    xmlMarshaller.setAnnotatedClasses(<span class="keyword">new</span> <span class="title class_">Class</span>[]&#123;User.class&#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建处理XML报文的HttpMessageConverter，将其组装到RestTemplate中</span></span><br><span class="line">    <span class="type">MarshallingHttpMessageConverter</span> <span class="variable">xmlConverter</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MarshallingHttpMessageConverter</span>();</span><br><span class="line">    xmlConverter.setMarshaller(xmlMarshaller);</span><br><span class="line">    xmlConverter.setUnmarshaller(xmlMarshaller);</span><br><span class="line">    restTemplate.getMessageConverters().add(xmlConverter);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建处理JSON报文的HttpMessageConverter，将其组装到RestTemplate中</span></span><br><span class="line">    <span class="type">MappingJackson2HttpMessageConverter</span> <span class="variable">jsonConverter</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MappingJackson2HttpMessageConverter</span>();</span><br><span class="line">    restTemplate.getMessageConverters().add(jsonConverter);</span><br><span class="line">    <span class="keyword">return</span> restTemplate;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行测试方法，观察浏览器控制台，我们可以知道客户端的 User 对象被流化为一段对应的 XML 报文，同时通过报文头属性 Accept 和 Content-Type 被指定接收的 MIME 类型和本请求的报文内容均为 applicaion&#x2F;xml。</p>
<p>请求报文被服务器端的 UserController#handle51() 方法正确处理，它根据请求的报文头属性 Accept 决定将服务器端的 User 对象流化为 XML 并返回 HTTP 响应报文，同时指定响应报文的 Content-Type 属性为 application.xml。</p>
<p>如果希望通过 JSON 方式通信，则更改代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//指定请求的报文头信息</span></span><br><span class="line"><span class="type">HttpHeaders</span> <span class="variable">entityHeaders</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">HttpHeaders</span>();</span><br><span class="line"><span class="comment">//JSON</span></span><br><span class="line">entityHeaders.setContentType(MediaType.valueOf(<span class="string">&quot;application/json;UTF-8&quot;</span>)); entityHeaders.setAccept(Collections.singletonList(MediaType.APPLICATION_JSON));</span><br></pre></td></tr></table></figure>

<h4 id="2-6-使用-RestController-和-AsyncRestTemplate"><a href="#2-6-使用-RestController-和-AsyncRestTemplate" class="headerlink" title="2.6 使用 @RestController 和 AsyncRestTemplate"></a>2.6 使用 @RestController 和 AsyncRestTemplate</h4><h5 id="RestController"><a href="#RestController" class="headerlink" title="@RestController"></a>@RestController</h5><p><strong>@RestController</strong> 注解包含了 @ResponseBody 和 @Controller。在控制器上标注 @RestController，就不用再标注 @ResponseBody 和 @Controller了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> <span class="meta">@RestController</span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当使用 REST 风格开发应用程序时，Spring MVC 仅需以下两行配置就行了：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">&quot;com.smart.*&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mvc:annotation-driven</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<h5 id="AsyncRestTemplate"><a href="#AsyncRestTemplate" class="headerlink" title="AsyncRestTemplate"></a>AsyncRestTemplate</h5><p><strong>AsyncRestTemplate</strong> 支持以异步无阻塞方式进行服务访问。</p>
<p>我们模拟一个执行时间为10秒的服务器方法，如果客户端使用 RestTemplate，则将以同步方式进行调用，也就是说客户端代码必须等待服务器执行完毕且返回后才会继续执行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> UserService userService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">UserController</span><span class="params">(UserService userService)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.userService = userService;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping(&quot;/api&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Callable&lt;User&gt; <span class="title function_">api</span><span class="params">()</span>&#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;=====hello&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Callable</span>&lt;User&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> User <span class="title function_">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">                Thread.sleep(<span class="number">10L</span>*<span class="number">1000</span>); <span class="comment">//暂停10s</span></span><br><span class="line">                <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">                user.setUserName(<span class="string">&quot;haha&quot;</span>);</span><br><span class="line">                user.setPassword(<span class="string">&quot;111&quot;</span>);</span><br><span class="line">                <span class="keyword">return</span> user;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>接下来使用 AsyncRestTemplate 以异步的方式进行服务调用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">    <span class="type">AsyncRestTemplate</span> <span class="variable">template</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">AsyncRestTemplate</span>();</span><br><span class="line">        </span><br><span class="line">    <span class="comment">//调用完后立即返回（没有阻塞）</span></span><br><span class="line">    ListenableFuture&lt;ResponseEntity&lt;User&gt;&gt; future = template.getForEntity(<span class="string">&quot;http://localhost:8080/springmvc2/api&quot;</span>, User.class);</span><br><span class="line">        </span><br><span class="line">    <span class="comment">//处理服务器端响应的异步回调方法，待服务器端返回请求响应后，该处注册的回调函数会被自动异步调用</span></span><br><span class="line">    future.addCallback(<span class="keyword">new</span> <span class="title class_">ListenableFutureCallback</span>&lt;ResponseEntity&lt;User&gt;&gt;() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onFailure</span><span class="params">(Throwable throwable)</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;=====client failure : &quot;</span>+ throwable);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onSuccess</span><span class="params">(ResponseEntity&lt;User&gt; userResponseEntity)</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;=====client get result : &quot;</span>+ userResponseEntity.getBody());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    System.out.println(<span class="string">&quot;==no wait&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-7-处理模型数据"><a href="#2-7-处理模型数据" class="headerlink" title="2.7 处理模型数据"></a>2.7 处理模型数据</h4><p>在 MVC 框架中，<strong>控制（C）</strong>是为了产生<strong>模型数据（M）</strong>，而<strong>视图（V）</strong>则是为了渲染模型数据。</p>
<p>在 Spring MVC 中，方法入参绑定请求消息只是处理方法的第一步，还需要根据入参执行相应的逻辑，产生模型数据，导向到特定视图中。</p>
<p>Spring MVC 提供了多种途径输出模型数据：</p>
<ul>
<li><strong>ModelAndView</strong>：可设置逻辑视图名，包含模型数据。</li>
<li><strong>@ModelAttribute</strong>：主要针对于方法入参是对象，以及初始化入参对象功能。</li>
<li><strong>Map 及 Model</strong>：可以提前调用模型中的数据（隐含模型）。</li>
<li><strong>@SessionAttributes</strong>：可用于不同请求共用模型数据。</li>
</ul>
<h5 id="ModelAndView"><a href="#ModelAndView" class="headerlink" title="ModelAndView"></a>ModelAndView</h5><p>控制器处理方法的返回值如果为 ModelAndView，则其既包含视图信息，也包含模型数据信息。</p>
<p>有如下两种方法添加模型数据：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ModelAndView <span class="title function_">addObject</span><span class="params">(String attributeName, Object attributeValue)</span></span><br><span class="line"></span><br><span class="line">ModelAndView <span class="title function_">addAllObjects</span><span class="params">(Map&lt;String,?&gt; modelMap)</span></span><br></pre></td></tr></table></figure>

<p>有如下两种方法设置视图：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//指定一个具体的视图对象</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setView</span><span class="params">(View view)</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">//指定一个逻辑视图名</span></span><br><span class="line"><span class="keyword">void</span> <span class="title function_">setViewName</span><span class="params">(String viewName)</span></span><br></pre></td></tr></table></figure>

<p>ModelAndView 的使用可以参照 1.3 节的实例。</p>
<h5 id="ModelAttribute"><a href="#ModelAttribute" class="headerlink" title="@ModelAttribute"></a>@ModelAttribute</h5><p>用于将方法入参对象添加到模型中，主要用于对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/handle61&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">handle61</span><span class="params">(<span class="meta">@ModelAttribute(&quot;user&quot;)</span> User user)</span>&#123;</span><br><span class="line">    user.setPassword(<span class="string">&quot;123&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;user/createSuccess&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Spring MVC 将请求消息绑定到 User 对象中，再以 @ModelAttribute 后跟的参数为键，将 User 对象作为值添加到模型中，在对视图进行渲染前，Spring MVC 会将模型中的数据转储到视图上下文，进而暴露给视图对象，然后视图对象（xxx.jsp）就可以使用占位符（如${user.userName}）等方式访问模型中的数据了。模型中的数据可以理解为对象所包含的属性，或者请求消息中带过来的数据。</p>
<p>除了可以在方法入参上使用 @ModelAttribute 外，还可以在方法定义处使用。Spring MVC 在调用目标处理方法前，会先逐个调用在方法级上标注了 @ModelAttribute 注解的方法，并将这些方法的返回值添加到模型中。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E5%9C%A8%E6%96%B9%E6%B3%95%E5%AE%9A%E4%B9%89%E5%A4%84%E4%BD%BF%E7%94%A8%E7%9A%84@ModelAttribute.7tiw6dc3wa4.webp" alt="在方法定义处使用的@ModelAttribute"></p>
<h5 id="Map-及-Model"><a href="#Map-及-Model" class="headerlink" title="Map 及 Model"></a>Map 及 Model</h5><p>Spring MVC 使用 <code>org.springframework.ui.Model</code> 接口存储模型数据，<code>org.springframework.ui.ModelMap</code> 实现了 Map 接口，而 <code>org.springframework.ui.ExtendedModelMap</code> 扩展于 ModelMap 的同时实现了 Model 接口。</p>
<p>Spring MVC 在调用方法前会创建一个隐含的模型对象，作为存储模型数据的存储容器，被称之为”隐含模型“。如果处理方法的入参为 Map 或 Model 类型，则 Spring MVC 会将隐含模型的引用传递给这些入参，进而开发者们可以通过这些入参对象访问到隐含模型中的所有数据，也可以其中添加新的数据。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%BD%BF%E7%94%A8ModelMap%E7%9A%84%E5%85%A5%E5%8F%82.ggdiw5j8cw8.webp" alt="使用ModelMap的入参"></p>
<h5 id="SessionAttribute"><a href="#SessionAttribute" class="headerlink" title="@SessionAttribute"></a>@SessionAttribute</h5><p>标注了 @SessionAttribute 注解后，Spring MVC 会将模型中对应的属性暂存到 HttpSession 中，那么就可以在多个请求之间公用这个模型数据了。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%BD%BF%E7%94%A8@SessionAttribute%E6%B3%A8%E8%A7%A3.1i57jq85241s.webp" alt="使用@SessionAttribute注解"></p>
<p>但是上述代码会产生一个 HttpSessionRequiredException，原因如下：</p>
<p>Spring MVC 对 @ModelAttribute 及 @SessionAttribute 的处理遵循一个特定流程，当流程条件不满足时就会报错，这个流程为：</p>
<ol>
<li>Spring MVC 在调用处理方法前，在请求线程中自动创建一个隐含的模型对象。</li>
<li>调用所有标注了 @ModelAttribute 的方法，并将方法返回值添加到隐含模型中。</li>
<li>查看 Session 中是否存在 @SessionAttribute(“xxx”) 所指定的 xxx 属性，如果有，则将其添加到隐含模型中，如果隐含模型中已有 xxx 属性，则该步操作会覆盖模型中已有的属性值。</li>
<li>对标注了 @ModelAttribute(“xxx”) 处理方法的入参按如下流程处理：<ul>
<li>如果隐含模型拥有名为 xxx 的属性，则将其赋给该入参，再用请求消息填充该入参对象直接返回，否则执行下一步。</li>
<li>如果 xxx 是会话属性，即在处理类定义处标注了 @SessionAttribute(“xxx”) ，则尝试从会话中获取该属性，并将其赋给该入参，然后再用请求消息填充该入参对象。如果在会话中找不到对应的属性，则抛出 HttpSessionRequiredException 异常，否则执行下一步。</li>
<li>如果隐含模型中不存在 xxx 属性，且 xxx 也不是会话属性，则创建入参的对象实例，然后再用请求消息填充该入参。</li>
</ul>
</li>
</ol>
<p>解决方法：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%A7%A3%E5%86%B3HttpSessionRequiredException%E9%97%AE%E9%A2%98.6mqu9jth8as0.webp" alt="解决HttpSessionRequiredException问题"></p>
<p>@SessionAttribute 还可以通过模型属性的对象类型指定哪些模型属性可以放到会话中，如 @SessionAttribute(types&#x3D;User.class)。</p>
<p>此外，@SessionAttribute 还可以通过属性名和 types 类型一起指定，二者允许多值。</p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
  </entry>
  <entry>
    <title>精通SpringMVC(六)-文件上传</title>
    <url>/2022/04/11/%E7%B2%BE%E9%80%9ASpringMVC-%E5%85%AD-%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0/</url>
    <content><![CDATA[<p>本系列笔记所有代码地址：<a class="link"   href="https://github.com/lisj98/SpringMVCSource.git" >https://github.com/lisj98/SpringMVCSource.git<i class="fas fa-external-link-alt"></i></a></p>
<p>Spring MVC 通过 <strong>MultipartResolver</strong> 为文件上传提供了支持。Spring 有一个现成的 MultipartResolver 实现类：<strong>CommonsMultipartResolver</strong>。</p>
<p>需要注意的是，<strong>在 Spring MVC 上下文中默认没有装配 MultipartResolver</strong>，因此默认情况下不能处理文件上传的工作，如果想使用文件上传的功能，则需要先在上下文中配置 MultipartResolver。</p>
<h4 id="6-1-配置-MultipartResolver"><a href="#6-1-配置-MultipartResolver" class="headerlink" title="6.1 配置 MultipartResolver"></a>6.1 配置 MultipartResolver</h4><p>下面使用 CommonsMultipartResolver 配置一个 MultipartResolver：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E9%85%8D%E7%BD%AECommonsMultipartResolver.1ip74fi2e82o.webp" alt="配置CommonsMultipartResolver"></p>
<p>defaultEncoding 必须和用户 JSP 的 pageEncoding 属性一致，以便正确读取表单的内容。</p>
<p>uploadTempDir 是文件上传过程中所使用的临时目录，文件上传完成后，临时目录中的临时文件会被自动清除。</p>
<p>为了让 CommonsMultipartResolver 正常工作，必须先将 <code>Jakarta Commons FileUpload</code> 及 <code>Jakarta Commons io</code> 的类包添加到类路径下。</p>
<h4 id="6-2-编写控制器和文件上传表单页面"><a href="#6-2-编写控制器和文件上传表单页面" class="headerlink" title="6.2 编写控制器和文件上传表单页面"></a>6.2 编写控制器和文件上传表单页面</h4><p>在 UserController 中添加一个用于处理用户头像上传的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;uploadPage&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">updatePage</span><span class="params">()</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;uploadPage&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line">    </span><br><span class="line"><span class="meta">@RequestMapping(&quot;/upload&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">updateThumb</span><span class="params">(<span class="meta">@RequestParam(&quot;name&quot;)</span> String name,</span></span><br><span class="line"><span class="params">                          <span class="meta">@RequestParam(&quot;file&quot;)</span>MultipartFile file)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">    <span class="keyword">if</span>(!file.isEmpty())&#123;</span><br><span class="line">        file.transferTo(<span class="keyword">new</span> <span class="title class_">File</span>(<span class="string">&quot;f:/temp/&quot;</span> + file.getOriginalFilename()));</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;success&quot;</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;fail&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Spring MVC 会将上传文件绑定到 MultipartFile 对象中，MultipartFile 提供了获取上传文件内容、文件名等方法，通过其 transferTo() 方法还可将文件存储到硬件中，还包括如下方法：</p>
<ul>
<li>byte[] getBytes()：获取文件字节流数据。</li>
<li>String getContentType()：获取文件 MIME 类型，如 image&#x2F;pjpeg、text&#x2F;plain 等。</li>
<li>InputStream getInputStream()：获取文件流。</li>
<li>String getName()：获取表单中文件组件的名字。</li>
<li>String getOriginFilename()：获取上传文件的原名。</li>
<li>long getSize()：获取文件的字节大小，单位为 Byte。</li>
<li>boolean isEmpty()：判断是否有上传的文件。</li>
<li>void transferTo(File dest)：将上传文件保存到一个目标文件中。</li>
</ul>
<p>负责上传文件的表单和一般表单有一些区别，表单的编码类型必须是 multipart&#x2F;form-data 类型。</p>
<p>uploadPage.jsp 如下：</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">&quot;java&quot;</span> contentType=<span class="string">&quot;text/html; charset=UTF-8&quot;</span> pageEncoding=<span class="string">&quot;UTF-8&quot;</span>%&gt;</span><br><span class="line">&lt;%@ taglib prefix=<span class="string">&quot;c&quot;</span>  uri=<span class="string">&quot;http://java.sun.com/jsp/jstl/core&quot;</span> %&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;title&gt;请上传用户头像&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">&lt;h1&gt;</span><br><span class="line">    请选择上传的头像文件</span><br><span class="line">&lt;/h1&gt;</span><br><span class="line">&lt;form method=<span class="string">&quot;post&quot;</span> action=<span class="string">&quot;&lt;c:url value=&quot;</span>/user/upload.html<span class="string">&quot;/&gt;&quot;</span> enctype=<span class="string">&quot;multipart/form-data&quot;</span>&gt;</span><br><span class="line">    &lt;input type=<span class="string">&quot;text&quot;</span> name=<span class="string">&quot;name&quot;</span> /&gt;</span><br><span class="line">    &lt;input type=<span class="string">&quot;file&quot;</span> name=<span class="string">&quot;file&quot;</span> /&gt;</span><br><span class="line">    &lt;input type=<span class="string">&quot;submit&quot;</span> /&gt;</span><br><span class="line">&lt;/form&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>上传页面：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%B8%8A%E4%BC%A0%E9%A1%B5%E9%9D%A2.1xgpfp5u2aao.webp" alt="上传页面"></p>
<p>上传成功，对应文件夹下已有文件：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%B8%8A%E4%BC%A0%E6%88%90%E5%8A%9F.4z9g1f88fh80.webp" alt="上传成功"></p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
  </entry>
  <entry>
    <title>精通SpringMVC(四)-视图和视图解析器</title>
    <url>/2022/04/09/%E7%B2%BE%E9%80%9ASpringMVC-%E5%9B%9B-%E8%A7%86%E5%9B%BE%E5%92%8C%E8%A7%86%E5%9B%BE%E8%A7%A3%E6%9E%90%E5%99%A8/</url>
    <content><![CDATA[<p>请求处理方法执行完成后，最终返回一个 ModelAndView 对象。对于那些返回 String、View 或 ModelMap 等类型的处理方法，Spring MVC 也会在内部将它们装配成一个 ModelAndView 对象，该对象包含了视图逻辑名和模型对象的信息。</p>
<p>Spring MVC 借助视图解析器（ViewResolver）得到最终的视图对象（View）。</p>
<p>对于最终究竟采取何种视图对象对模型数据进行渲染，处理器并不关心，处理器的工作重点聚焦在生产模型数据的工作上。</p>
<h4 id="4-1-认识视图"><a href="#4-1-认识视图" class="headerlink" title="4.1 认识视图"></a>4.1 认识视图</h4><p>视图的作用是渲染模型数据，将模型里的数据以某种形式呈现给客户。视图对象可以是多种形式，如 JSP、PDF 等，Spring 在 <code>org.springframework.web.servlet</code> 包中定义了一个高度抽象的 View 接口，其中包含了两个方法：</p>
<ul>
<li>String getContentType()：获取视图对应的 MIME 类型，如 text&#x2F;html，application&#x2F;xml 等。</li>
<li>void render(Map model, HttpServletRequest request, HttpServletResponse response)：将模型数据以某种 MIME 类型渲染出来。</li>
</ul>
<p>视图对象是一个 Bean，视图对象由视图解析器负责实例化。</p>
<p>不同类型的视图实现技术对应不同的 View 实现类。常见的有如下几种：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%B8%8D%E5%90%8C%E7%9A%84%E8%A7%86%E5%9B%BE%E5%AE%9E%E7%8E%B0%E7%B1%BB.4wlxocnzqmm0.webp" alt="不同的视图实现类"></p>
<h4 id="4-2-认识视图解析器"><a href="#4-2-认识视图解析器" class="headerlink" title="4.2 认识视图解析器"></a>4.2 认识视图解析器</h4><p>Spring MVC 为逻辑视图名的解析提供了不同的策略，可以在 Spring Web 上下文中配置一种或多种解析策略，并指定它们之间的先后顺序。每种解析策略对应一个具体的视图解析器实现类。视图解析器的工作比较单一，即将逻辑视图名解析为一个具体的视图对象。所有的视图解析器都实现了 ViewResolver 接口，该接口仅有一个方法：</p>
<ul>
<li>View resolveViewName(String viewName, Locale locale)</li>
</ul>
<p>该方法的签名（入参）清楚地展示了视图解析器的工作原理，根据逻辑视图名和本地化对象得到一个视图对象。</p>
<p>Spring 拥有众多的视图解析器实现类，如：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%A7%86%E5%9B%BE%E8%A7%A3%E6%9E%90%E5%99%A8%E5%AE%9E%E7%8E%B0%E7%B1%BB.40vcxglbwfm0.webp" alt="视图解析器实现类"></p>
<p>使用时可以选择单一视图解析器也可以混用多种视图解析器，每个视图解析器都实现了 Ordered 接口并开放出一个 orderNo 属性，可以通过该属性指定视图解析器的优先顺序，值越小优先级越高，值越大优先级越低。有些视图解析器默认为最高优先级（如 ContentNegotiatingViewResolver），而有些则默认为最低优先级（如 InternalResourceViewResolver、XstlViewResolver等）。</p>
<p>Spring MVC 会根据视图解析器的优先级顺序对逻辑视图名进行解析，直到解析成功并返回视图对象，否则将抛出异常。</p>
<h4 id="4-3-JSP-和-JSTL"><a href="#4-3-JSP-和-JSTL" class="headerlink" title="4.3 JSP 和 JSTL"></a>4.3 JSP 和 JSTL</h4><h5 id="1-使用-InternalResourceViewResolver"><a href="#1-使用-InternalResourceViewResolver" class="headerlink" title="1. 使用 InternalResourceViewResolver"></a>1. 使用 InternalResourceViewResolver</h5><p>使用 InternalResourceViewResolver 作为视图解析器的配置如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.InternalResourceViewResolver&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:prefix</span>=<span class="string">&quot;/WEB-INF/views/&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:suffix</span>=<span class="string">&quot;.jsp&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure>

<p>控制器中的相关方法处理完成后返回一个 String 类型的逻辑视图名，InternalResourceViewResolver 对逻辑视图名进行解析，便得到相应的视图对象如 xxx.jsp 等。</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E9%80%BB%E8%BE%91%E8%A7%86%E5%9B%BE%E5%90%8D%E8%A7%A3%E6%9E%90%E4%B8%BAuri%E8%B5%84%E6%BA%90.2mg2rhsyrpq0.webp" alt="逻辑视图名解析为uri资源"></p>
<p>如果 JSP 使用了 JSTL 的国际化功能，确切地说，当 JSP 页面使用 JSTL 的 &lt;fmt:message&#x2F;&gt; 标签时，用户就需要使用 JstlView 替换默认的视图实现类，注意视图解析器和视图对象类不是一回事，如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.InternalResourceViewResolver&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:viewClass</span>=<span class="string">&quot;org.springframework.web.servlet.view.JstlView&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:prefix</span>=<span class="string">&quot;/WEB-INF/views/&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:suffix</span>=<span class="string">&quot;.jsp&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure>

<p>举一个例子演示使用 JSTL 的 &lt;fmt:message&#x2F;&gt; 标签让 JSP 页面实现本地化输出，首先编写 UserController：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping(&quot;/showUserList&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">showUserList</span><span class="params">(ModelMap mm)</span>&#123;</span><br><span class="line">        <span class="type">GregorianCalendar</span> <span class="variable">calendar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GregorianCalendar</span>();</span><br><span class="line">        ArrayList&lt;User&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user1.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">        user1.setRealName(<span class="string">&quot;汤姆&quot;</span>);</span><br><span class="line">        calendar.set(<span class="number">2022</span>, <span class="number">4</span>, <span class="number">9</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user2.setUserName(<span class="string">&quot;john&quot;</span>);</span><br><span class="line">        user2.setRealName(<span class="string">&quot;约翰&quot;</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        userList.add(user1);</span><br><span class="line">        userList.add(user2);</span><br><span class="line">        mm.addAttribute(<span class="string">&quot;userList&quot;</span>, userList);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;user/userList&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再编写 userList.jsp：</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">&quot;java&quot;</span> contentType=<span class="string">&quot;text/html; charset=UTF-8&quot;</span> pageEncoding=<span class="string">&quot;UTF-8&quot;</span>%&gt;</span><br><span class="line">&lt;%@ taglib prefix=<span class="string">&quot;c&quot;</span> uri=<span class="string">&quot;http://java.sun.com/jsp/jstl/core&quot;</span>%&gt;</span><br><span class="line">&lt;%@ taglib prefix=<span class="string">&quot;fmt&quot;</span> uri=<span class="string">&quot;http://java.sun.com/jsp/jstl/fmt&quot;</span>%&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">	&lt;head&gt;</span><br><span class="line">		&lt;title&gt;&lt;fmt:message key=<span class="string">&quot;website.title&quot;</span>/&gt;&lt;/title&gt;</span><br><span class="line">	&lt;/head&gt;</span><br><span class="line">	&lt;body&gt;</span><br><span class="line">		&lt;fmt:message  key=<span class="string">&quot;user.userList.title&quot;</span>/&gt;</span><br><span class="line">		&lt;table&gt;</span><br><span class="line">			&lt;c:forEach items=<span class="string">&quot;$&#123;userList&#125;&quot;</span> <span class="keyword">var</span>=<span class="string">&quot;user&quot;</span>&gt;</span><br><span class="line">				&lt;tr&gt;</span><br><span class="line">					&lt;td&gt;</span><br><span class="line">						&lt;a href=<span class="string">&quot;&lt;c:url value=&quot;</span>/user/showUser/$&#123;user.userName&#125;.html<span class="string">&quot;/&gt;&quot;</span>&gt;</span><br><span class="line">						$&#123;user.userName&#125;</span><br><span class="line">                        &lt;/a&gt;</span><br><span class="line">					&lt;/td&gt;</span><br><span class="line">					&lt;td&gt;</span><br><span class="line">						$&#123;user.realName&#125;</span><br><span class="line">					&lt;/td&gt;</span><br><span class="line">					&lt;td&gt;</span><br><span class="line">						&lt;fmt:formatDate value=<span class="string">&quot;$&#123;topic.createDate&#125;&quot;</span> pattern=<span class="string">&quot;yyyy-MM-dd&quot;</span> /&gt;</span><br><span class="line">					&lt;/td&gt;</span><br><span class="line">				&lt;/tr&gt;</span><br><span class="line">			&lt;/c:forEach&gt;</span><br><span class="line">			&lt;table&gt;</span><br><span class="line">	&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>userList.jsp 所使用的国际化资源在 content 资源文件中定义。</p>
<p>content.properties 配置如下：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">website.title</span>=<span class="string">smart</span></span><br><span class="line"><span class="attr">user.userList.title</span>=<span class="string">user list</span></span><br></pre></td></tr></table></figure>

<p>对应于 JSP 页面中的 key&#x3D;”website.title” 与 key&#x3D;”user.userList.title”，相应的 value 就是 smart 与 userList，采用的是一种键值对的形式，这样不同国家地区的用户在使用这一 JSP 页面时就不必频繁地更改页面了而是只用修改对应地国际化资源文件 content.properties 与 content_zh_CN.properties，国际化资源文件会根据键填充相应的值。</p>
<p>更改 smart-servlet.xml 中 ResourceBundleMessageSource 的配置，添加此国际化资源：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;messageRecouce&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.context.support.ResourceBundleMessageSource&quot;</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;basenames&quot;</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">value</span>&gt;</span>i18n/messages<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">value</span>&gt;</span>i18n/content<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这样，userList.jsp 就会根据 i18n&#x2F;content 来寻找 content.properties 与 content_zh_CN.properties 文件，就可以根据客户端的不同显示相应的本地化页面，除了可以使用 JSTL 的标签外，还能使用 Spring 提供的表单标签。</p>
<h5 id="2-使用表单标签"><a href="#2-使用表单标签" class="headerlink" title="2. 使用表单标签"></a>2. 使用表单标签</h5><p>通过 Spring 表单标签，可以很容易地将模型数据中地表单&#x2F;命令对象（实体类对象）绑定到 HTML 表单元素中。</p>
<p>和使用任何 JSP 扩展标签一样，在使用 Spring 表单标签之前，必须在 JSP 页面中添加一行引用 Spring 表单标签的声明：</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;%@ page language=<span class="string">&quot;java&quot;</span> contentType=<span class="string">&quot;text/html; charset=UTF-8&quot;</span> pageEncoding=<span class="string">&quot;UTF-8&quot;</span>%&gt;</span><br><span class="line">&lt;%--引用表单标签的声明，声明后就可以使用了--%&gt;</span><br><span class="line">&lt;%<span class="meta">@taglib</span> uri=<span class="string">&quot;http://java.sun.com/jsp/jstl/core&quot;</span> prefix=<span class="string">&quot;c&quot;</span> %&gt;</span><br><span class="line">&lt;%@ taglib prefix=<span class="string">&quot;form&quot;</span> uri=<span class="string">&quot;http://www.springframework.org/tags/form&quot;</span> %&gt;</span><br></pre></td></tr></table></figure>

<p>下面是一个使用 &lt;form:form&gt; 表单标签的实例，它将最终生成一个 HTML 的 form 表单：</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;form:form modelAttribute=<span class="string">&quot;user&quot;</span>&gt;</span><br><span class="line">	用户名：&lt;form:input path=<span class="string">&quot;userName&quot;</span>/&gt;&lt;br&gt;</span><br><span class="line">	密码：&lt;form:input path=<span class="string">&quot;password&quot;</span>/&gt;&lt;br&gt;</span><br><span class="line">	真实姓名：&lt;form:input path=<span class="string">&quot;realName&quot;</span>/&gt;&lt;br&gt;</span><br><span class="line">	&lt;input type=<span class="string">&quot;submit&quot;</span> value=<span class="string">&quot;注册&quot;</span> name=<span class="string">&quot;testSubmit&quot;</span>&gt;</span><br><span class="line">	&lt;input type=<span class="string">&quot;reset&quot;</span> value=<span class="string">&quot;重置&quot;</span>&gt;</span><br><span class="line">&lt;/form:form&gt;</span><br></pre></td></tr></table></figure>

<p>一般情况下，通过 GET 请求获取表单页面，通过 POST 请求提交表单页面。</p>
<p>可以通过 modelAttribute 指定绑定的模型属性。如果 modelAttribute 不指定，那么默认从模型中尝试获取名为 command 的表单对象，如果 command 也不存在的话，则将发生错误。通过 action 指定处理表单提交的 URL 地址。此外， &lt;form:form&gt; 标签还拥有多种属性，此处不一一列举。</p>
<p>Spring 提供了十多个表单组件标签，用以绑定表单对象的属性值，这些标签大都拥有如下属性：</p>
<ul>
<li>path：用属性路径表示的表单对象属性。如 userName、dept.deptName、dept.address.tel等。</li>
<li>htmlEscape：表示绑定的表单属性值是否要对 HTML 特殊字符进行转换，默认值为 true。</li>
<li>cssClass：表单组件对应的 CSS 样式类名。</li>
<li>cssErrorClass：当表单组件的数据存在相应的错误时（提交表单后由服务器产生）采用的 CSS 样式类。</li>
</ul>
<p>常见表单标签如下：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/Spring%E8%A1%A8%E5%8D%95%E6%A0%87%E7%AD%BE1.7kxfz79o2cw0.webp" alt="Spring表单标签1"></p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/Spring%E8%A1%A8%E5%8D%95%E6%A0%87%E7%AD%BE2.nkrxn2xxfow.webp" alt="Spring表单标签2"></p>
<h5 id="3-关于复选框、单选框、下拉框和表单对象属性的映射问题"><a href="#3-关于复选框、单选框、下拉框和表单对象属性的映射问题" class="headerlink" title="3. 关于复选框、单选框、下拉框和表单对象属性的映射问题"></a>3. 关于复选框、单选框、下拉框和表单对象属性的映射问题</h5><p>对于以下复选框标签：</p>
<figure class="highlight jsp"><table><tr><td class="code"><pre><span class="line">&lt;form:checkboxes path=<span class="string">&quot;preferences.interests&quot;</span> items=<span class="string">&quot;$&#123;interests&#125;&quot;</span>/&gt;</span><br></pre></td></tr></table></figure>

<p>其生成的对应 HTML 如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">id</span>=<span class="string">&quot;favorites1&quot;</span> <span class="attr">name</span>=<span class="string">&quot;favorites&quot;</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">value</span>=<span class="string">&quot;1&quot;</span> <span class="attr">checked</span>=<span class="string">&quot;checked&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">id</span>=<span class="string">&quot;favorites2&quot;</span> <span class="attr">name</span>=<span class="string">&quot;favorites&quot;</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">value</span>=<span class="string">&quot;2&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">id</span>=<span class="string">&quot;favorites3&quot;</span> <span class="attr">name</span>=<span class="string">&quot;favorites&quot;</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">value</span>=<span class="string">&quot;3&quot;</span> <span class="attr">checked</span>=<span class="string">&quot;checked&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">id</span>=<span class="string">&quot;favorites4&quot;</span> <span class="attr">name</span>=<span class="string">&quot;favorites&quot;</span> <span class="attr">type</span>=<span class="string">&quot;checkbox&quot;</span> <span class="attr">value</span>=<span class="string">&quot;4&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;hidden&quot;</span> <span class="attr">value</span>=<span class="string">&quot;1&quot;</span> <span class="attr">checked</span>=<span class="string">&quot;_favorites&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>为什么 HTML 后面多了一个 hidden 组件呢？因为当 HTML 页面中的所有复选框都没有勾选时，表单提交所对应的 HTTP 请求报文不会包含这个复选框的参数名，这样就给 Spring 的表单绑定机制带来了麻烦。解决方法就是在每个复选框后添加一个隐藏组件，并且在对应的复选框名字前添加一个下划线作为隐藏组件的名字，这样一来，就相当于告诉 Spring MVC ：在这个表单中存在这个复选框，Spring MVC 可以据此保证表单对象和页面表单组件数据的一致性。编写 HTML 页面时应当注意这一点。</p>
<p>复选框、单选框及下拉框都有这个问题。</p>
<h4 id="4-4-模板视图"><a href="#4-4-模板视图" class="headerlink" title="4.4 模板视图"></a>4.4 模板视图</h4><p>FreeMarker 和 Velocity 是除 JSP 外被使用最多的页面模板技术。页面模板编写好页面结构，模板页面中使用一些特殊的变量标识符绑定 Java 对象的动态数据。</p>
<p>虽然 FreeMarker 具有一些编程能力，但通常由 Java 程序准备模型数据，FreeMarker 仅负责基于模板对模型数据进行渲染。</p>
<h5 id="1-编写一个-FreeMarker-模板"><a href="#1-编写一个-FreeMarker-模板" class="headerlink" title="1. 编写一个 FreeMarker 模板"></a>1. 编写一个 FreeMarker 模板</h5><p>重新编写 userList.jsp，并把它放在&#x2F;WEB-INF&#x2F;ftl 目录下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">    &lt;title&gt;smart&lt;/title&gt;</span><br><span class="line">&lt;/head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">用户列表</span><br><span class="line">&lt;table&gt;</span><br><span class="line">	&lt;%-- #list 用于对模型数据中的userList属性对象进行迭代 --%&gt;</span><br><span class="line">    &lt;#list userList as user&gt;</span><br><span class="line">        &lt;tr&gt;</span><br><span class="line">            &lt;td&gt;</span><br><span class="line">            	&lt;%-- 显示模型属性对象中的值 --%&gt;</span><br><span class="line">                &lt;a href=&quot;/user/showUser/$&#123;user.userName&#125;.html&#x27;/&gt;$&#123;user.userName&#125;</span><br><span class="line">                &lt;/a&gt;</span><br><span class="line">            &lt;/td&gt;</span><br><span class="line">            &lt;td&gt;$&#123;user.realName&#125;&lt;/td&gt;</span><br><span class="line">            &lt;%-- 格式化日期 --%&gt;</span><br><span class="line">            &lt;td&gt; $&#123;user.birthday?string(&quot;yyyy-MM-dd&quot;)&#125;&lt;/td&gt;</span><br><span class="line">        &lt;/tr&gt;</span><br><span class="line">    &lt;/#list&gt;</span><br><span class="line">    &lt;/table&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>在 UserController 中添加一个处理方法，如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/showUserListByFtl&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">showUserListByFtl</span><span class="params">(ModelMap mm)</span>&#123;</span><br><span class="line">    ArrayList&lt;User&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    <span class="comment">// do sth</span></span><br><span class="line">    <span class="comment">//以userList为属性名，将User列表添加到模型数据中</span></span><br><span class="line">    mm.addAttribute(<span class="string">&quot;userList&quot;</span>, userList);</span><br><span class="line">    <span class="comment">//逻辑视图名为userListFtl</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;userListFtl&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后就需要在 Spring 上下文中装配好 FreeMarker 的工作环境及 FreeMarker 视图解析器了：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E8%A3%85%E9%85%8DFreeMarker%E5%9F%BA%E7%A1%80%E8%AE%BE%E6%96%BD%E5%92%8C%E8%A7%A3%E6%9E%90%E5%99%A8.60k3mictbwc0.webp" alt="装配FreeMarker基础设施和解析器"></p>
<h5 id="3-使用-Spring-为-FreeMaker-提供的宏"><a href="#3-使用-Spring-为-FreeMaker-提供的宏" class="headerlink" title="3. 使用 Spring 为 FreeMaker 提供的宏"></a>3. 使用 Spring 为 FreeMaker 提供的宏</h5><p>考虑以下如下代码：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;/user/showUser/$&#123;user.userName&#125;.html&quot;</span>&gt;</span>$&#123;user.userName&#125;<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这里使用的 URL 地址没有指定部署路径，如果部署路径不是 “&#x2F;”，则将发生错误。由于应用部署路径通常在最终部署时才能确定，因此不能直接通过硬编码的方式指定。在 JSP 中，通常使用 &lt;c:url&gt; 将 URL 转换为包含应用部署路径的绝对 URL。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;&lt;c:url value=&quot;</span>/<span class="attr">user</span>/<span class="attr">showUser</span>/$&#123;<span class="attr">user.userName</span>&#125;<span class="attr">.html</span>&quot;/&gt;</span>&quot;&gt;$&#123;topic.title&#125;<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>在 FreeMaker 中，一般使用 Spring 的 FreeMaker 宏完成这一功能。此外，Spring 的 FreeMaker 宏还提供了本地化信息显示功能。</p>
<p>使用 Spring 的 FreeMaker 宏：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/%E4%BD%BF%E7%94%A8Spring%E7%9A%84%E5%AE%8F.h3ezhdz1ir4.webp" alt="使用Spring的宏"></p>
<p>Spring 为 FreeMaker 提供的宏有如下几种：</p>
<p><img src="https://cdn.jsdelivr.net/gh/lisj98/image-hosting@master/SpringMVC/Spring-%E4%B8%BA-FreeMaker-%E6%8F%90%E4%BE%9B%E7%9A%84%E5%AE%8F.6fhimc7jtjw0.webp" alt="Spring-为-FreeMaker-提供的宏"></p>
<h4 id="4-5-Excel"><a href="#4-5-Excel" class="headerlink" title="4.5 Excel"></a>4.5 Excel</h4><p>如果希望使用 Excel 展示用户列表，则仅需扩展 Spring 的 AbstractExcelView 或者 AbstractJExcelView 即可。实现 buildExcelDocument() 方法，在方法中使用模型数据对象即可构造 Excel 文档。</p>
<p>下面通过扩展 AbstractExcelView 定义显示用户列表的 Excel 视图类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserListExcelView</span> <span class="keyword">extends</span> <span class="title class_">AbstractExcelView</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">buildExcelDocument</span><span class="params">(Map&lt;String, Object&gt; model, org.apache.poi.hssf.usermodel.HSSFWorkbook workbook, HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        response.setHeader(<span class="string">&quot;Content-Disposition&quot;</span>, <span class="string">&quot;inline;filename=&quot;</span> + <span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;用户列表&quot;</span>.getBytes(), <span class="string">&quot;iso-8859-1&quot;</span>));</span><br><span class="line">        List&lt;User&gt; userList = (List&lt;User&gt;) model.get(<span class="string">&quot;userList&quot;</span>);</span><br><span class="line">        <span class="type">HSSFSheet</span> <span class="variable">sheet</span> <span class="operator">=</span> workbook.createSheet(<span class="string">&quot;users&quot;</span>);</span><br><span class="line">        <span class="type">HSSFRow</span> <span class="variable">header</span> <span class="operator">=</span> sheet.createRow(<span class="number">0</span>);</span><br><span class="line">        header.createCell(<span class="number">0</span>).setCellValue(<span class="string">&quot;账号&quot;</span>);</span><br><span class="line">        header.createCell(<span class="number">1</span>).setCellValue(<span class="string">&quot;姓名&quot;</span>);</span><br><span class="line">        header.createCell(<span class="number">2</span>).setCellValue(<span class="string">&quot;生日&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">rowNum</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">for</span> (User user : userList) &#123;</span><br><span class="line">            <span class="type">HSSFRow</span> <span class="variable">row</span> <span class="operator">=</span> sheet.createRow(rowNum++);</span><br><span class="line">            row.createCell(<span class="number">0</span>).setCellValue(user.getUserName());</span><br><span class="line">            row.createCell(<span class="number">1</span>).setCellValue(user.getRealName());</span><br><span class="line">            <span class="type">SimpleDateFormat</span> <span class="variable">sdf</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;yyyy-MM-dd&quot;</span>);</span><br><span class="line">            <span class="type">String</span> <span class="variable">createDate</span> <span class="operator">=</span> sdf.format(user.getBirthday());</span><br><span class="line">            row.createCell(<span class="number">2</span>).setCellValue(createDate);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>response.setHeader(&quot;Content-Disposition&quot;, &quot;inline;filename=&quot; + new String(&quot;用户列表&quot;.getBytes(), &quot;iso-8859-1&quot;));</code> 处代码让浏览器直接在页面中显示 Excel 文件，而非显示一个对话框提示用户下载或打开文件。如果希望显示一个对话框提示用户下载或打开文件，则可以使用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">response.setHeader(<span class="string">&quot;Content-Disposition&quot;</span>, <span class="string">&quot;attchment;filename=&quot;</span> + <span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;用户列表&quot;</span>.getBytes(), <span class="string">&quot;iso-8859-1&quot;</span>));</span><br></pre></td></tr></table></figure>

<p>编写好 Excel 视图类后，必须在 smart-servlet.xml 中进行相应的配置：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.BeanNameViewResolver&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">p:order</span>=<span class="string">&quot;10&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userListExcel&quot;</span> <span class="attr">class</span>=<span class="string">&quot;com.smart.controller.UserListExcelView&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>由于 UserListExcelView 代表的 Excel 视图对象是一个 Bean，因此我们使用 BeanNameViewResolver 作为视图解析器。</p>
<p>在 UserController 中添加一个相应的请求处理方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/showUserListByXls&quot;)</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">showUserListByXls</span><span class="params">(ModelMap mm)</span>&#123;</span><br><span class="line">    <span class="type">GregorianCalendar</span> <span class="variable">calendar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GregorianCalendar</span>();</span><br><span class="line">    ArrayList&lt;User&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">    user1.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">    user1.setRealName(<span class="string">&quot;汤姆&quot;</span>);</span><br><span class="line">    calendar.set(<span class="number">2022</span>, <span class="number">4</span>, <span class="number">9</span>);</span><br><span class="line">    user1.setBirthday(calendar.getTime());</span><br><span class="line">    <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">    user2.setUserName(<span class="string">&quot;john&quot;</span>);</span><br><span class="line">    user2.setRealName(<span class="string">&quot;约翰&quot;</span>);</span><br><span class="line">    user1.setBirthday(calendar.getTime());</span><br><span class="line">    userList.add(user1);</span><br><span class="line">    userList.add(user2);</span><br><span class="line">    mm.addAttribute(<span class="string">&quot;userList&quot;</span>, userList);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;userListExcel&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在浏览器输入 <code>http://localhost:8080/springmvc4/user/showUserListByXls.html</code> 根据浏览器的不同，将显示或下载一个 Excel 文件。</p>
<h4 id="4-6-PDF"><a href="#4-6-PDF" class="headerlink" title="4.6 PDF"></a>4.6 PDF</h4><p>PDF 视图和 Excel 类似，也使用一个 Bean 作为视图对象。</p>
<p>先编写 PDF 视图类，PDF 视图类需继承 AbstractPdfView：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserListPdfView</span> <span class="keyword">extends</span> <span class="title class_">AbstractPdfView</span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">buildPdfDocument</span><span class="params">(Map&lt;String, Object&gt; model,</span></span><br><span class="line"><span class="params">			Document document, PdfWriter writer, HttpServletRequest request,</span></span><br><span class="line"><span class="params">			HttpServletResponse response)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">		response.setHeader(<span class="string">&quot;Content-Disposition&quot;</span>, <span class="string">&quot;inline; filename=&quot;</span>+ </span><br><span class="line">				<span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;用户列表&quot;</span>.getBytes(), <span class="string">&quot;iso8859-1&quot;</span>));  </span><br><span class="line">		List&lt;User&gt; userList = (List&lt;User&gt;) model.get(<span class="string">&quot;userList&quot;</span>);</span><br><span class="line">		<span class="type">Table</span> <span class="variable">table</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Table</span>(<span class="number">3</span>);</span><br><span class="line">		table.setWidth(<span class="number">80</span>);</span><br><span class="line">		table.setBorder(<span class="number">1</span>);</span><br><span class="line">		table.getDefaultCell().setHorizontalAlignment(Element.ALIGN_CENTER);</span><br><span class="line">		table.getDefaultCell().setVerticalAlignment(Element.ALIGN_MIDDLE);</span><br><span class="line">		</span><br><span class="line">		<span class="type">BaseFont</span> <span class="variable">cnBaseFont</span> <span class="operator">=</span> BaseFont.createFont(<span class="string">&quot;STSongStd-Light&quot;</span>, <span class="string">&quot;UniGB-UCS2-H&quot;</span>, <span class="literal">false</span>);</span><br><span class="line">	    <span class="type">Font</span> <span class="variable">cnFont</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Font</span>(cnBaseFont, <span class="number">10</span>, Font.NORMAL, Color.BLUE);</span><br><span class="line"></span><br><span class="line">		table.addCell(buildFontCell(<span class="string">&quot;帐号&quot;</span>,cnFont));</span><br><span class="line">		table.addCell(buildFontCell(<span class="string">&quot;姓名&quot;</span>,cnFont));</span><br><span class="line">		table.addCell(buildFontCell(<span class="string">&quot;生日&quot;</span>,cnFont));</span><br><span class="line">		<span class="keyword">for</span> (User user : userList) &#123;</span><br><span class="line">			table.addCell( user.getUserName());</span><br><span class="line">			table.addCell(buildFontCell(user.getRealName(),cnFont));</span><br><span class="line">			<span class="type">String</span> <span class="variable">createDate</span> <span class="operator">=</span> DateFormatUtils.format(user.getBirthday(),</span><br><span class="line">					<span class="string">&quot;yyyy-MM-dd&quot;</span>);</span><br><span class="line">			table.addCell(createDate);</span><br><span class="line">		&#125;</span><br><span class="line">		document.add(table);</span><br><span class="line">		</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">private</span> Cell <span class="title function_">buildFontCell</span><span class="params">(String content,Font font)</span> <span class="keyword">throws</span> RuntimeException&#123; </span><br><span class="line">		<span class="keyword">try</span> &#123;</span><br><span class="line">			 <span class="type">Phrase</span> <span class="variable">phrase</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Phrase</span>(content, font);</span><br><span class="line">			 <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Cell</span>(phrase);</span><br><span class="line">		&#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">			<span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">		&#125;		</span><br><span class="line">	&#125;	</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>UserListPdfView 和 UserListExcelView 视图一样，可以采用 BeanNameViewResolver 作为视图解析器，在 smart-servlet.xml 中添加视图 Bean 声明即可：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userListPdf&quot;</span> <span class="attr">class</span>=<span class="string">&quot;com.smart.controller.UserListPdfView&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>同样，在 UserController 中添加一个 showUserListInPdf() 方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/showUserListByPdf&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">showUserListByPdf</span><span class="params">(ModelMap mm)</span>&#123;</span><br><span class="line">        <span class="type">GregorianCalendar</span> <span class="variable">calendar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GregorianCalendar</span>();</span><br><span class="line">        ArrayList&lt;User&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user1.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">        user1.setRealName(<span class="string">&quot;汤姆&quot;</span>);</span><br><span class="line">        calendar.set(<span class="number">2022</span>, <span class="number">4</span>, <span class="number">9</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user2.setUserName(<span class="string">&quot;john&quot;</span>);</span><br><span class="line">        user2.setRealName(<span class="string">&quot;约翰&quot;</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        userList.add(user1);</span><br><span class="line">        userList.add(user2);</span><br><span class="line">        mm.addAttribute(<span class="string">&quot;userList&quot;</span>, userList);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;userListPdf&quot;</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>在浏览器输入 <code>http://localhost:8080/springmvc4/user/showUserListByPdf.html</code> 根据浏览器的不同，将显示或下载一个 PDF 文件。</p>
<h4 id="4-7-输出-XML"><a href="#4-7-输出-XML" class="headerlink" title="4.7 输出 XML"></a>4.7 输出 XML</h4><p>Spring MVC 还可以将模型中的数据以 XML 的形式输出，其对应的视图对象为 MashallingView。</p>
<p>在 smart-servlet.xml 中添加 MashallingView 的配置：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userListXml&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.xml.MarshallingView&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:modelKey</span>=<span class="string">&quot;userList&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:marshaller-ref</span>=<span class="string">&quot;xmlMarshaller&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;xmlMarshaller&quot;</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.oxm.xstream.XStreamMarshaller&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;streamDriver&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;com.thoughtworks.xstream.io.xml.StaxDriver&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;annotatedClasses&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>com.smart.domain.User<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>MarshallingView 使用 Marshaller 将模型数据转换为 XML，通过 marshaller 属性注入一个 Marshaller 实例。在默认情况下，MarshallingView 会将模型中的所有属性都转换为 XML。由于模型属性包含很多隐式数据，直接将模型中的所有数据全部输出往往并不是我们所期望的。MarshallingView 允许通过 modelKey 指定模型中的哪个属性需要输出为 XML。</p>
<p>在 UserController 中添加一个方法，返回 userListXml 逻辑视图名：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/showUserListByXml&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">showUserListByXml</span><span class="params">(ModelMap mm)</span>&#123;</span><br><span class="line">        <span class="type">GregorianCalendar</span> <span class="variable">calendar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GregorianCalendar</span>();</span><br><span class="line">        ArrayList&lt;User&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user1.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">        user1.setRealName(<span class="string">&quot;汤姆&quot;</span>);</span><br><span class="line">        calendar.set(<span class="number">2022</span>, <span class="number">4</span>, <span class="number">9</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user2.setUserName(<span class="string">&quot;john&quot;</span>);</span><br><span class="line">        user2.setRealName(<span class="string">&quot;约翰&quot;</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        userList.add(user1);</span><br><span class="line">        userList.add(user2);</span><br><span class="line">        mm.addAttribute(<span class="string">&quot;userList&quot;</span>, userList);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;userListXml&quot;</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>在浏览器输入 <code>http://localhost:8080/springmvc4/user/showUserListByXml.html</code> 将返回 XML 内容的响应。</p>
<h4 id="4-8-输出-JSON"><a href="#4-8-输出-JSON" class="headerlink" title="4.8 输出 JSON"></a>4.8 输出 JSON</h4><p>Spring MVC 的 MappingJackson2JsonView 借助 Jackson 框架的 ObjectMapper 将模型数据转换为 JSON 格式输出。由于 MappingJackson2JsonView 也是一个 Bean，因此可以通过 BeanNameViewResolver 进行解析，因此仅需在 smart-servlet.xml 中配置即可：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userListJson&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.json.MappingJackson2JsonView&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:modelKeys</span>=<span class="string">&quot;userList&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>默认情况下，MappingJackson2JsonView 会将模型中的所有数据全部输出为 JSON，这显然是不合理的，可以通过 modelKeys 属性指定模型中的哪些属性需要输出。</p>
<p>在 UserController 中添加一个方法，返回 userListJson 逻辑视图名：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/showUserListByJson&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">showUserListByJson</span><span class="params">(ModelMap mm)</span>&#123;</span><br><span class="line">        <span class="type">GregorianCalendar</span> <span class="variable">calendar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GregorianCalendar</span>();</span><br><span class="line">        ArrayList&lt;User&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user1.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">        user1.setRealName(<span class="string">&quot;汤姆&quot;</span>);</span><br><span class="line">        calendar.set(<span class="number">2022</span>, <span class="number">4</span>, <span class="number">9</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user2.setUserName(<span class="string">&quot;john&quot;</span>);</span><br><span class="line">        user2.setRealName(<span class="string">&quot;约翰&quot;</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        userList.add(user1);</span><br><span class="line">        userList.add(user2);</span><br><span class="line">        mm.addAttribute(<span class="string">&quot;userList&quot;</span>, userList);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;userListJson&quot;</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>在浏览器输入 <code>http://localhost:8080/springmvc4/user/showUserListByJson.html</code> 将以 JOSN 格式输出模型中名为 userList 的属性对象。</p>
<h4 id="4-9-使用-XmlViewResolver"><a href="#4-9-使用-XmlViewResolver" class="headerlink" title="4.9 使用 XmlViewResolver"></a>4.9 使用 XmlViewResolver</h4><p>XmlViewResolver 和 BeanNameViewResolver 功能相似，唯一不同的是它可以将视图 Bean 定义在一个独立的 XML 文件中，而不是将所有视图 Bean 都定义在 smart-servlet.xml 中。</p>
<p>要使用 XmlViewResolver，必须在 smart-servlet.xml 中添加：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.XmlViewResolver&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:order</span>=<span class="string">&quot;20&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:location</span>=<span class="string">&quot;WEB-INF/views/smart-views.xml&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>默认情况下，XmlViewResolver 在 WEB-INF&#x2F;views.xml 中查找视图 Bean 的定义，我们将视图 Bean 定义在 WEB-INF&#x2F;views&#x2F;smart-views.xml 中，所以需要通过 location 显示指定。</p>
<p>定义 smart-views.xml 文件，和普通的 Spring 配置文件没有任何区别：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">&quot;http://www.springframework.org/schema/beans&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">&quot;http://www.w3.org/2001/XMLSchema-instance&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">&quot;http://www.springframework.org/schema/context&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:p</span>=<span class="string">&quot;http://www.springframework.org/schema/p&quot;</span> <span class="attr">xmlns:mvc</span>=<span class="string">&quot;http://www.springframework.org/schema/mvc&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">&quot;http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd</span></span></span><br><span class="line"><span class="string"><span class="tag">    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">&quot;com.smart.*&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userListJson1&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.json.MappingJackson2JsonView&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:modelKeys</span>=<span class="string">&quot;userList&quot;</span>/&gt;</span></span><br><span class="line">    </span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;userListExcel1&quot;</span> <span class="attr">class</span>=<span class="string">&quot;com.smart.controller.UserListExcelView&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:annotation-driven</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>不过在 smart-views.xml 文件中定义的 Bean 不能被 Spring Web 上下文的其他 Bean 引用，它是被 XmlViewResolver 独享的。</p>
<h4 id="4-10-使用-ResourceBundleViewResolver"><a href="#4-10-使用-ResourceBundleViewResolver" class="headerlink" title="4.10 使用 ResourceBundleViewResolver"></a>4.10 使用 ResourceBundleViewResolver</h4><p>对于同一个逻辑视图名，如果希望为不同地区的用户提供不同类型的视图，则应该考虑使用 ResourceBundleViewResolver。和 XmlViewResolver 将视图定义在 XML 文件中不同，ResourceBundleViewResolver 通过一个国家化资源文件定义视图对象。</p>
<p>假如有个需求，对同一个页面的展现方式，一个人要求用 HTML 展现，另一个人要求用 PDF 展现，这个时候就可以通过 ResourceBundleViewResolver 来实现。</p>
<p>在 smart-servlet.xml 中添加代码：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.ResourceBundleViewResolver&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:order</span>=<span class="string">&quot;30&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:basenames</span>=<span class="string">&quot;i18n/views&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>通过 basenames 属性指定视图国际化资源文件的基名，对应于中国大陆的视图资源文件为 views_zh_CN.properties，内容为：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">userListi18n.(class)</span>=<span class="string">org.springframework.web.servlet.view.JstlView</span></span><br><span class="line"><span class="attr">userListi18n.url</span>=<span class="string">/WEB-INF/views/user/userList.jsp</span></span><br></pre></td></tr></table></figure>

<p>其中通过 &lt;逻辑视图名&gt;.(class) 属性设置试图实现类。</p>
<p>通过 &lt;逻辑视图名&gt;.url 设置 URL 资源。</p>
<p>对应的 views_en_US.properties 内容为：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">userListi18n.(class)</span>=<span class="string">com.smart.web.UserListPdfView</span></span><br></pre></td></tr></table></figure>

<p>ResourceBundleViewResolver 视图解析器会根据具体的用户请求调用不同的 properties。</p>
<p>在一个视图国际化资源中也可以定义多个视图对象：</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一个视图</span></span><br><span class="line"><span class="attr">welcomeView.(class)</span>=<span class="string">org.springframework.web.servlet.view.JstlView</span></span><br><span class="line"><span class="attr">welcomeView.url</span>=<span class="string">/WEB-INF/jsp/welcome.jsp</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"># 第二个视图</span></span><br><span class="line"><span class="attr">vetsView.(class)</span>=<span class="string">org.springframework.web.servlet.view.JstlView</span></span><br><span class="line"><span class="attr">vetsView.url</span>=<span class="string">/WEB-INF/jsp/vets.jsp</span></span><br><span class="line"><span class="attr">...</span></span><br></pre></td></tr></table></figure>

<h4 id="4-11-混合使用多种视图技术"><a href="#4-11-混合使用多种视图技术" class="headerlink" title="4.11 混合使用多种视图技术"></a>4.11 混合使用多种视图技术</h4><h5 id="1-ContentNegotiatingViewResolver"><a href="#1-ContentNegotiatingViewResolver" class="headerlink" title="1. ContentNegotiatingViewResolver"></a>1. ContentNegotiatingViewResolver</h5><p>先前介绍过使用了 HttpMessageConverter 对标注了 @ResponseBody 或返回值为 ResponseEntity 的处理器方法进行响应信息转换的内容。Spring MVC 可以根据请求报文头的 Accept 属性选择合适的 HttpMessageConverter 将处理方法的返回值以 XML、JSON 等不同形式输出响应。也就是说，调用者可以通过设置请求报文头的 Accept 值控制服务器端返回的数据格式，从而实现对同一资源采用相同 URL 的 REST 编程风格。</p>
<p>但是基于 HttpMessageConverter 的实现方式存在以下限制：</p>
<ul>
<li>只能通过请求报文头的 Accept 值控制服务器端返回的数据格式。如果客户端是浏览器，那么很难控制 Accept 报文头的值，一般情况下，该值是由浏览器自己决定的。</li>
<li>无法通过 URL 扩展名或请求参数控制服务器端的资源输出形式，因此无法将其对应一个 URL 发布出去。</li>
<li>如果希望使用 XML、JSON、一个网页等形式输出资源，则 HttpMessageConverter 很难达到要求。因为 HttpMessageConverter 很难调用一个视图对象渲染模型，它直接负责将资源输出为某一内容形式。</li>
</ul>
<p>所以，如果希望将资源以 XML、JSON 等纯数据的格式输出，且不在意使用报文头控制资源输出，那么适合选择 HttpMessageConverter 实现方式。否则，建议采用 Spring MVC 的 ContentNegotiatingViewResolver 视图解析器。</p>
<p>ContentNegotiatingViewResolver 并不是一个传统的视图解析器，它实际上更像是一个中介人，根据请求信息从上下文中选择一个合适的视图解析器进行解析。因此，一般将 ContentNegotiatingViewResolver 的优先级设为最高，以便它可以方便的选择其他视图解析器。</p>
<p>ContentNegotiatingViewResolver 根据请求所要求的资源类型决定由哪个视图解析器负责处理，它按照如下方式工作：</p>
<ol>
<li>如果其 favorPathExtension 属性设置为 true，则根据 URL 中的文件扩展名确定其 MIME 类型（如userList.xml，userList.json 等）。</li>
<li>如果其 favorPathExtension 属性设置为 true，则根据请求参数的值确定其 MIME 类型。默认的请求参数是 formart。可以通过 parameterName 属性指定一个自定义的参数。</li>
<li>如果还没有找到对应的 MIME 类型，且 Java Activation Framework（JAF）位于类路径下，则通过 JAF 的 FileTypeMap.getContentType(url) 方法对 URL 进行判断，以便获取对应的 MIME 类型。</li>
<li>如果以上步骤都失败，且 ignoreAcceptHeader 属性设置为 false，则采用 Accept 请求报文头的值确定 MIME 类型。由于不同浏览器产生的 Accept 头都是不一样的，所以一般不建议采用 Accept 确定 MIME 类型。</li>
</ol>
<h5 id="2-使用同一-URL-获取不同形式的返回内容"><a href="#2-使用同一-URL-获取不同形式的返回内容" class="headerlink" title="2. 使用同一 URL 获取不同形式的返回内容"></a>2. 使用同一 URL 获取不同形式的返回内容</h5><p>假设希望使用以下 REST 风格的 URL 以不同的 MIME 格式获取相同的资源：</p>
<ul>
<li>&#x2F;user&#x2F;showUserListMix.html：返回一个 HTML 页面显示的用户列表。</li>
<li>&#x2F;user&#x2F;showUserListMix.html?content&#x3D;xml：返回一个 XML 格式的用户列表。</li>
<li>&#x2F;user&#x2F;showUserListMix.html?content&#x3D;json：返回一个 JSON 格式的用户列表。</li>
</ul>
<p>首先，在 UserController 中添加一个处理方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/showUserListMix&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">showUserListMix</span><span class="params">(ModelMap mm)</span>&#123;</span><br><span class="line">        <span class="type">GregorianCalendar</span> <span class="variable">calendar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GregorianCalendar</span>();</span><br><span class="line">        ArrayList&lt;User&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">User</span> <span class="variable">user1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user1.setUserName(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">        user1.setRealName(<span class="string">&quot;汤姆&quot;</span>);</span><br><span class="line">        calendar.set(<span class="number">2022</span>, <span class="number">4</span>, <span class="number">9</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        <span class="type">User</span> <span class="variable">user2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>();</span><br><span class="line">        user2.setUserName(<span class="string">&quot;john&quot;</span>);</span><br><span class="line">        user2.setRealName(<span class="string">&quot;约翰&quot;</span>);</span><br><span class="line">        user1.setBirthday(calendar.getTime());</span><br><span class="line">        userList.add(user1);</span><br><span class="line">        userList.add(user2);</span><br><span class="line">        mm.addAttribute(<span class="string">&quot;userList&quot;</span>, userList);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;userListMix&quot;</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>接着，在 smart-servlet.xml 中添加以下配置片段，配置一个 ContentNegotiatingViewResolver，采用请求参数指定内容资源的返回类型：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;contentNegotiationManager&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.accept.ContentNegotiationManagerFactoryBean&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:ignoreAcceptHeader</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:favorPathExtension</span>=<span class="string">&quot;false&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:favorParameter</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:parameterName</span>=<span class="string">&quot;format&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">p:defaultContentType</span>=<span class="string">&quot;text/html&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;mediaTypes&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">value</span>&gt;</span></span><br><span class="line">                html=text/html</span><br><span class="line">                xml=application/xml</span><br><span class="line">                json=application/json</span><br><span class="line">            <span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 协商多种视图解析器 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.ContentNegotiatingViewResolver&quot;</span> <span class="attr">p:order</span>=<span class="string">&quot;0&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;contentNegotiationManager&quot;</span> <span class="attr">ref</span>=<span class="string">&quot;contentNegotiationManager&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">&quot;defaultViews&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.json.MappingJackson2JsonView&quot;</span></span></span><br><span class="line"><span class="tag">                      <span class="attr">p:modelKeys</span>=<span class="string">&quot;userList&quot;</span>/&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">&quot;org.springframework.web.servlet.view.xml.MarshallingView&quot;</span></span></span><br><span class="line"><span class="tag">                      <span class="attr">p:modelKey</span>=<span class="string">&quot;userList&quot;</span> <span class="attr">p:marshaller-ref</span>=<span class="string">&quot;xmlMarshaller&quot;</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">加上之前配置的视图解析器</span><br></pre></td></tr></table></figure>

<p>ContentNegotiatingViewResolver 会在上下文中查找和 MIME 类型匹配的视图解析器，并委托它们进行视图解析。这样通过 content 请求参数就可以控制资源的输出格式了。</p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
  </entry>
</search>
